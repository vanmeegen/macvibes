{
  "summary": {
    "candidates": 32,
    "confirmed": 11,
    "refuted": 21,
    "unverified": 0
  },
  "tally": [
    {
      "id": "C1",
      "voters": 3,
      "refuted": 2,
      "kept": 1,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Code-Wahrheit: Die Zitate stimmen wörtlich. `/Users/marco/projects/macvibes/apps/server/src/schema/index.ts:409-419` enthält `leaveProject` mit exakt `requireUser(ctx)` (414), `await getProjectAny(ctx, String(args.id))` (415) und `ctx.sandboxManager.leave(String(args.id))` (416); `getProjectAny` (index.ts:152-159) prüft tatsächlich keine Ownership; `SandboxManager.leave` (/Users/marco/projects/macvibes/apps/server/src/sandbox/sandboxManager.ts:118-122) ruft `armGrace` (129-139) → `stop` (164) → `onBeforeStop` → `autoCommit` (/Users/marco/projects/macvibes/apps/server/src/index.ts:195-210). Nichts davon ist halluziniert, und ich habe keinen zusätzlichen Ownership-Guard gefunden.\n\nDie Kausalkette zu einem realen Schaden bricht aber an mehreren Stellen, die der Forscher weggelassen hat:\n1. `enter()` löscht den Grace-Timer (sandboxManager.ts:72 und :78), und `sendMessage` ruft vor JEDEM Turn `ctx.sandboxManager.enter(...)` (schema/index.ts:449-459). Jede Owner-Aktivität innerhalb der 15 Minuten annulliert den Angriff vollständig; nach einem Stopp startet die nächste Nachricht die VM wieder (enter wartet bei Status `stopping` sogar auf `stopPromise`, sandboxManager.ts:65-68).\n2. Der Idle-Timer stoppt exakt dieselbe nicht-beschäftigte Sandbox ohnehin (sandboxManager.ts:204-211, `idleMs` 30 min in config.ts:161). `touch()` wird nur von `enter`/`noteAgentActivity` gesetzt — der Angreifer beschleunigt also nur einen ohnehin terminierten Abbau um maximal `graceMs` (15 min, config.ts:160).\n3. Ein laufender Turn wird nie gekillt: `armGrace` re-armt bei `isBusy` (sandboxManager.ts:133-136, verdrahtet in index.ts:193).\n4. Der `autoCommit` ist kein Schaden, sondern der designierte R9-Speicherpfad, der bei JEDEM Stopp läuft (Idle, LRU, `stopAll`, index.ts:195-210) — kein unerwarteter Git-Zustand auf fremdem Branch.\n5. Fremd ausgelöste Stopps fremder Sandboxes sind bereits bewusst akzeptiert: die LRU-Eviction über den absichtlich ownership-freien `enterProject` stoppt die am längsten untätige fremde Sandbox (sandboxManager.ts:232-258, Kommentar: \"Ownership selbst bleibt außen vor — sonst blockiert ein Einzelner die gesamte Kapazität\").\n6. `leaveProject` ist die bewusste Symmetrie zum R10-freien `enterProject`: /Users/marco/projects/macvibes/apps/web/src/pages/ChatPage.tsx:188-196 armt die Grace-Period explizit auch für Nur-Lese-Besucher; mit Ownership-Zwang würden fremde Sandboxes nach dem Besuch dauerhaft weiterlaufen.\n\nVoraussetzung ist zudem ein admin-freigeschaltetes Konto (authService.ts:99/109/131). Übrig bleibt für einen halb-vertrauenswürdigen angemeldeten Nutzer eine verzögerte, selbstheilende Verfügbarkeits-Nervigkeit ohne Datenverlust und ohne Überschreiten der VM-Grenze — die fehlende Ownership-Prüfung ist real, der behauptete MEDIUM-Impact (\"force-terminate\", \"force-commit\") ist es nicht."
        },
        {
          "lens": "exploitability",
          "refuted": false,
          "confidence": "HIGH",
          "correctedSeverity": "LOW",
          "reasoning": "Kette vollständig im echten Code nachgezogen und nicht gebrochen:\n\n1) Angreiferrolle = beliebiger angemeldeter, freigeschalteter Nutzer (authService.ts:259 verlangt `approved`; schema/index.ts:121-126 `requireUser`). Genau die im Bedrohungsmodell genannte \"angemeldet, aber nicht privilegiert\"-Rolle.\n2) Ziel-IDs sind frei erreichbar: `projects` (apps/server/src/schema/index.ts:212-218) ruft `listProjects(ctx.db)` — projectsService.ts:33-40 filtert NICHT nach Owner, liefert also alle fremden Projekt-IDs.\n3) Sink ohne Autorisierung: `leaveProject` (apps/server/src/schema/index.ts:409-419) macht nur `requireUser` + `getProjectAny`; `getProjectAny` (schema/index.ts:152-159) prüft bewusst keine Ownership. Zeile 416 `ctx.sandboxManager.leave(String(args.id))` stimmt exakt mit dem Kandidaten überein.\n4) Wirkung: sandbox/sandboxManager.ts:118-122 → `armGrace` (129-139) → nach `graceMs` (Default 15 min, config.ts:160) → `stop()` (164-199) → `onBeforeStop` (index.ts:195-210, `autoCommit` im fremden Branch) + VM-Stopp. Kein Viewer-Refcount: `leave()` prüft nur den Entry-Status, nicht wer eingetreten war.\n5) Kein Schutz für den anwesenden Owner: `clearGrace` passiert nur in `enter()` (sandboxManager.ts:72/78), und `enter` wird serverseitig nur von `enterProject` (schema:390) und `sendMessage` (schema:453) aufgerufen. Das UI ruft `enterProject` ausschließlich beim Mount (ChatPage.tsx:191-197); das Polling (ChatPage.tsx:183-186 → ProjectsStore.ts:447-452) macht nur `refresh()` und hält die Grace NICHT ab. `isBusy` (index.ts:193) schützt nur einen gerade laufenden Turn.\n\nWiderlegungsversuche gescheitert: kein Middleware-/Rate-Guard vor der Mutation, kein Owner-Heartbeat, kein Refcount; der Angreifer schadet fremden Nutzern, nicht sich selbst — die DoS-Ausnahme des Auftrags greift also nicht.\n\nSeverity aber auf LOW korrigiert: Der Schaden ist reine, um 15 min verzögerte Verfügbarkeit; der Workspace bleibt persistent, `onBeforeStop`/`autoCommit` ist der reguläre Speicherpfad (kein Datenverlust), und der Owner startet die VM mit einem Seitenaufruf in Sekunden neu. Zudem ist ein gleichwertiges Zwangs-Stoppen fremder, nicht-beschäftigter Sandboxes bereits bewusstes Design (sandboxManager.ts:241-258: \"Ownership selbst bleibt außen vor\") — das mindert den marginalen Zugewinn des Angriffs erheblich. Die Impact-Behauptung \"force-commit\" als Schaden ist überzeichnet."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "correctedSeverity": "LOW",
          "reasoning": "Die Code-Fakten stimmen, die Wirkungskette ist aber durch bereits vorhandene, gelesene Minderungen auf einen Nicht-Befund reduziert.\n\n1) Der Resolver existiert wie beschrieben: apps/server/src/schema/index.ts:409-418 ruft nach `requireUser` nur `getProjectAny` (index.ts:151-158, bewusst ohne Ownership) und dann `ctx.sandboxManager.leave(...)`. Das ist die symmetrische Gegenoperation zu `enterProject` (index.ts:379-391, dort ausdrücklich als R10-Ausnahme kommentiert), das dieselbe UI-Lifecycle-Stelle für JEDES Projekt aufruft (apps/web/src/pages/ChatPage.tsx:191-197). Ein Ownership-Zwang auf `leave` würde den von R10 gewollten Besucher-Pfad (Fremdsandbox starten) ohne Freigabemöglichkeit lassen.\n\n2) `leave` stoppt nichts, es bewaffnet nur einen Timer: apps/server/src/sandbox/sandboxManager.ts:118-131. Der Timer prüft beim Ablauf `isBusy` und re-armt sich, statt einen laufenden Turn zu killen (sandboxManager.ts:126-129, verdrahtet in apps/server/src/index.ts:193). Die Impact-Behauptung „force-terminate running MicroVMs / kills the live preview“ trifft also nie eine arbeitende VM.\n\n3) Jede echte Owner-Aktion löscht den Timer wieder: `sendMessage` ist Owner-only (schema/index.ts:445 `getProjectOwned`) und ruft `sandboxManager.enter` (schema/index.ts:453), das bei `starting`/`running` `clearGrace` ausführt (sandboxManager.ts:70, 77). Ebenso jedes erneute Öffnen der Chat-Page. Wiederholte `leaveProject`-Aufrufe beschleunigen nichts, sondern schieben den Stopp weiter hinaus (armGrace ruft zuerst clearGrace, sandboxManager.ts:123).\n\n4) Der Rest-Delta ist ~15 Minuten: jede laufende Sandbox trägt ohnehin einen Idle-Timer von 30 min (sandboxManager.ts:191-198, config.ts:161), der Grace beträgt 15 min (config.ts:160). Ein Angreifer kann also den ohnehin geplanten Stopp einer untätigen Sandbox um maximal eine Viertelstunde vorziehen. Der `onBeforeStop`-Auto-Commit (apps/server/src/index.ts:195-210) ist kein Schaden, sondern der designierte Datenerhalt jedes Stopps (Idle-Stopp, LRU-Eviction, `stopAll` bei index.ts:374) und schreibt in den eigenen Branch des Owners — R9 (REQUIREMENTS.md:227-231) fordert genau das.\n\n5) Dieselbe (stärkere) Fähigkeit ist bereits bewusst offen: `evictLeastActiveIfNeeded` (sandboxManager.ts:233-258) schließt Ownership explizit aus dem Filter aus („Ownership selbst bleibt außen vor — sonst blockiert ein Einzelner die gesamte Kapazität“) und stoppt die untätigste FREMDE Sandbox sofort inkl. Auto-Commit; erreichbar für jeden angemeldeten Nutzer über den bewusst ungeschützten `enterProject`-Pfad bzw. eigene Projekte. Die F20-Härtung hat die Schutzlinie ausdrücklich bei „busy“ gezogen, nicht bei „fremd“ (Test apps/server/src/sandbox/__tests__/sandboxManager.test.ts:255-269: „eine untätige Sandbox wird weiterhin verdrängt“). `leave` bleibt innerhalb genau dieser bereits akzeptierten Linie und respektiert sie via `isBusy`.\n\nNicht widerlegt bleibt einzig: `leaveProject` ist formal eine Lifecycle-Mutation ohne Ownership-Prüfung, was der Zeile REQUIREMENTS.md:271-273 widerspricht — das ist Hygiene/Defense-in-Depth (LOW), kein MEDIUM-Autorisierungsbefund. Die vom Forscher behaupteten Konsequenzen (Turn-Abbruch, erzwungener Fremd-Commit, „schedule a shutdown of every sandbox“) halten dem Code nicht stand.",
          "confidence": "HIGH"
        }
      ]
    },
    {
      "id": "C2",
      "voters": 3,
      "refuted": 0,
      "kept": 3,
      "survives": true,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "confidence": "HIGH",
          "reasoning": "Alle zitierten Stellen sind wörtlich echt, kein übersehener Guard bricht die Kette.\n\n1) Sink/Gate: /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:249-253 — `subscribe: (_root, args, ctx) => { requireUser(ctx); return ctx.chatService.subscribe(String(args.projectId)); }`, `resolve: (payload) => payload`. Das Snippet des Forschers stimmt auf Zeile 251 exakt. `requireUser` (index.ts:121-126) prüft nur `ctx.currentUser` und wird beim Aufbau des Iterators genau einmal ausgeführt; der per-Event laufende `resolve` (index.ts:253) macht KEINE Prüfung mehr.\n\n2) Kontext einmal pro Request: /Users/marco/projects/macvibes/apps/server/src/http/createAppYoga.ts:95-97 (Pfad im Kandidat ohne `http/`, Zeilen stimmen) — `context: async ({request, ip}) => { const token = await readSessionToken(request); const currentUser = token ? await resolveSession(...) : null; ...}`. Plugins sind nur `useCookies()` und `useCsrfGuard()` (createAppYoga.ts:94), letzterer hängt an `onRequest` — also keine Re-Auth im Stream.\n\n3) Revocation-Absicht belegt: /Users/marco/projects/macvibes/apps/server/src/services/authService.ts:246-262 (Expiry-Löschung) und 259-262 (`if (!user.approved) { await db.delete(sessions)...; return null; }`), `rejectUser` löscht Sessions+User (authService.ts:197-198), `logout` löscht die Session (authService.ts:266-268). Kein einziger dieser Pfade berührt laufende Subscriptions.\n\n4) Aktiv nach dem fehlenden Guard gesucht: `grep -rn \"subscribers\"` in apps/server/src liefert nur chatService.ts:54,149,291,312,321. Der Subscriber wird ausschließlich in `iterator.return()` (chatService.ts:310-315) entfernt, d. h. nur beim Verbindungsabbruch — es gibt keinen serverseitigen Kill-Switch pro User/Session.\n\n5) Die vom Forscher selbst offengelassene Vorbedingung (überlebt der SSE-Stream Buns idleTimeout?) löst sich zu Lasten des Codes auf: /Users/marco/projects/macvibes/apps/server/src/index.ts:320 setzt `idleTimeout: 255`, und graphql-yogas SSE-Prozessor schreibt alle 12 s einen Keep-Alive-Ping (`setInterval(... ':\\n\\n' ..., 12_000)`, node_modules/graphql-yoga/cjs/plugins/result-processor/sse.js:11,32-38). Selbst im ungünstigsten Fall (Timer würde nicht zurückgesetzt) bliebe ein Revocation-Fenster von ~255 s.\n\nImpact bleibt wie eingestuft klein (nur lesender Live-Chat-Stream eines Projekts, kein Mutations-Pfad, Angreifer braucht eine vorher gültige Session und eine bereits offene Verbindung) — LOW ist korrekt. Fällt unter keines der vier bewusst akzeptierten Restrisiken."
        },
        {
          "lens": "exploitability",
          "reasoning": "Die Kette hält der adversarialen Prüfung stand.\n\n1) Quelle/Rolle: Ein angemeldeter, freigeschalteter Nutzer (im Bedrohungsmodell ausdrücklich \"nicht privilegiert\") öffnet `GET /graphql?query=subscription{chatEvents(projectId:\"X\"){...}}`. Die CSRF-Prüfung greift nicht (apps/server/src/http/originPolicy.ts:73-79: ohne Origin und ohne Sec-Fetch-Site wird bewusst durchgelassen — curl mit Cookie kommt durch).\n\n2) Einmalige Autorisierung bestätigt: `subscribe` prüft genau einmal (apps/server/src/schema/index.ts:249-251, exakt die zitierte Zeile 251), der Kontext samt `currentUser` wird genau einmal pro HTTP-Request gebaut (apps/server/src/http/createAppYoga.ts:95-97). Es gibt keinen weiteren Envelop-/Yoga-Plugin-Hook, der neu authentifiziert — die Plugin-Liste ist vollständig `[useCookies(), useCsrfGuard(originsFor)]` (createAppYoga.ts:94).\n\n3) Senke: `ChatService.subscribe` registriert den Push-Callback in `state.subscribers` (apps/server/src/services/chatService.ts:281-296); `publish` bedient alle Subscriber unbesehen (chatService.ts:319-323). Entfernt wird der Callback ausschließlich durch `iterator.return()`, also durch Verbindungsabbruch (chatService.ts:309-315). Weder `rejectUser` (authService.ts:188-199, löscht nur sessions+users) noch `logout` (authService.ts:265-267) noch `purgeExpiredSessions` kennen offene Streams.\n\n4) Die vom Forscher offen gelassene Vorbedingung (\"überlebt die SSE-Verbindung Buns idleTimeout?\") lässt sich ohne Ausführen klären: graphql-yoga schreibt alle 12 s einen Keep-Alive-Ping (node_modules/graphql-yoga/cjs/plugins/result-processor/sse.js:11, 32-38) gegen `idleTimeout: 255` (apps/server/src/index.ts:320) — der Kommentar apps/server/src/index.ts:316-319 nennt die SSE-Chat-Subscription explizit als Grund für den erhöhten Timeout, die Langlebigkeit ist also gewollt und funktioniert im Produktivbetrieb.\n\n5) Schaden real, nicht nur Selbstschaden: Nach `rejectUser` scheitert jeder neue Request des Angreifers, der vorher geöffnete Stream liefert aber weiter jede fremde Nutzer-Prompt und jeden gestreamten Agent-Token des Projekts. Genau diese Garantie formuliert der Code selbst (authService.ts:255-262: \"Ein zurückgezogenes Approval muss bestehende Sessions kappen\").\n\nEinschränkungen (rechtfertigen LOW, nicht Widerlegung): nur Lesen, nur auf vor der Sperrung bekannte Projekt-IDs (Projektliste ist unter R10 für Angemeldete ohnehin lesbar), keine Ausweitung, Ende beim Verbindungsabbruch/Serverneustart. Kein Punkt der vier akzeptierten Restrisiken trifft zu, es ist kein DoS-Befund. Severity LOW des Forschers ist zutreffend.",
          "confidence": "MEDIUM",
          "refuted": false
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": false,
          "confidence": "MEDIUM",
          "correctedSeverity": "LOW",
          "reasoning": "Kette gegen den echten Code verifiziert, keine Minderung gefunden.\n\n1) Einmalige Autorisierung: /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:249-253 — `subscribe: (_root, args, ctx) => { requireUser(ctx); return ctx.chatService.subscribe(...) }`, `resolve: (payload) => payload`. Das Zitat des Forschers stimmt woertlich, Zeile 251 ist korrekt. Weder im Event-Resolver noch in ChatEventRef/ChatMessageRef (index.ts:84-102) findet eine weitere Auth-Pruefung statt.\n\n2) Kontext genau einmal pro Request: /Users/marco/projects/macvibes/apps/server/src/http/createAppYoga.ts:94-97 (`context: async ({request, ip}) => { const token = await readSessionToken(request); const currentUser = token ? await resolveSession(...) : null; }`). Kein Auth-Cache, kein periodisches Re-Resolve (grep auf `cache` in authService.ts/cookies.ts/builder.ts: leer).\n\n3) Abonnent bleibt registriert: /Users/marco/projects/macvibes/apps/server/src/services/chatService.ts:281-317 — `state.subscribers.add(push)` wird nur in `iterator.return()` (Zeile 310-315) entfernt, also ausschliesslich beim Verbindungsabbruch. `logout` (apps/server/src/services/authService.ts:266-268) und `rejectUser` (authService.ts:188-199) loeschen nur Session-/User-Zeilen und fassen chatService-Subscriber nicht an — die im Code selbst formulierte Revocation-Zusage (authService.ts:257-262) greift fuer den laufenden Stream nicht.\n\n4) Die vom Forscher offengelassene Vorbedingung (\"haelt die Verbindung ueber Buns idleTimeout?\") habe ich aufgeloest, und zwar gegen den Kandidaten: /Users/marco/projects/macvibes/node_modules/graphql-yoga/cjs/plugins/result-processor/sse.js:31-37 sendet alle 12 s ein `:\\n\\n`-Ping, damit laeuft der Socket nie in `idleTimeout: 255` (apps/server/src/index.ts:320, dessen Kommentar die SSE-Chat-Subscription ausdruecklich nennt). Transport ist ein einzelner langlebiger GET (apps/web/src/models/ChatStore.ts:207-208, `new EventSource('/graphql?query=...')`); ein Angreifer mit curl haelt eine Verbindung offen und triggert nie den EventSource-Reconnect, der neu authentifizieren wuerde.\n\nGeltungsbereich (meine Linse): Keines der vier akzeptierten Restrisiken (Preview-Origin 4173, VM-Egress, Warmup-Abbruch, bypassPermissions) trifft zu. R10 deckt es NICHT ab: R10 erlaubt Live-Mitlesen fremder Projekte ausdruecklich nur fuer angemeldete Nutzer (REQUIREMENTS.md:271-273) und fordert zugleich \"Logout invalidiert die Session serverseitig\" (REQUIREMENTS.md:265-266) — der Angreifer ist nach der Revocation gerade nicht mehr angemeldet. Gezielt gesuchte Minderungen (CSRF-Guard createAppYoga.ts:38-60, CORS ebd. 84-91, Rate-Limits schema/index.ts:109-118) wirken alle nur auf neue Requests; kein Reverse-Proxy-, Zod- oder Middleware-Fang vorhanden.\n\nEinschraenkung, die LOW rechtfertigt (Forscher-Einstufung ist korrekt): nur Lesezugriff, keine Mutation, Daten identisch zu dem, was der Nutzer vor der Revocation legitim lesen durfte, und es braucht eine vorher aufgebaute Verbindung plus ein Revocation-Ereignis. Es bleibt aber ein vollstaendig belegter CWE-613-Revocation-Bypass und kein reines Code-Qualitaetsthema."
        }
      ]
    },
    {
      "id": "C3",
      "voters": 3,
      "refuted": 3,
      "kept": 0,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "CODE-WAHRHEIT: Alle Zitate des Forschers stimmen wörtlich. `/Users/marco/projects/macvibes/apps/server/src/schema/index.ts:249-251` lautet exakt `subscribe: (_root, args, ctx) => { requireUser(ctx); return ctx.chatService.subscribe(String(args.projectId)); }` — kein Ownership-/Existenz-Check, im Gegensatz zu `sendMessage` (index.ts:446 `getProjectOwned`) und `stopTurn` (index.ts:478 `getProjectOwned`). `/Users/marco/projects/macvibes/apps/server/src/services/chatService.ts:141-153` legt in `state()` bei Miss einen Eintrag an (`this.states.set(projectId, state)` Zeile 151), `subscribe()` ruft das in Zeile 282 auf, und `return()` löscht in Zeile 312 nur `state.subscribers.delete(push)`. `grep \"states\"` in chatService.ts liefert ausschließlich die Zeilen 97/142/151/165 — es gibt tatsächlich KEIN `states.delete` irgendwo im Repo. Auch die behaupteten fehlenden Schutzschichten stimmen: `createAppYoga` (`/Users/marco/projects/macvibes/apps/server/src/http/createAppYoga.ts:94`) lädt nur `useCookies()` und `useCsrfGuard()`, kein Armor/Größen-/Ratelimit-Plugin; `assertWithinLimit` wird nur bei register (index.ts:267) und login (index.ts:305) benutzt; `Bun.serve` (index.ts:313-320) setzt kein `maxRequestBodySize`; `isCrossSiteRequest` (`/Users/marco/projects/macvibes/apps/server/src/http/originPolicy.ts:77-79`) lässt Requests ohne Origin und ohne Sec-Fetch-Site (curl) durch. Ich konnte also KEINEN übersehenen Guard finden — die technische Kette existiert.\n\nTrotzdem refuted, weil der Kandidat AM VORGEGEBENEN BEDROHUNGSMODELL scheitert: (1) Der Einstieg verlangt zwingend eine gültige Session eines vom Admin FREIGESCHALTETEN Nutzers — `requireUser` prüft `ctx.currentUser` (index.ts:121-126), und `resolveSession` verwirft Sessions nicht freigeschalteter Nutzer explizit (`/Users/marco/projects/macvibes/apps/server/src/services/authService.ts:259-262 if (!user.approved) { delete session; return null; }`). Damit ist es exakt der ausgeschlossene Fall „unbegrenzte Map-Keys / fehlende Längenlimits, nur mit gültiger Session auslösbar\". (2) Die Impact-Behauptung ist quantitativ falsch: pro Request wird nur der Key-String plus ein leeres `ProjectChatState` (leeres Array, leeres Set, drei Skalare, chatService.ts:144-150) retained; „Looping this a few hundred times exhausts the heap\" ist um ein bis zwei Größenordnungen daneben — für einen OOM müsste ein authentifizierter Nutzer viele GB an Request-Bodies hochladen. (3) Der offene Lesezugriff selbst ist bewusst so gebaut (chatService.ts:280 „auch für Nur-Lese-Besucher, R10\"), es bleibt also reine Speicher-Hygiene, kein Vertraulichkeits-/Integritätsbruch. Bewertet als Hardening-Empfehlung (states-Lifecycle), nicht als Finding."
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Technische Fakten stimmen, die Ausnutzungskette trägt aber im macvibes-Bedrohungsmodell nicht.\n\nVerifiziert: /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:249-252 reicht `String(args.projectId)` ungeprüft an `ctx.chatService.subscribe()`; /Users/marco/projects/macvibes/apps/server/src/services/chatService.ts:141-152 legt dafür `this.states.set(projectId, state)` an, und grep über die gesamte Datei zeigt nur get/set (Zeilen 142, 151, 165) — kein einziges `delete`; `subscribe`s `return()` entfernt in chatService.ts:311-313 nur den push-Callback. Kein Pothos-Validation-Plugin (/Users/marco/projects/macvibes/apps/server/src/schema/builder.ts:19 `new SchemaBuilder({})`), kein `maxRequestBodySize` in Bun.serve (/Users/marco/projects/macvibes/apps/server/src/index.ts:313-320).\n\nWarum die Kette dennoch reißt:\n1) Rolle: `requireUser(ctx)` (schema/index.ts:250) läuft VOR dem subscribe-Aufruf (Zeile 251), und resolveSession verwirft nicht freigeschaltete Nutzer (/Users/marco/projects/macvibes/apps/server/src/services/authService.ts:259-262). Es braucht also ein vom Admin approvtes Konto — anonym unmöglich. Der per Design untrusted Agent in der VM hat kein Session-Cookie; Preview-Origin-JS (Port 4173) wird geblockt, weil `isCrossSiteRequest` bei gesetztem Origin `!allowed.includes(origin)` liefert (/Users/marco/projects/macvibes/apps/server/src/http/originPolicy.ts; allowedOrigins leitet nur aus dem Host-Header ab) und /Users/marco/projects/macvibes/apps/server/src/http/createAppYoga.ts:44-59 mit 403 abbricht.\n2) Keine Verstärkung: der gespeicherte Key ist exakt der übertragene String — 1 Byte gepinnter Heap pro 1 Byte Upload. Die Behauptung im exploitScenario, „looping this a few hundred times exhausts the heap\" (wenige hundert × 1 MB = wenige hundert MB), ist rechnerisch falsch; für echtes OOM müsste der Angreifer zweistellige Gigabytes hochladen. Das ist Brute-Force-Ressourcenverbrauch ohne Hebel.\n3) Damit fällt der Kandidat exakt in die im Bedrohungsmodell akzeptierte DoS-Klasse („unbegrenzte Map-Keys\") durch einen angemeldeten Nutzer, der per R10 ohnehin fremde Sandboxen starten und beliebig viele Turns/Chat-Zeilen erzeugen darf.\n\nRestbewertung: echter Hygiene-Mangel (states-Map ohne Lifecycle), aber kein sicherheitsrelevantes Finding — im Normalbetrieb entsteht ein Eintrag pro real existierendem Projekt."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Die technischen Einzelfakten stimmen, die Sicherheitsfolge trägt aber nicht.\n\nVerifiziert im echten Code: `apps/server/src/schema/index.ts:249-252` reicht `String(args.projectId)` ungeprüft an `ctx.chatService.subscribe` weiter; `apps/server/src/services/chatService.ts:281` ruft `this.state(projectId)` auf, das in `chatService.ts:151` `this.states.set(projectId, state)` ausführt. `grep` über die ganze Datei zeigt nur drei Zugriffe auf die Map (`chatService.ts:142,151,165`) — es gibt tatsächlich kein `states.delete`, der Eintrag bleibt (`return()` in `chatService.ts:308-313` löscht nur `state.subscribers`).\n\nWarum es trotzdem kein meldepflichtiges Finding ist:\n\n1. Der Autorisierungs-Teil der Empfehlung ist ausdrückliches Design (R10). `REQUIREMENTS.md:271-275`: „Fremde Projekte sind lesend zugänglich: Projektliste, Chat-Verlauf und Preview … **live**: Zuschauer sehen laufende Agent-Turns … über dieselbe Subscription.\" Der Code kommentiert das an `chatService.ts:280` explizit („auch für Nur-Lese-Besucher, R10\"). Die vorgeschlagene `getProjectOwned`-Prüfung würde R10 brechen.\n\n2. Der verbleibende Rest ist reines DoS über unbegrenzte Map-Keys — genau die im Auftrag ausgenommene Kategorie — und nur MIT gültiger, admin-freigeschalteter Session auslösbar: `schema/index.ts:250` `requireUser(ctx)` wirft ohne Session, und `apps/server/src/services/authService.ts:259-262` verwirft Sessions nicht freigeschalteter Nutzer. Unauthentifiziert oder aus der VM heraus ist der Sink nicht erreichbar (CSRF-Guard `apps/server/src/http/createAppYoga.ts:41-60` mit `originPolicy.ts:76` blockt Preview-Origins wie `:4173`, da nur die Host-eigene Origin erlaubt ist).\n\n3. Impact-Delta ist vernachlässigbar: derselbe angemeldete Nutzer darf per Design (R10) fremde Sandboxes starten — `schema/index.ts:379-397` `enterProject` mit `getProjectAny`, und jede Sandbox ist in `apps/server/src/config.ts:171` mit `memoryMib: 4096` bei `maxSandboxes: 8` (`config.ts:162`) dimensioniert. Eine einzige winzige Mutation allokiert 4 GiB; der Map-Angriff ist dagegen 1:1 ohne Amplifikation (nur der hochgeladene String bleibt liegen, `ProjectChatState` selbst ist leer; GraphQL erlaubt bei Subscriptions nur ein Root-Feld, also keine Alias-Vervielfachung).\n\nBleibt: fehlende Existenzprüfung und fehlender Lifecycle für `states` — Robustheit/Code-Qualität, kein Sicherheitsbefund unter diesem Bedrohungsmodell."
        }
      ]
    },
    {
      "id": "C4",
      "voters": 3,
      "refuted": 3,
      "kept": 0,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Code-Wahrheit: Die Zitate des Forschers stimmen wörtlich und zeilengenau. `/Users/marco/projects/macvibes/apps/server/src/schema/index.ts:447-450` enthält tatsächlich nur `const text = args.text.trim();` plus Leer-Prüfung, der Aufruf steht auf :461; `apps/server/src/services/chatService.ts:13` definiert `MAX_MESSAGE_CHARS = 200_000`, :235 schreibt `insertMessage(input.projectId, turnId, 'user', input.text)` ohne Kappung, und der Deckel greift belegbar nur in `appendDelta` auf :522. Ich habe aktiv nach einem übersehenen Guard gesucht: keine Längenprüfung irgendwo in schema/index.ts, kein Validierungs-/Armor-Plugin in `apps/server/src/http/createAppYoga.ts` (nur useCookies + useCsrfGuard), kein `maxRequestBodySize` in `apps/server/src/index.ts:313-320`, Rate-Limit existiert nur für login/register (`apps/server/src/schema/index.ts:105-113`). Die Kette ist auf Code-Ebene also NICHT gebrochen.\n\nWiderlegt wird der Kandidat trotzdem auf der Ebene „echtes Finding im macvibes-Bedrohungsmodell\":\n1. Der Pfad ist ausschließlich für einen angemeldeten UND vom Admin freigeschalteten Projekt-Eigentümer erreichbar — `requireUser` (schema/index.ts:121-126, aufgerufen :445), `getProjectOwned` mit Ownership-Check (schema/index.ts:137-150), Freischaltungs-Gate in `apps/server/src/services/authService.ts:109` und :131. Damit ist es exakt der vom Auftrag ausgeschlossene Fall „DoS-artiges Finding (fehlende Längenlimits) durch einen angemeldeten Nutzer\".\n2. Der laut Bedrohungsmodell untrusted Akteur (der Agent/Daemon in der VM) trifft auf diesem Pfad gerade NICHT: Agent-Inhalte laufen über `appendDelta` und werden bei chatService.ts:522 gedeckelt. Der ungedeckelte Pfad gehört dem menschlichen Eigentümer.\n3. Die behauptete Wirkung „Platte des Hosts füllen, auf dem auch fremde Projekte liegen\" ist für denselben Akteur bereits per Design verfügbar: Der Workspace ist ein ungequoteter Host-Ordner (`apps/server/src/services/workspaceService.ts:18-20`), der read-write in die VM gemountet wird (`apps/server/src/sandbox/microsandboxProvider.ts:166`, `${workspaceDir}:${GUEST_WORKDIR}`); ein Quota-/Größenlimit existiert nirgends im sandbox-Verzeichnis. Ein freigeschalteter Nutzer kann den Agenten (bypassPermissions, akzeptiertes Restrisiko 4) schlicht eine große Datei schreiben lassen — sendMessage verschafft ihm keine neue Fähigkeit.\n\nErgebnis: Zitat echt, aber der beschriebene Effekt ist ein authentifizierter Selbst-DoS bzw. eine Fähigkeit, die die akzeptierte Architektur demselben Akteur ohnehin gibt. Kein eigenständiges Finding."
        },
        {
          "lens": "exploitability",
          "reasoning": "Der technische Befund stimmt (kein Längenlimit auf `args.text`), die Ausnutzungskette trägt im Bedrohungsmodell aber nicht.\n\n1) Rolle: Kein anonymer Zugriff. `sendMessage` verlangt `requireUser` und `getProjectOwned` (apps/server/src/schema/index.ts:445-446); `requireUser` wirft ohne Session (index.ts:121-126), `getProjectOwned` erzwingt Eigentum (index.ts:146-148). Jede Session-Auflösung prüft zusätzlich `approved` (apps/server/src/services/authService.ts:258-262), und nur der ERSTE Nutzer wird automatisch freigeschaltet (authService.ts:99). Nötig ist also ein vom Admin freigeschalteter Nutzer, der auf SEIN EIGENES Projekt schreibt — genau der Fall, den das Bedrohungsmodell als \"angemeldeter Nutzer macht seinen eigenen Server langsam / fehlendes Längenlimit mit gültiger Session\" ausschließt.\n\n2) Der per Design nicht vertrauenswürdige Agent in der VM kann diesen Pfad NICHT auslösen: Auth hängt am Session-Cookie, dazu kommt der CSRF-Guard (apps/server/src/http/createAppYoga.ts:40-62), der Cross-Site-Mutationen mit 403 beendet. Kein VM-Ausbruchs- oder Grenzübertritts-Anteil.\n\n3) Der behauptete Kernschaden \"Plattenerschöpfung auf dem Host, der auch alle anderen Projekte hält\" ist keine neue Fähigkeit: der schwächere, bewusst nicht vertrauenswürdige Akteur (Code in der VM) schreibt bereits unbegrenzt auf dieselbe Host-Platte — der Workspace liegt unter `~/macvibes/<projekt>/workspace` (apps/server/src/services/workspaceService.ts:18-20) und wird read-write in die VM gemountet (apps/server/src/sandbox/microsandboxProvider.ts:166 `${workspaceDir}:${GUEST_WORKDIR}`). Der fehlende Chat-Deckel fügt dem nichts hinzu.\n\n4) Die Sekundärschäden treffen praktisch nur den Angreifer selbst: `publish` fächert nur an Abonnenten GENAU DIESES Projekts (apps/server/src/services/chatService.ts:320-324), und Abonnent wird man nur durch aktives Öffnen des fremden Projekts (schema/index.ts:244-252). `withHistoryContext` (chatService.ts:408) läuft nur im eigenen Projekt und kappt den Verlauf danach auf 8.000 Zeichen (chatService.ts:414-417); der Speicherausschlag ist transient und durch die selbst geschriebenen Zeilen begrenzt.\n\nDamit: gültige Session + Admin-Freigabe + Eigentum als Vorbedingung, Schaden im Wesentlichen selbstzugefügt, und der einzige geteilte Effekt (Host-Platte) ist bereits designbedingt vom untrusted Gast erreichbar. Kein Finding.",
          "confidence": "HIGH",
          "refuted": true
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Der Faktenkern des Kandidaten stimmt, die sicherheitsrelevante Einordnung nicht.\n\nVerifiziert: /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:447-450 trimmt `args.text` nur und prüft auf Leere; /Users/marco/projects/macvibes/apps/server/src/services/chatService.ts:235 schreibt ihn ungekappt per `insertMessage`. Ein Zod-`max()` dafür existiert nicht (packages/shared/src/validation/* hat nur previewPort- und Projektnamen-Limits), und `sendMessage` ist nicht rate-limitiert (Limiter nur login/register, schema/index.ts:109-110). Der Deckel greift tatsächlich nur in `appendDelta` (chatService.ts:520-523).\n\nWarum es trotzdem kein zu meldendes Finding ist:\n\n1. Kein Pfad ohne gültige Session: schema/index.ts:445 `requireUser` (Def. :121-126) und :446 `getProjectOwned` (Def. :137-150, wirft bei fremdem Owner). Nötig ist ein vom Admin freigeschaltetes Konto (apps/server/src/services/authService.ts:99 und :109 `approved`). Damit fällt der Kandidat exakt in die im Bedrohungsmodell ausgenommene DoS-Klasse „fehlende Längenlimits — nur real ohne gültige Session\".\n\n2. Die behauptete Wirkung (Platten-Erschöpfung des Hosts, der auch fremde Projekte hält) ist nicht additiv: derselbe angemeldete Nutzer kann per Design beliebig viel auf dieselbe Host-Platte schreiben, denn der Workspace ist ein ungequoteter Bind-Mount eines Host-Verzeichnisses in die VM (apps/server/src/sandbox/microsandboxProvider.ts:166-168, `-v ${workspaceDir}:${GUEST_WORKDIR}`), und der Agent dort läuft mit bypassPermissions (bewusst akzeptiertes Restrisiko 4). Ein Zeichenlimit im Chat nimmt diese Fähigkeit nicht weg.\n\n3. Die Prämisse „der dokumentierte Schutz deckt den Nutzerpfad fälschlich nicht ab\" ist eine Fehllesung: chatService.ts:8-13 und security-haertung-plan.md:459-463 benennen als Akteur ausdrücklich den „kompromittierten Daemon\", also die echte Vertrauensgrenze VM. Genau dort ist der Deckel gesetzt und getestet (apps/server/src/services/__tests__/chatService.test.ts:948-977). Der Nutzerpfad überschreitet keine Vertrauensgrenze.\n\n4. Ergänzend ist die Einzelanfrage durch Buns Default begrenzt (node_modules/bun-types/serve.d.ts:674-678, `maxRequestBodySize` @default 128MB); apps/server/src/index.ts:314-320 überschreibt ihn nicht.\n\n5. Die Speicher-Behauptung zu `withHistoryContext` ist überzeichnet: der zusammengebaute Verlauf wird auf 8_000 Zeichen gekappt (chatService.ts:413-417), und der Pfad läuft nur ohne `--resume`.\n\nErgebnis: Robustheits-/Härtungslücke innerhalb des bewusst akzeptierten Rahmens (authentifizierter Nutzer, eigenes Projekt, gleiche Wirkung bereits per Design erreichbar) — kein Sicherheits-Finding."
        }
      ]
    },
    {
      "id": "C5",
      "voters": 3,
      "refuted": 0,
      "kept": 3,
      "survives": true,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "confidence": "HIGH",
          "reasoning": "Linse CODE-WAHRHEIT: Alle Zitate stimmen wörtlich mit HEAD 34939c69 überein, kein übersehener Guard gefunden.\n\nVerifiziert:\n- /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:303-308 (auch via `git show HEAD:` gegengeprüft): der `login`-Resolver ruft OHNE jede Authentifizierung und OHNE Längen-/Formatprüfung `assertWithinLimit(loginLimiter, ['ip:…', `user:${args.username.toLowerCase()}`])`. Zeile 307 ist exakt das zitierte Snippet.\n- /Users/marco/projects/macvibes/apps/server/src/services/rateLimiter.ts:38-45: `hits.set(key, recent)` speichert den vom Angreifer bestimmten String als Map-Key; gelöscht wird ausschließlich im Zweig `if (hits.size > 1000)` (Zeile 41) und dort nur abgelaufene Einträge (Zeile 43). Bei wenigen Riesen-Keys bleibt `hits.size` weit unter 1001 → nie ein Delete, prozesslebenslange Retention. Die vom Forscher genannten Zeilen 38/39/41/43 stimmen exakt.\n- /Users/marco/projects/macvibes/apps/server/src/services/authService.ts:116-136: `login()` validiert `username` NICHT — Zeile 122 ist wörtlich wie zitiert. Der Kontrast ist belegt: `register()` prüft mit `usernameSchema` (authService.ts:57) und dieses Schema begrenzt auf 3–32 Zeichen (packages/shared/src/validation/auth.ts:8-13) — für `login` läuft es aber nie.\n- Kein Body-Limit auf dem Weg: /Users/marco/projects/macvibes/apps/server/src/index.ts:313-322 startet `Bun.serve` ohne `maxRequestBodySize`; der Bun-Default ist 128 MB (node_modules/bun-types/serve.d.ts:676-678). Route zu Yoga: index.ts:328-330 ohne jede Größenprüfung.\n- Aktiv nach Guards gesucht und keine gefunden: createAppYoga (/Users/marco/projects/macvibes/apps/server/src/http/createAppYoga.ts:94) lädt nur `useCookies()` und `useCsrfGuard()`; kein Pothos-Validation-Plugin (schema/builder.ts:19), kein graphql-armor, kein maxTokens (grep in node_modules/graphql-yoga leer). Der CSRF-Guard bricht die Kette NICHT: /Users/marco/projects/macvibes/apps/server/src/http/originPolicy.ts:78 gibt bei fehlendem Origin UND fehlendem Sec-Fetch-Site bewusst `false` zurück — ein curl-POST passiert ungehindert.\n- Der einzige echte Abschalter ist `rateLimitDisabled()` (schema/index.ts:113, rateLimiter.ts:56, `MACVIBES_RATE_LIMIT_DISABLED=1`), der per Default NICHT gesetzt ist; die Vorbedingung ist im Kandidaten korrekt genannt.\n\nErreichbarkeit passt zum Bedrohungsmodell: /graphql hängt an derselben Bun.serve wie das Agent-Gateway auf `config.port` (index.ts:325-331), das die VM per `host.microsandbox.internal:${config.port}` (index.ts:161) erreicht — die per Design nicht vertrauenswürdige VM UND jeder LAN-Peer können den Pfad ohne Session auslösen. Damit ist es kein \"angemeldeter Nutzer bremst seinen eigenen Server\", sondern unauthentifiziert.\n\nRatenschranke ist korrekt beschrieben: der `ip:`-Key wird zuerst geprüft, also werden pro IP nur 20 Riesen-Keys pro 5-Minuten-Fenster eingefügt — bei 128 MB Body sind das ~2,5 GB dauerhaft gehaltener Speicher je Fenster, was der Kandidat exakt so darstellt.\n\nNicht verifizierbar (weil nicht ausführbar): die tatsächliche JSC-String-/Rope-Flattening-Byte-Zahl und ob ein 128-MB-JSON-Body real durchparsed wird. Das ändert nur die Geschwindigkeit, nicht den im Code lesbaren Retentionsmechanismus. MEDIUM ist angemessen."
        },
        {
          "lens": "exploitability",
          "reasoning": "Die Kette hält von Anfang bis Ende und ist für einen ANONYMEN Netzwerk-Peer im LAN auslösbar — ich konnte sie an keiner Stelle brechen.\n\n1) Erreichbarkeit ohne Session: `apps/server/src/config.ts:151` bindet per Default `0.0.0.0`; `apps/server/src/index.ts:328-330` leitet POST `/graphql` ungefiltert an Yoga. Der CSRF-Guard greift NICHT bei einem curl/Skript-Request ohne `Origin` und ohne `Sec-Fetch-Site`: `apps/server/src/http/originPolicy.ts:79` gibt in genau diesem Fall bewusst `false` zurück. `login` ist per Definition unauthentifiziert.\n\n2) Unvalidierte, längenunbegrenzte Quelle: `apps/server/src/schema/index.ts:299` deklariert `username: t.arg.string({ required: true })` ohne jede Längenprüfung; der Pothos-Builder (`apps/server/src/schema/builder.ts:19`) hat KEIN Validation-Plugin, und `createAppYoga` lädt nur `useCookies()` und `useCsrfGuard()` (`apps/server/src/http/createAppYoga.ts:94`) — keine Body-/Query-Size-Begrenzung. `usernameSchema` läuft nur in `register` (`apps/server/src/services/authService.ts:57`), `login` (`authService.ts:116-122`) validiert nichts. `Bun.serve` in `apps/server/src/index.ts:313` setzt kein `maxRequestBodySize`, also gilt Buns Default von 128 MB (`node_modules/bun-types/serve.d.ts:676-678`) — die Obergrenze pro Request, nicht null.\n\n3) Senke: `apps/server/src/schema/index.ts:305-308` schreibt `user:${args.username.toLowerCase()}` als Map-Key, BEVOR `login()` (Zeile 309) überhaupt läuft. In `apps/server/src/services/rateLimiter.ts:39` landet der Key in der prozessweiten Map (`rateLimiter.ts:25`, Modul-Level-Instanz `schema/index.ts:109`). Aufgeräumt wird ausschließlich in `rateLimiter.ts:41` bei `hits.size > 1000` — bei 20 Riesen-Keys pro Fenster wird diese Schwelle nie erreicht, die Strings bleiben für die Prozesslebensdauer liegen. Es gibt keinen periodischen `reset()`/Cleanup (grep über apps/server/src: kein Aufruf außerhalb der Tests).\n\n4) Reihenfolge/Deckelung geprüft: Der IP-Key wird zuerst geprüft, d.h. pro IP sind 20 Versuche/5 min möglich — das deckelt auf ~20 × 128 MB ≈ 2,5 GB dauerhaft gehaltenen Speicher pro Fenster und IP, akkumuliert aber über alle Fenster unbegrenzt weiter. Auf einem LAN ist der Upload dafür in Sekunden bis Minuten machbar.\n\nKeine der bewusst akzeptierten Restrisiken (Preview-Origin, Egress, Warmup, bypassPermissions) trifft zu, und die DoS-Einschränkung des Bedrohungsmodells greift nicht: der Angreifer braucht KEINE Session und schadet nicht sich selbst, sondern reißt Server, Previews und laufende Sandboxes mit. Einzige nicht verifizierbare Restunsicherheit: der tatsächliche OOM-Zeitpunkt hängt vom Host-RAM ab (nicht ausführbar hier) — die unbegrenzte, nie freigegebene Retention ist aber zeilengenau belegt. Severity MEDIUM ist angemessen (Verfügbarkeit, keine Datenkompromittierung).",
          "refuted": false,
          "confidence": "HIGH"
        },
        {
          "lens": "scope-and-mitigation",
          "reasoning": "Ich habe jede Behauptung gegen HEAD 34939c69 geprüft; der Kandidat hält meinem Widerlegungsversuch stand.\n\nBelege (echte Zeilen):\n- Quelle: `apps/server/src/schema/index.ts:300` — `username: t.arg.string({ required: true })` ohne jede Längen-/Formatbeschränkung; die unauthentifizierte `login`-Mutation.\n- Senke: `apps/server/src/schema/index.ts:305-307` — `assertWithinLimit(loginLimiter, ['ip:…', \\`user:${args.username.toLowerCase()}\\`])` läuft VOR `login(...)` (Zeile 309). Der Template-String wird in `apps/server/src/services/rateLimiter.ts:39` (`hits.set(key, recent)`) als Map-Key abgelegt; `loginLimiter` ist modulweit (`schema/index.ts:109`), lebt also prozesslang. Kein `reset()` im Produktivcode (grep über apps/server/src: keine Treffer außerhalb der Interface-Definition).\n- Kein Aufräumen: `rateLimiter.ts:41` prunet nur bei `hits.size > 1000`. Da `assertWithinLimit` den `ip:`-Key zuerst zählt (max 20/5 min, `schema/index.ts:109`), entstehen nur ~20 Riesen-Keys pro Fenster und pro IP — die 1000er-Schwelle wird ~50 Fenster lang NICHT erreicht, die Riesen-Strings bleiben bis dahin liegen.\n\nGesuchte Minderungen — keine gefunden:\n- Kein Body-Limit: `apps/server/src/index.ts:313` `Bun.serve({...})` setzt kein `maxRequestBodySize`; der Default steht dokumentiert in `node_modules/bun-types/serve.d.ts:676` (`@default 1024 * 1024 * 128 // 128MB`). Also bis 128 MB pro Request.\n- Keine Schema-/Zod-Validierung davor: `apps/server/src/services/authService.ts:57` benutzt `usernameSchema` nur in `register`; `login` (authService.ts:116-121) validiert nichts — und läuft ohnehin erst nach der Map-Eintragung.\n- Keine Yoga-Schutzplugins: `apps/server/src/http/createAppYoga.ts:94` lädt nur `useCookies()` und `useCsrfGuard(...)`; `apps/server/package.json` enthält kein graphql-armor/Validation-Plugin.\n- CSRF/Origin greift nicht: `apps/server/src/http/originPolicy.ts:77-79` lässt Requests ohne `Origin` und ohne `Sec-Fetch-Site` bewusst durch — ein curl/Skript-Angreifer ohne Session passiert die Middleware.\n- Kein Reverse-Proxy im Repo (keine Caddy-/nginx-/compose-Datei), `index.ts` routet `/graphql` ungefiltert an `yoga.fetch` (index.ts:325-328).\n- Rate-Limit ist per Default aktiv (`rateLimiter.ts:55-57`, `MACVIBES_RATE_LIMIT_DISABLED` unset), d.h. der Pfad in die Map wird gerade im Default-Betrieb genommen.\n\nGeltungsbereich: Fällt unter KEINES der vier akzeptierten Restrisiken (Port-4173-Origin, VM-Egress, Warmup-Abbruch, bypassPermissions) und nicht unter R10. Der DoS-Filter des Bedrohungsmodells greift ebenfalls nicht: der Angreifer braucht KEINE gültige Session und keinen Account, nur LAN-Erreichbarkeit des auf 0.0.0.0 gebundenen Servers. Es ist auch nicht reine Code-Qualität: der Unterschied zu gewöhnlichem Upload-Flooding ist die Persistenz — 20 Requests à 128 MB hinterlassen ~2,5 GB, die nach Requestende NIE freigegeben werden.\n\nEinzige Restunsicherheit (daher confidence MEDIUM): die tatsächliche OOM-Schwelle des Hosts lässt sich ohne Ausführung nicht messen; die Speicherretention selbst folgt aber direkt aus rateLimiter.ts:39/41. Severity MEDIUM ist angemessen (nur Verfügbarkeit, erfordert GB-Uploads).",
          "refuted": false,
          "confidence": "MEDIUM",
          "correctedSeverity": "MEDIUM"
        }
      ]
    },
    {
      "id": "C6",
      "voters": 3,
      "refuted": 3,
      "kept": 0,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "CODE-WAHRHEIT: Alle Zitate des Kandidaten stimmen exakt. `apps/server/src/schema/index.ts:250-251` lautet wörtlich `requireUser(ctx);` / `return ctx.chatService.subscribe(String(args.projectId));` — keinerlei Existenz-, Besitz- oder Formatprüfung der ID. `apps/server/src/services/chatService.ts:281-282` (`subscribe(projectId)` → `this.state(projectId)`) und die Factory `chatService.ts:141-153` mit `this.states.set(projectId, state)` (Zeile 151) existieren wie beschrieben; `chatService.ts:312` löscht im `return()` tatsächlich nur `state.subscribers.delete(push)`. Ein repo-weiter grep über `states` liefert exakt vier Treffer (`chatService.ts:97,142,151,165`) — es gibt nirgends ein `states.delete`/`clear`, die Map wird nie evakuiert. Auch ein übersehener Guard fehlt: `builder.ts:19` konfiguriert keine eigenen Scalars (Pothos-Default-ID validiert nichts), `createAppYoga.ts:94` lädt nur `useCookies()` und `useCsrfGuard()` (kein Armor/Body-Limit/Complexity-Plugin), und `index.ts:313-320` setzt kein `maxRequestBodySize`. Der Schreibpfad `sendMessage` ist dagegen durch `getProjectOwned` (`schema/index.ts:445-446`) abgesichert, d.h. die Subscription ist wirklich der einzige unvalidierte Weg in `state()`.\n\nTROTZDEM kein Finding: Die Kette endet zwingend hinter `requireUser` (`schema/index.ts:121-126`), und `resolveSession` (`apps/server/src/services/authService.ts:259-262`) löscht Sessions nicht freigeschalteter Nutzer — es ist also strikt eine gültige, freigeschaltete Session nötig. Damit fällt der Kandidat exakt in die im Bedrohungsmodell benannte, bewusst akzeptierte DoS-Klasse (\"unbegrenzte Map-Keys, fehlende Längenlimits\" durch einen angemeldeten Nutzer). Zusätzlich ist die Wirkung 1:1 ohne Verstärkung: der gepinnte Heap entspricht den vom Angreifer übertragenen Bytes (der Map-Key IST das hochgeladene String-Payload), die Behauptung \"ein paar Dutzend Aufrufe pinnen Gigabytes\" verlangt also das Hochladen von Gigabytes. Ein angemeldeter Nutzer kann per Design (R10) ohnehin fremde Sandboxes/MicroVMs starten; die VM-Grenze wird hier nicht berührt. Meine Widerlegung stützt sich bewusst NICHT auf einen im Code gefundenen Guard (den gibt es nicht), sondern auf die Session-Voraussetzung, die fehlende Verstärkung und den expliziten Akzeptanz-Carve-out — das sollte das Panel bei der Gewichtung wissen."
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "HIGH",
          "correctedSeverity": "LOW",
          "reasoning": "Die technischen Einzelbehauptungen stimmen, die Ausnutzbarkeitskette reißt aber am Bedrohungsmodell.\n\nCode-Verifikation (Datei/Zeilen stimmen, der Pfad im Kandidaten ist ungenau — die Datei liegt unter apps/server/src/services/chatService.ts):\n- apps/server/src/schema/index.ts:249-252: `subscribe: (_root, args, ctx) => { requireUser(ctx); return ctx.chatService.subscribe(String(args.projectId)); }` — keine Existenz-/Längen-/Ownership-Prüfung.\n- chatService.ts:281-282 -> 142/151: `state()` legt den Angreifer-String als Map-Key an; `grep` über die ganze Datei zeigt nur `states.get` (142, 165) und `states.set` (151) — kein einziges `states.delete`. chatService.ts:312 löscht tatsächlich nur den Callback. Der Leak existiert also.\n\nWarum trotzdem kein Finding:\n1. Zwingende Vorbedingung ist ein FREIGESCHALTETER Account: schema/index.ts:121-126 wirft ohne `ctx.currentUser`, und services/authService.ts:259-262 löscht die Session, sobald `user.approved` falsch ist. Anonyme Angreifer und Code in der VM (der keine Session besitzt; die VM erreicht den Host nur über den Agent-Gateway-WS bzw. /anthropic/*) kommen nicht an `subscribe()` heran — `requireUser` läuft VOR `chatService.subscribe`. schema/index.ts:251 ist der einzige Aufrufer (grep über apps/server/src).\n2. Damit ist es exakt der im Auftrag benannte, bewusst akzeptierte DoS-Typ: „unbegrenzte Map-Keys“ durch einen angemeldeten Nutzer.\n3. Es gibt keine Verstärkung: der Angreifer muss ungefähr so viele Bytes hochladen, wie er Heap binden will (der Key IST der übertragene String). „Ein paar Dutzend Aufrufe pinnen Gigabytes“ aus dem exploitScenario ist falsch — dafür müssten Gigabytes übertragen werden.\n4. Derselbe angemeldete Nutzer hat per Design einen um Größenordnungen billigeren Ressourcen-Hebel: schema/index.ts:386-398 (`enterProject`) bootet für JEDES fremde Projekt eine komplette MicroVM (R10, ausdrücklich gewollt, Kommentar Zeile 385-386). Ein Map-Eintrag von wenigen hundert Byte ist daneben irrelevant.\n\nKein Nachweis erbracht für die behauptete „full-platform outage“ mit realistischem Aufwand; Rest ist Hygiene (Validierung der projectId + Aufräumen leerer States) mit LOW-Charakter."
        },
        {
          "lens": "scope-and-mitigation",
          "reasoning": "Der Mechanismus stimmt technisch, fällt aber genau in die im Bedrohungsmodell ausdrücklich als Nicht-Finding benannte DoS-Klasse (\"unbegrenzte Map-Keys, fehlende Längenlimits\" hinter gültiger Session).\n\nVerifiziert im echten Code:\n- apps/server/src/schema/index.ts:248-252 — `subscribe: (_root, args, ctx) => { requireUser(ctx); return ctx.chatService.subscribe(String(args.projectId)); }` (Zeile 250/251 wie zitiert, korrekt).\n- apps/server/src/services/chatService.ts:141-153 — `state()` legt den Eintrag mit dem Angreifer-String als Key an; `grep \"this.states\"` liefert nur die Zeilen 142, 151, 165 — es gibt tatsächlich KEIN `states.delete`, `return()` (chatService.ts:309-314) entfernt nur den Callback. Der Leak existiert also.\n\nWarum trotzdem kein meldepflichtiges Finding:\n1. Zwingende Vorbedingung ist eine gültige, vom Admin freigeschaltete Session: apps/server/src/schema/index.ts:121-126 (`requireUser` wirft ohne `ctx.currentUser`) i.V.m. apps/server/src/services/authService.ts:258-262 (`if (!user.approved) { session löschen; return null }`). Kein unauthentifizierter Pfad: `grep` zeigt `chatService.subscribe` nur an dieser einen Stelle (schema/index.ts:251); Agent-Gateway/Preview-Gateway rufen `subscribe` nicht auf. Damit greift die Vorgabe \"Ein angemeldeter Nutzer, der seinen eigenen Server langsam macht, ist kein Finding\".\n2. Kein realistischer Aufwandsvorteil/Amplifikation: Der gepinnte Heap entspricht ~1:1 den hochgeladenen Bytes (der Key IST der übertragene String); pro kurzem Key nur ein kleines Objekt + Set. \"Gigabytes mit ein paar Dutzend Requests\" heißt Gigabytes Upload — keine Verstärkung.\n3. Der gleiche angemeldete Nutzer hat bereits eine bewusst gewährte, um Größenordnungen teurere Ressourcen-Fähigkeit: apps/server/src/schema/index.ts:379-397 `enterProject` startet über `getProjectAny` (Zeile 388, ohne Owner-Prüfung, ausdrücklich R10-kommentiert Zeile 385-386) für JEDES fremde Projekt eine MicroVM. Ein Map-Eintrag ist gegenüber diesem akzeptierten Restrisiko irrelevant.\n4. Andere `state()`-Aufrufer sind validiert: `stopTurn` (schema/index.ts:474-479) und `sendMessage` (schema/index.ts:444-445) laufen über `getProjectOwned` (schema/index.ts:137-149); `isTurnActive` (chatService.ts:164-168) nutzt `states.get` und legt nichts an.\n\nBleibt: Robustheits-/Code-Qualitätsmangel (fehlende Eviction, fehlende Existenzprüfung) ohne Sicherheitsfolge unter diesem Bedrohungsmodell.",
          "confidence": "HIGH",
          "refuted": true
        }
      ]
    },
    {
      "id": "C7",
      "voters": 3,
      "refuted": 3,
      "kept": 0,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Code-Wahrheit weitgehend bestätigt, Kausalkette/Impact aber überzogen und durch das Bedrohungsmodell ausgeschlossen.\n\nVerifiziert (Zitate stimmen): apps/server/src/schema/index.ts:447-450 prüft `args.text` tatsächlich nur auf Leere (`trim()` + `length === 0`), index.ts:461 reicht den Text unverändert an `ctx.chatService.sendMessage` weiter; apps/server/src/services/chatService.ts:234-235 legt ihn wörtlich in Queue und `insertMessage`; `insertMessage` (chatService.ts:326-343) kappt nichts; `MAX_MESSAGE_CHARS = 200_000` (chatService.ts:13) wird ausschließlich in `appendDelta` (chatService.ts:522) angewandt; `withHistoryContext` joint 30 volle Zeilen vor dem 8k-Slice (chatService.ts:414-418); `chatMessages` ist bewusst für jeden angemeldeten User lesbar (index.ts:219-229, Kommentar „Lesen dürfen alle angemeldeten User (R10)\"). Einziger Aufrufer von `sendMessage` ist index.ts:461; ein Guest in der VM erreicht ihn nicht.\n\nBruchstellen: (1) Der Pfad ist strikt authentifiziert und eigentümergebunden — index.ts:445 `requireUser(ctx)` und index.ts:446 `getProjectOwned(...)`; Registrierung ist admin-freigabepflichtig (index.ts:267-274, nur der erste Nutzer bekommt sofort eine Session). Damit ist der Kandidat exakt der im Auftrag ausgeschlossene Fall „angemeldeter Nutzer macht seinen eigenen Server langsam\" (fehlendes Längenlimit, gültige Session zwingend, kein Rate-Limit auf sendMessage vorhanden — Limiter existieren nur für login/register, index.ts:109-110).\n(2) Die Behauptung „Bun.serve is configured without maxRequestBodySize\" suggeriert Grenzenlosigkeit, tatsächlich greift der Framework-Default: node_modules/bun-types/serve.d.ts:674-678 `@default 1024 * 1024 * 128 // 128MB`. Eine Einzelzeile ist also auf ~128 MB gedeckelt, nicht unbegrenzt.\n(3) Folglich ist „turning one planted message into a multi-gigabyte allocation per subsequent request\" faktisch falsch — eine gepflanzte Nachricht ergibt im `join` (chatService.ts:414-417) eine Kopie in der Größenordnung 128 MB; die im Exploit-Szenario genannten Gigabytes erfordern ~30 Uploads à 128 MB (mehrere GB LAN-Traffic) durch ein admin-freigeschaltetes Konto.\n\nRestrisiko/Härtungspunkt (Limit auf `args.text`) ist berechtigt, aber kein Finding im Sinne des Bedrohungsmodells: keine VM-Grenzverletzung, keine Privilegien-Eskalation, nur Verfügbarkeit durch einen legitim angemeldeten Nutzer."
        },
        {
          "lens": "exploitability",
          "reasoning": "Die Code-Fakten des Kandidaten stimmen, die Angriffskette reißt aber an der Rolle/Bedrohungsmodell-Grenze.\n\nVerifiziert (echter Code, Zeilen leicht verschoben gegenüber dem Report):\n- `apps/server/src/schema/index.ts:437-470` — `sendMessage` prüft `args.text` tatsächlich nur auf Leere (`const text = args.text.trim(); if (text.length === 0) throw ...`, Zeile 447-450) und reicht ihn ungekappt an `ctx.chatService.sendMessage` (Zeile 461) weiter. Kein Längenlimit in `packages/shared` (dort nur `auth.ts:21` für Projektnamen).\n- `apps/server/src/services/chatService.ts:235` — `insertMessage(..., 'user', input.text)` persistiert verbatim; `chatService.ts:326-341` publiziert die Zeile an alle Abonnenten.\n- Der Deckel `MAX_MESSAGE_CHARS` (chatService.ts:13) greift wirklich nur in `appendDelta` (chatService.ts:522).\n\nWarum die Kette im gegebenen Bedrohungsmodell nicht trägt:\n1. Rolle: `apps/server/src/schema/index.ts:445-446` verlangt `requireUser(ctx)` UND `getProjectOwned(ctx, user, projectId)` — der Angreifer muss ein vom Admin FREIGESCHALTETER Nutzer sein (`authService.ts:99,109,131`; nur der erste Nutzer ist automatisch approved) und ein EIGENES Projekt besitzen. Kein anonymer LAN-Pfad.\n2. Der laut Bedrohungsmodell relevante Angreifer — Code in der VM/kompromittierter Daemon — kommt hier nicht durch: der Agent-Ausgabepfad IST gedeckelt (`chatService.ts:520-523`, Kommentar nennt genau diesen Fall), und ohne Session-Cookie scheitert jeder Direktaufruf von `/graphql` aus der VM an `requireUser` (index.ts:445); Cross-Site-Auslösung aus einer Preview-Origin blockt der CSRF-Guard (`http/createAppYoga.ts:40-62`).\n3. Damit bleibt exakt der vom Auftrag ausgeschlossene Fall: ein angemeldeter, freigeschalteter Nutzer, der mit Gigabyte-Uploads in sein eigenes Projekt den gemeinsam genutzten lokalen Dev-Server verlangsamt/OOM-t. Reiner Verfügbarkeitsschaden, per Neustart behebbar, keine Rechte-, VM- oder Credential-Grenze überschritten.\n4. Zusätzlich ist die Verstärkungsbehauptung schwächer als dargestellt: `withHistoryContext` (chatService.ts:403-423) läuft nur, wenn `canResume === false` (chatService.ts:466-474), also nur nach Session-Reset/Modellwechsel/Retry, nicht bei \"jedem späteren Turn\"; und die Pump-Ausnahme wird abgefangen (`chatService.ts:247-249`), reißt den Prozess also nicht selbst mit.\n\nDer einzige Fremdschaden-Anteil (`chatMessages` ist per R10 bewusst für jeden angemeldeten Nutzer lesbar, index.ts:219-229) setzt voraus, dass der Insider vorher selbst Gigabytes hochgeladen hat — das ist das bewusst akzeptierte Insider-DoS-Restrisiko, kein Finding im Sinne des Auftrags.",
          "confidence": "MEDIUM",
          "refuted": true
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Die Code-Fakten des Kandidaten stimmen, der Geltungsbereich trägt jedoch nicht.\n\nVerifiziert (Zeilennummern exakt wie zitiert):\n- `apps/server/src/schema/index.ts:447-450` prüft `args.text` nur auf Leere; kein Längenlimit.\n- `apps/server/src/services/chatService.ts:235` persistiert den Text unverändert; der Deckel `MAX_MESSAGE_CHARS` (chatService.ts:13) greift ausschließlich in `appendDelta` (chatService.ts:522).\n- `apps/server/src/services/chatService.ts:414-418` joint bis zu 30 volle Zeilen vor dem 8k-Slice.\n- `apps/server/src/index.ts:313-320` setzt kein `maxRequestBodySize`; `apps/server/src/http/createAppYoga.ts:92` installiert nur `useCookies()` und `useCsrfGuard()` — kein Cost-/Body-Limit.\n\nWarum trotzdem kein zu meldendes Finding:\n1. Der Pfad ist vollständig authentifiziert UND eigentümergebunden: `index.ts:445` `requireUser`, `index.ts:446` `getProjectOwned`, das bei fremdem Projekt hart wirft (`index.ts:146-148`). Der eigentliche untrusted Principal des Bedrohungsmodells — der Agent/Daemon in der VM — erreicht `sendMessage` nicht (keine Session, CSRF-Guard createAppYoga.ts:39-60). Genau deshalb wurde F16 nur auf der Daemon-Seite gedeckelt; der Kommentar chatService.ts:9-12 nennt explizit \"ein kompromittierter Daemon\" als Gegner.\n2. Damit fällt der Kandidat wörtlich unter die DoS-Ausnahme des Auftrags (\"fehlende Längenlimits\" nur real ohne gültige Session): Angreifer ist ein admin-freigeschaltetes Konto (nur der erste User bekommt bei `register` eine Session, index.ts:266-274), das zusätzlich ein eigenes Projekt besitzen und vor jedem Insert eine MicroVM hochfahren muss (`index.ts:451-460` `sandboxManager.enter` läuft VOR `chatService.sendMessage`).\n3. Marginaler Gewinn gleich null: dasselbe Konto darf per `createProject` (index.ts:324-335) ohne Quote und ohne Rate-Limit beliebig viele Projekte samt Worktree, Volume und MicroVM anlegen und im Workspace beliebige Dateien schreiben lassen. Die Ressourcen-Obergrenze dieses Principals ist by design bereits unbegrenzt; ein fehlender Chat-Textdeckel eröffnet keine neue Grenzüberschreitung. Rate-Limits existieren bewusst nur auf den unauthentifizierten Pfaden `register`/`login` (index.ts:267, 300).\n4. Die Amplifikations-Behauptung ist zusätzlich überzogen: `chatMessages` ist für alle angemeldeten User lesbar per ausdrücklicher Design-Entscheidung (Kommentar index.ts:225 \"Lesen dürfen alle angemeldeten User (R10)\"), und `withHistoryContext` läuft nicht \"per subsequent request\", sondern nur wenn `canResume === false` (chatService.ts:466-474), also nach Modellwechsel/Interrupt/Reset.\n\nReine Robustheits-/Qualitätslücke ohne Grenzüberschreitung im macvibes-Modell."
        }
      ]
    },
    {
      "id": "C8",
      "voters": 3,
      "refuted": 3,
      "kept": 0,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Code-Wahrheit geprüft: Das Zitat ist echt und zeilengenau. `login` steht in /Users/marco/projects/macvibes/apps/server/src/services/authService.ts:116, der Early-Throw für unbekannte Nutzer in Zeile 124-126, das argon2id-`Bun.password.verify` in Zeile 127 — exakt wie zitiert. Auch der Aufrufer stimmt: schema/index.ts:297-312 exponiert `login` unauthentifiziert, und /Users/marco/projects/macvibes/apps/server/src/http/originPolicy.ts:77-79 lässt Clients ohne Origin/Sec-Fetch-Site (curl) bewusst durch, der Pfad ist also erreichbar. Ein zeitangleichender Guard (Dummy-Hash, künstliche Verzögerung) existiert nirgends: `Bun.password` kommt im gesamten Server nur an authService.ts:84 und :127 vor, in createAppYoga.ts/schema/index.ts gibt es kein setTimeout/sleep. Insofern ist die technische Beschreibung nicht halluziniert.\n\nTrotzdem kein echtes Finding, weil die behauptete Konsequenz (\"Angreifer kann ermitteln, welche Usernamen existieren\") ohne jeden Timing-Trick bereits deterministisch verfügbar ist: die ebenfalls unauthentifizierte `register`-Mutation (apps/server/src/schema/index.ts:260-268) ruft `register` auf, das bei existierendem Namen in authService.ts:71-72 explizit `'Benutzername ist bereits vergeben'` wirft — ein eindeutiger Ja/Nein-Orakel-Response in einem einzigen Request. Selbst-Registrierung ist per Design immer offen (kein Abschaltflag; grep nach registration/allowRegistration findet nur Kommentare). Die empfohlene Gegenmaßnahme (Dummy-Argon2 im Nicht-gefunden-Zweig) würde die Enumeration also um kein Bit reduzieren; Zeile 127 schafft keine neue Fähigkeit. Zusätzlich bremst der vor dem Verify liegende Limiter (schema/index.ts:305-308 mit `ip:`- und `user:`-Key, rateLimiter.ts:32-37, 20 Versuche/5 min pro IP). Damit bleibt ein Best-Practice-Härtungspunkt (CWE-208) ohne inkrementelle Wirkung, kein vollständiger Angriffspfad mit realer Konsequenz — im Bedrohungsmodell (VM-Grenze ist die Sicherheitsgrenze, Host-Credentials/VM-Ausbruch unberührt) nicht als Finding zu werten."
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Die Code-Behauptungen stimmen wörtlich: apps/server/src/services/authService.ts:122-130 ist exakt wie zitiert (Early-Throw bei !user in Zeile 124/125, argon2id-Verify erst in Zeile 127), und der login-Resolver ist unauthentifiziert erreichbar (apps/server/src/schema/index.ts:297-309). Auch die Erreichbarkeit für einen anonymen LAN-Client bricht nicht: der CSRF-Guard lässt Requests ohne Origin und ohne Sec-Fetch-Site bewusst durch (apps/server/src/http/originPolicy.ts:70-77, Kommentar + `return false`), curl kommt also an /graphql.\n\nDie Kette reißt aber beim SCHADEN, nicht bei der Erreichbarkeit. Der behauptete Impact ist ausschließlich \"ein unauthentifizierter Client kann feststellen, welche Usernamen existieren\". Genau diese Information gibt derselbe anonyme Angreifer sich deterministisch, ohne jede Messung, über die ebenfalls unauthentifizierte register-Mutation: apps/server/src/schema/index.ts:260-268 ruft ohne Auth-Gate register() auf, und apps/server/src/services/authService.ts:65-72 wirft bei existierendem Namen wörtlich 'Benutzername ist bereits vergeben' — noch vor jedem Hashing. Eine Möglichkeit, Selbstregistrierung abzuschalten, existiert im Code nicht (Grep nach registration/allowRegister/REGISTER in apps/server/src liefert keinen Treffer), sie ist also in JEDEM Deployment offen. Das Schließen von Zeile 127 entfernt dem Angreifer somit keine einzige Fähigkeit; der Forscher räumt diesen deterministischen Kanal in seiner eigenen \"recommendation\" selbst ein. Auch die per-IP-Bremse ändert daran nichts, denn beide Limiter (apps/server/src/schema/index.ts:109-110, 267, 305-308) hängen an der echten Socket-IP (apps/server/src/index.ts:330, `server.requestIP`), die ein LAN-Angreifer beliebig aliasen kann — was für BEIDE Kanäle gleichermaßen gilt.\n\nDer Folgeschaden ist zusätzlich gedeckelt: die Nutzerliste ist admin-gated (apps/server/src/schema/index.ts:204-210, requireAdmin), gezieltes Raten bremst der per-Username-Key `user:${...}` auf 20/5min (apps/server/src/schema/index.ts:305-308), und selbst ein korrektes Passwort führt ohne Admin-Freischaltung zu keinem Login (authService.ts:131-133).\n\nSchließlich ist die zweite Vorbedingung des Kandidaten selbst als ungeprüft deklariert (\"reasoned from the code, not measured\"): apps/server/src/services/authService.ts:84 ruft `Bun.password.hash(...)` ohne Parameter auf, die argon2-Kosten sind also nirgends im Repo festgelegt und der Signal-Rausch-Abstand ließe sich nur durch Ausführen belegen — was hier nicht erlaubt ist. Kein Boundary des Bedrohungsmodells (VM-Grenze, Host-Credentials, fremde VMs) wird überschritten. Ergebnis: Härtungs-Hinweis ohne marginalen Angreifergewinn, kein eigenständiges Finding."
        },
        {
          "lens": "scope-and-mitigation",
          "reasoning": "Der Code-Befund selbst stimmt wörtlich: `apps/server/src/services/authService.ts:122-130` bricht bei unbekanntem Username in Zeile 125 ab, ohne das teure `Bun.password.verify` in Zeile 127 auszuführen; die `login`-Mutation ist unauthentifiziert erreichbar (`apps/server/src/schema/index.ts:297-311`), und der CSRF-Guard blockiert Nicht-Browser-Clients nicht (`apps/server/src/http/originPolicy.ts:77-79` lässt Requests ohne Origin/Sec-Fetch-Site bewusst durch). Trotzdem ist das kein zu meldendes Finding, weil die behauptete Sicherheitsfolge bereits per Design offen preisgegeben wird:\n\n1. Duplikat einer bewussten Design-Eigenschaft: Die Selbst-Registrierung ist unauthentifiziert und dauerhaft offen (`apps/server/src/schema/index.ts:260-268`, Doku-Kommentar `authService.ts:46-51`, \"kein Invite-Code mehr\"); ein Grep über apps/server und apps/web fand keinen Schalter, der Registrierung abschaltet. `register` antwortet für existierende Namen deterministisch mit `'Benutzername ist bereits vergeben'` (`authService.ts:66-73`). Die Existenz eines Usernames ist also keine Information, die dieses Deployment geheim hält — ein Fix an Zeile 127 allein entfernt die Enumeration nicht, er senkt nur den Durchsatz (10/h/IP via register gegen 240/h/IP via Timing). Der Melder räumt das in seiner eigenen `recommendation` ein.\n\n2. Der behauptete Folgenutzen (\"Credential-Guessing auf echte Konten richten\") ist bereits gemindert: `assertWithinLimit(loginLimiter, ['ip:…','user:…'])` in `apps/server/src/schema/index.ts:304-308` mit `createRateLimiter({windowMs: 5*60_000, max: 20})` (`schema/index.ts:109`, Implementierung `services/rateLimiter.ts:23-52`) begrenzt Rateversuche pro Username auf 20/5 min — unabhängig davon, ob der Angreifer die Existenz kennt. Zusätzlich sind neue Konten `pending` und können sich auch mit korrektem Passwort nicht anmelden (`authService.ts:131-133`), das Wissen um einen pending-Username bringt also nichts.\n\n3. Der Kern der Behauptung (messbarer Zeitunterschied gegenüber Netz-Jitter) ist unbelegt; der Melder schreibt selbst \"reasoned from the code, not measured\", und ausführen darf/kann ich hier nichts.\n\nDamit bleibt reines Hardening (CWE-208-Klassiker) ohne inkrementelle Angreiferfähigkeit gegenüber dem bewusst offenen Registrierungspfad — kein eigenständiges Finding.",
          "refuted": true,
          "confidence": "HIGH"
        }
      ]
    },
    {
      "id": "C9",
      "voters": 3,
      "refuted": 3,
      "kept": 0,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "CODE-WAHRHEIT: Alle Zitate stimmen wörtlich. /Users/marco/projects/macvibes/apps/server/src/services/mirrorService.ts:35 enthält exakt `const output = await runGit(['push', '--mirror', config.remoteUrl], config.bareRepoPath);`, Zeile 41 exakt `logger.error(\\`GitHub-Mirror fehlgeschlagen: ${String(error)}\\`)` (Default-Logger = `console`, mirrorService.ts:23; index.ts:357 übergibt keinen eigenen Logger). gitService.ts:52 spawnt `['git', ...args]` ohne env-Override, gitService.ts:64 baut `git ${args.join(' ')} schlug fehl …` — die Remote-URL steht also unmaskiert in argv UND in der GitError-Message. config.ts:197 `remoteUrl: Bun.env.MACVIBES_GITHUB_REMOTE ?? null` stimmt, .env.example:58-59 dokumentiert tatsächlich `https://<user>:<token>@github.com/...`. Eine Redaktion existiert nirgends (grep redact|sanitiz findet nur previewGateway-Header). Der Forscher hat also nichts halluziniert und keinen Guard übersehen.\n\nDie KETTE bricht trotzdem an drei Stellen:\n1) Kein Prinzipal des macvibes-Bedrohungsmodells erreicht eine der beiden Senken. Der Gast (das eigentlich untrusted Prinzip) läuft in einer MicroVM ohne Sicht auf die Host-Prozesstabelle und ohne Zugriff auf Host-stdout; das Bare-Repo liegt host-only (~/macvibes/macvibes-apps.git). Der LAN-/GraphQL-Weg fällt aus: `mirror` kommt in apps/server/src/graphql/ und apps/web/src überhaupt nicht vor — der Fehlertext erreicht nie eine API-Antwort. Der postulierte Angreifer ist ein zweites lokales Unix-Konto auf dem Mac, das im Modell nicht vorkommt (und für das .env mit 0600 plus die Warnung in index.ts:101-108 bereits die gleiche Klasse abdeckt).\n2) Das Log-Bein ist unbelegt: der Server schreibt sein stdout nirgends in eine Datei. package.json:25 -> apps/server/package.json:8 ist ein reines `bun run src/index.ts` im Vordergrund; die einzige Host-Logdatei ist `~/macvibes/local-router.log` (index.ts:90) für den LiteLLM-Shim, die den Mirror-Fehler nicht erhält. Die launchd-Plist ist laut architektur.md:106 ausdrücklich \"Offen (optional, später)\".\n3) Die Vorbedingung \"Token in der URL\" ist in der realen Konfiguration nicht erfüllt: apps/server/.env:21 lautet `MACVIBES_GITHUB_REMOTE=https://github.com/vanmeegen/macvibes-apps.git` — kredentiallos, Auth über den gh-Credential-Helper (mirrorService ruft runGit ohne SAFE_GIT_ENV auf, GIT_ASKPASS=/bin/false gilt dort also nicht, der Helper greift).\n\nÜbrig bleibt eine Doku-/Hygiene-Empfehlung (.env.example:58-59 sollte die kredentiallose Form nennen, GitError sollte userinfo strippen) — kein bestätigter, vollständiger Angriffspfad."
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Die Code-Fakten stimmen, aber die Angriffskette reißt beim Beobachter — kein Akteur des macvibes-Bedrohungsmodells kann die Senke lesen.\n\nVerifiziert: apps/server/src/services/mirrorService.ts:35 gibt `config.remoteUrl` als argv an git weiter (apps/server/src/services/gitService.ts:52 `Bun.spawn(['git', ...args])`), und apps/server/src/services/gitService.ts:64 baut `git ${args.join(' ')}` in die GitError-Message, die mirrorService.ts:41 loggt. Der Logger ist der Default-Parameter `console` (mirrorService.ts:23), denn apps/server/src/index.ts:357-360 übergibt nur zwei Argumente. Quelle ist apps/server/src/config.ts:197 (`Bun.env.MACVIBES_GITHUB_REMOTE ?? null`), dokumentiert in apps/server/.env.example:58-60 (auskommentiert, also Opt-in; ohne Konfiguration ist der Scheduler ein No-op, mirrorService.ts:63-65 — die Behauptung \"fires every 10 minutes by default\" ist falsch).\n\nWarum kein Finding:\n1. Keine der beiden Senken ist für einen Angreifer im Modell erreichbar. Die Prozess-Tabelle des Hosts: Der Agent läuft per Fail-closed ausschließlich in der MicroVM (apps/server/src/sandbox/backendSelection.ts:52-65 wirft UnsafeBackendError, wenn der echte Agent ohne msb starten würde), sieht also weder Host-argv noch Host-stdout. LAN-/anonyme Requester und angemeldete Nutzer haben ohnehin keinen Host-Zugriff.\n2. Es gibt keine Log-Datei und keine API-Oberfläche für Server-Logs: gestartet wird per `bun run src/index.ts` (apps/server/package.json:8, package.json:25) ohne Umleitung; das GraphQL-Schema (apps/server/src/schema/ enthält nur builder.ts und index.ts) hat kein Feld für Logs oder Mirror, und mirrorToGitHub schluckt den Fehler und gibt nur `'error'` zurück (mirrorService.ts:40-42). Der Fehlertext erreicht also nie einen Client. Die Behauptung des Forschers \"written into the server log, which is retained on disk\" ist im Repo nicht belegt.\n3. Die verbleibende Vorbedingung (\"ein zweites lokales Konto/Prozess auf dem Mac\") liegt außerhalb des Modells und bringt keinen Rechtezuwachs: Ein Prozess mit derselben UID kann apps/server/.env direkt lesen — dort steht dieselbe MACVIBES_GITHUB_REMOTE plus CLAUDE_CODE_OAUTH_TOKEN; index.ts:101-113 warnt sogar aktiv, wenn diese Datei group/other-lesbar ist. Ein fremdes lokales Konto kann unter macOS die argv fremder Prozesse nicht auslesen (KERN_PROCARGS2 ist auf gleiche UID bzw. root beschränkt) — dieses OS-Detail ist im Repo nicht prüfbar, wird für die Widerlegung aber auch nicht gebraucht.\n\nBleibt eine Best-Practice-Verletzung (Credential in argv/Fehlermeldung, Redaction wäre sinnvoll), aber ohne Angreifer, der sie im gegebenen Modell auslösen oder beobachten kann."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Die Code-Zitate stimmen: /Users/marco/projects/macvibes/apps/server/src/services/mirrorService.ts:35 reicht `config.remoteUrl` als argv weiter an gitService.ts:52 (`Bun.spawn(['git', ...args])`), gitService.ts:64 baut `git ${args.join(' ')} …` in die GitError, mirrorService.ts:41 loggt sie, und eine Redaktion existiert nicht (Grep nach redact/mask/*** in apps/server/src: keine Treffer). Trotzdem fehlt jeder Angreifer aus dem Bedrohungsmodell:\n\n1) Kein Pfad für den untrauten Gast. Der Mirror läuft ausschließlich host-seitig (index.ts:357-359, Default-Logger = console). Die Prozessliste des Hosts ist aus der MicroVM nicht sichtbar, und der Prozess-Provider mit echtem Agenten wird hart verweigert (sandbox/backendSelection.ts:52-65: UnsafeBackendError, außer MACVIBES_ALLOW_HOST_AGENT=1). Der Server-stdout wird nirgends in eine Sandbox gemountet.\n\n2) Kein Pfad für LAN-/angemeldete Nutzer. `mirror` kommt in apps/server/src/schema überhaupt nicht vor (Grep leer); mirrorToGitHub wird nur an index.ts:357 verdrahtet, kein Resolver gibt den Fehler an einen Client zurück.\n\n3) Die Prämisse \"Server-Log auf Platte\" ist nicht im Code belegt. Der Server schreibt keine eigene Logdatei; die einzige Datei ist `local-router.log` (index.ts:90) für den lokalen Router, nicht für den Mirror. Die stdout-umleitende launchd-Plist steht in architektur.md:106 ausdrücklich noch unter \"Offen (optional, später)\".\n\n4) Der verbleibende Restangreifer ist ein zweites lokales Konto bzw. root auf dem Mac — der hat aber ohnehin apps/server/.env mit CLAUDE_CODE_OAUTH_TOKEN/ANTHROPIC_API_KEY; genau dieses Szenario ist im Projekt bereits als F26 adressiert und bewusst nur mit einer Warnung belegt (index.ts:100-108, `statSync(envPath).mode & 0o077`). Das argv-Leck verschafft ihm keine neue Fähigkeit.\n\nDazu ist die Funktion optional und standardmäßig aus (config.ts:197 `Bun.env.MACVIBES_GITHUB_REMOTE ?? null`, .env.example:60 auskommentiert). Damit bleibt eine echte Hygiene-/Defense-in-Depth-Schwäche (CWE-532/214) ohne erreichbaren Angreifer im definierten Modell — kein zu meldendes Finding."
        }
      ]
    },
    {
      "id": "C10",
      "voters": 3,
      "refuted": 3,
      "kept": 0,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Code-Wahrheit: Die Zitate des Forschers stimmen weitgehend. `chatMessages` steht in /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:219-229, das Snippet `return ctx.chatService.listMessages(String(args.projectId));` ist wörtlich Zeile 227, einziges Arg ist `projectId` (Z. 222), einziger Guard ist `requireUser(ctx)` (Z. 226). `listMessages` in apps/server/src/services/chatService.ts:156-162 hat tatsächlich kein LIMIT/OFFSET. `createAppYoga` (apps/server/src/http/createAppYoga.ts:77-94) übergibt nur `plugins: [useCookies(), useCsrfGuard(originsFor)]` (Z. 94) — kein Depth-/Complexity-/Alias-Plugin. Ich habe aktiv nach übersehenen Schutzmaßnahmen gesucht und auch keine Framework-Default gefunden: node_modules/graphql-yoga/package.json Version 5.21.2, in node_modules/graphql-yoga/cjs/server.js:150-190 sind als Defaults nur useRequestParser/useParserAndValidationCache/useLimitBatching/useCheckGraphQLQueryParams aktiv, kein `maxTokens`/`parseOptions` (grep nach `maxTokens` in graphql-yoga und @envelop: keine Treffer; graphql 16.14.2 hat `maxTokens` nur als optionalen Parser-Parameter, apps/server übergibt ihn nicht). Rate-Limiting existiert ausschließlich für login/register (apps/server/src/schema/index.ts:109-118), nicht für Queries.\n\nWas die Kette dennoch bricht: Der Pfad ist nur mit gültiger, ADMIN-FREIGESCHALTETER Session erreichbar. `requireUser` (apps/server/src/schema/index.ts:117-122) wirft ohne `ctx.currentUser`; `resolveSession` löscht Sessions nicht freigeschalteter User (apps/server/src/services/authService.ts:259-261) und `login` lehnt sie ab (authService.ts:109, :131). Ein unauthentifizierter LAN-Angreifer erzeugt mit 1000 Aliassen nur 1000 sofortige DomainError-Würfe — kein Speicherhub. Damit fällt der Kandidat exakt in die im Bedrohungsmodell vorgegebene DoS-Ausnahme (\"nur real, wenn ohne gültige Session auslösbar; ein angemeldeter Nutzer, der seinen Server langsam macht, ist kein Finding\"). Zusätzlich ist die Kernbehauptung des Impacts unbelegt: die Vorbedingung \"10 MB Historie\" muss der Angreifer erst über `sendMessage` erzeugen, das pro Nachricht zwingend `ctx.sandboxManager.enter(...)` (apps/server/src/schema/index.ts:453) und einen echten Agent-Turn durchläuft, und die Zahl \"~10 GB Heap\" liesse sich nur durch Ausführen verifizieren, was hier nicht möglich ist. Fehlende Pagination in `listMessages` ist eine reale Robustheitslücke, aber unter diesem Bedrohungsmodell kein Security-Finding."
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Die technischen Einzelbehauptungen stimmen, die Ausnutzungskette reißt aber genau an der Stelle, die das Bedrohungsmodell ausklammert.\n\nVerifiziert (Code stimmt mit dem Kandidaten überein):\n- apps/server/src/schema/index.ts:218-228 — `chatMessages` hat nur `projectId`, keine Pagination.\n- apps/server/src/services/chatService.ts:156-162 — `select().from(chatMessages).where(eq(...)).orderBy(...)` ohne LIMIT.\n- apps/server/src/http/createAppYoga.ts:93 — `plugins: [useCookies(), useCsrfGuard(originsFor)]`, kein Depth-/Complexity-/Alias-Limit; Rate-Limits existieren nur für Login/Register (apps/server/src/schema/index.ts:108-109).\n\nWarum die Kette dennoch reißt:\n1. Kein anonymer Pfad: `requireUser(ctx)` steht in apps/server/src/schema/index.ts:226 VOR `listMessages`; ohne Session wirft der Resolver (index.ts:121-126), es wird keine Zeile geladen. Parsen von ~30 KB Alias-Query ist kein DoS.\n2. Kein Pfad aus der VM / aus einer Preview: der CSRF-Guard greift für ALLE Methoden (createAppYoga.ts:41-59) und `isCrossSiteRequest` (originPolicy.ts:69-78) blockt jede Origin, die nicht aus dem Host-Header abgeleitet ist (originPolicy.ts:34-49) — die Preview-Origin auf :4173 ist damit ausgeschlossen. Der Gast hat ohnehin kein Session-Cookie.\n3. Bleibt allein der angemeldete, vom Admin freigeschaltete Nutzer: Selbstregistrierung ist nicht ausreichend, nur der erste User ist auto-approved (apps/server/src/services/authService.ts:98-99), Login/Session verlangen `approved` (authService.ts:109, 131).\n4. Die behauptete Wirkung setzt zusätzlich eine selbst aufgeblähte Historie voraus: `sendMessage` verlangt Ownership (`getProjectOwned`, index.ts:445), bootet die eigene VM (index.ts:452) und schreibt erst dann die Zeile (chatService.ts:235). Der Angreifer füllt also sein eigenes Projekt, um anschließend den lokalen Dev-Server lahmzulegen, den er selbst legitim nutzt.\n\nDamit ist das exakt der im Auftrag benannte, bewusst akzeptierte Fall („Ein angemeldeter Nutzer, der seinen eigenen Server langsam macht, ist kein Finding\"): keine Rechteausweitung, keine Datenoffenlegung, keine Überschreitung der VM-/Host-Grenze, keine Auslösbarkeit ohne gültige Session."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Die technischen Einzelfakten des Kandidaten stimmen, die Sicherheitsfolge fällt aber aus dem Bedrohungsmodell heraus.\n\nVerifiziert (Fakten korrekt):\n- /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:218-228 — `chatMessages` hat nur `projectId`, kein limit/offset, Gate ist allein `requireUser(ctx)` (Zeile 225).\n- /Users/marco/projects/macvibes/apps/server/src/services/chatService.ts:155-161 — `listMessages` selektiert ohne LIMIT die gesamte Historie.\n- /Users/marco/projects/macvibes/apps/server/src/http/createAppYoga.ts:76-95 — `plugins: [useCookies(), useCsrfGuard(originsFor)]`, kein Depth-/Complexity-/Alias-Plugin; grep über apps/server/src findet nur den Login/Register-RateLimiter (schema/index.ts:109-110).\n\nWarum trotzdem kein meldbares Finding:\n1. Der Pfad ist ausschließlich mit gültiger, vom Admin freigeschalteter Session erreichbar. `requireUser` (schema/index.ts:120-125) verlangt `ctx.currentUser`, und `resolveSession` verwirft/löscht jede Session eines nicht freigegebenen Nutzers (authService.ts:254-261); `login` blockt `!approved` (authService.ts:131). Selbst-Registrierung erzeugt `approved: istErsterAdmin` (authService.ts:99) und ist IP-ratenbegrenzt (schema/index.ts:110, 267). Damit ist der Angreifer genau der Fall, den das Bedrohungsmodell ausschließt: „Ein angemeldeter Nutzer, der seinen eigenen Server langsam macht, ist kein Finding.\"\n2. Der eigentlich untrusted Akteur — Agent/Preview in der MicroVM — erreicht den Sink nicht. Der CSRF-Guard (createAppYoga.ts:41-61) lehnt methodenunabhängig jeden Request ab, dessen Origin nicht in `allowedOrigins` steht; `isCrossSiteRequest` (originPolicy.ts:69-79) blockt `Origin: null` und die Preview-Origin :4173, weil `allowedOrigins` (originPolicy.ts:34-49) nur die Host-Origin des API-Ports (+ MACVIBES_ALLOWED_ORIGINS/devWebPort) enthält. Ein VM-getriebener CSRF-DoS mit dem Cookie eines Betrachters scheitert daran; der direkte Weg aus der VM an der Egress-Policy (akzeptiertes Restrisiko 2).\n3. Die behauptete Alias-Verstärkung schafft kein neues Privileg: für authentifizierte GraphQL-Requests existiert ohnehin kein Rate-Limit (assertWithinLimit nur in register/login, schema/index.ts:267 und 300-305), derselbe Nutzer kann die identische Last mit N parallelen HTTP-Requests erzeugen.\n4. Der Ownership-Teil der Rationale („any project, no ownership check\") ist ausdrücklich Requirement R10 — der Kommentar an schema/index.ts:224 nennt R10 explizit, fremde Projekte sind bewusst lesbar.\n\nEs bleibt eine sinnvolle Härtung (Pagination + Query-Cost-Guard) ohne Sicherheitsfolge im definierten Modell. Nicht abschließend prüfbar war nur, ob eine reale Historie tatsächlich in den MB-Bereich wächst (Persistenz chatService.ts:334) — das ändert die Einstufung nicht, da der Auslöser in jedem Fall eine freigeschaltete Session braucht."
        }
      ]
    },
    {
      "id": "C11",
      "voters": 3,
      "refuted": 2,
      "kept": 1,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "confidence": "HIGH",
          "correctedSeverity": "LOW",
          "reasoning": "Code-Wahrheit vollständig bestätigt, kein übersehener Guard.\n\nAlle fünf Zitate stimmen wörtlich mit dem Repo überein:\n- /Users/marco/projects/macvibes/apps/server/src/services/mirrorService.ts:35 `const output = await runGit(['push', '--mirror', config.remoteUrl], config.bareRepoPath);` — die Remote-URL ist tatsächlich ein argv-Element.\n- gitService.ts:64 `throw new GitError(\\`git ${args.join(' ')} schlug fehl (${exitCode}): ${stderr.trim()}\\`);` — das argv wird ungefiltert in die Fehlermeldung gejoint; GitError setzt name='GitError' (gitService.ts:6-11), also enthält `String(error)` die volle URL.\n- mirrorService.ts:41 `logger.error(\\`GitHub-Mirror fehlgeschlagen: ${String(error)}\\`);` — exakt an der genannten Zeile.\n- config.ts:197 `remoteUrl: Bun.env.MACVIBES_GITHUB_REMOTE ?? null,` — exakt.\n- apps/server/.env.example:58-59 dokumentiert ausdrücklich die Token-in-URL-Form (`https://<user>:<token>@github.com/...`), die Präkondition ist also die vom Projekt selbst empfohlene Konfiguration.\n\nAktiv nach der Minderung gesucht, keine gefunden: kein Redact/Mask/Sanitize im Pfad (grep über apps/server/src liefert nur previewGateway-Header-Sanitizer und Yoga-`maskedErrors` in createAppYoga.ts:84, das nur GraphQL-Antworten betrifft, nicht `console.error`). Kein console-Patch. index.ts:357-360 ruft `startMirrorScheduler` OHNE Logger-Argument auf, greift also der Default `logger: MirrorLogger = console` (mirrorService.ts:23) — die Meldung landet real auf stderr des Serverprozesses. Kein Hardening-Commit auf diesem Branch für mirrorService.ts/gitService.ts (git log: letzter relevanter Commit 32ed25d/14f3f19).\n\nEinschränkungen, die die Einstufung dämpfen (deshalb LOW statt MEDIUM): Der Sink ist ausschließlich stdout/stderr des Host-Prozesses. Es gibt keinen Endpunkt, der Serverlogs ausliefert (grep nach log-Endpunkten in apps/server/src und apps/web/src liefert nichts) — der Gast in der VM, LAN-Nutzer und angemeldete Nutzer erreichen die Ausgabe nicht. Der Token verlässt die Host-Vertrauenszone erst, wenn der Betreiber Logs umleitet/weitergibt. Die zusätzliche `ps`-Behauptung des Forschers konnte ich nicht bestätigen und halte sie auf macOS (dem einzigen Zielsystem, brew/msb) für zweifelhaft, da argv fremder Nutzer dort nicht sichtbar ist. Die Kernbehauptung — PAT im Klartext in der Fehlermeldung, ungefiltert geloggt — ist jedoch code-wahr und unmitigiert."
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Die Code-Behauptungen stimmen zwar (`/Users/marco/projects/macvibes/apps/server/src/services/mirrorService.ts:35` gibt `config.remoteUrl` als argv-Element weiter, `apps/server/src/services/gitService.ts:64` baut `args.join(' ')` in die GitError-Message, `mirrorService.ts:41` loggt `String(error)`), aber die Ausnutzungskette reisst an zwei Stellen:\n\n1) KEIN Angreifer im Bedrohungsmodell kann die Senke lesen. Der Default-Logger ist `console` (mirrorService.ts:23, Aufruf ohne Logger in `apps/server/src/index.ts:357-360`), d.h. die Ausgabe geht auf stderr des Host-Prozesses, der interaktiv per `bun run dev` / `bun run start` gestartet wird (`/Users/marco/projects/macvibes/package.json:11,25`, `apps/server/package.json:7-8`). Im Repo existiert keine Log-Datei-Umleitung fuer den Server (nur `local-router.log` in `index.ts:90` und `/var/log/macvibes-devserver.log` in der VM, `apps/server/src/sandbox/vmServices.ts:128`). Es gibt kein GraphQL-Feld/keine Route, die Serverlogs ausliefert (grep ueber `apps/server/src/schema/index.ts` liefert nur login/logout-Treffer), und keine console-Interception/Weiterleitung an Clients. Anonymer LAN-Angreifer, angemeldeter Nutzer und Code in der MicroVM kommen damit alle nicht an die Zeile.\n\n2) Der `ps`-Nebenpfad braucht ein zweites lokales Host-Konto. Der untrusted Agent kann per Default nie als Host-Prozess laufen: `apps/server/src/sandbox/backendSelection.ts:59-66` bricht fail-closed ab, ausser der Operator setzt ausdruecklich `MACVIBES_ALLOW_HOST_AGENT=1`. Ein lokales Zweitkonto ist im Modell nicht enumeriert und haette ohnehin dasselbe Zugriffsniveau wie auf die Klartext-`.env` (bewusst akzeptiert, nur Warnung in `apps/server/src/index.ts:101-113`).\n\n3) Zusaetzlich ist die Vorbedingung \"Token in der URL\" nicht der reale Zustand: `apps/server/.env:21` enthaelt `https://github.com/vanmeegen/macvibes-apps.git` ohne Credential (Auth laeuft ueber den git-Credential-Helper; `runGit` erbt in mirrorService.ts:35 die volle `process.env`, weil kein envOverride uebergeben wird). Token-in-URL ist nur ein Kommentar-Beispiel in `apps/server/.env.example:59`.\n\nBleibt eine reine Hygiene-/Best-Practice-Frage (Secret in argv und Fehlertext) ohne in-model Angreifer, dessen einziges Szenario \"Operator schickt sein Terminal-Log in einen Bugreport\" ist — operatives Verhalten, keine vom Angreifer ausloesbare Kette. Damit Fehlalarm im Sinne der Ausnutzbarkeitslinse."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "correctedSeverity": "LOW",
          "confidence": "MEDIUM",
          "reasoning": "Die technische Kette stimmt und die Zeilen sind korrekt: /Users/marco/projects/macvibes/apps/server/src/services/mirrorService.ts:35 übergibt `config.remoteUrl` als argv an git, /Users/marco/projects/macvibes/apps/server/src/services/gitService.ts:64 baut `git ${args.join(' ')} schlug fehl (...)` in die GitError-Message, und mirrorService.ts:41 schreibt `String(error)` per `console.error` raus. Eine Redaktion existiert nirgends (grep nach redact/sanitize/mask trifft nur previewGateway.ts:105/130 für Header). So weit hält der Kandidat.\n\nEr scheitert aber am Geltungsbereich: keiner der im Bedrohungsmodell genannten Akteure erreicht diese Ausgabe.\n1) Gast-VM: microsandboxProvider.ts:165-174 mountet ausschließlich workspaceDir, agentConfigDir, bunCacheDir, etcDir (ro) und das Daemon-Bundle (ro) — kein Host-Log, kein Server-stdout. Die VM hat eigenen Kernel/Prozesstabelle, der `ps`-Vektor endet an der VM-Grenze.\n2) LAN-/angemeldete Nutzer: Es gibt keinen Endpunkt, der Serverlogs ausliefert (kein log-Feld im GraphQL-Schema unter apps/server/src/schema/).\n3) Das Repo legt selbst gar keine Logdatei an: apps/server/package.json:8 `\"start\": \"bun run src/index.ts\"`, kein Redirect/nohup/plist in scripts/ oder README. Das Szenario „Log-Datei mit 644, jemand liest sie“ ist eine unterstellte Betreiber-Praxis, kein Code-Pfad.\n\nDazu kommt: Das Feature ist Opt-in und per Default aus (config.ts:197 `remoteUrl: Bun.env.MACVIBES_GITHUB_REMOTE ?? null`), und dasselbe Geheimnis liegt ohnehin im Klartext in apps/server/.env auf demselben Host — ein Zustand, den das Projekt bewusst akzeptiert und nur mit einer Warnung adressiert (index.ts:100-108 und der Kommentar index.ts:348-350: „Die .env trägt den Claude-Token im Klartext. Nicht automatisch korrigieren (es ist eine Nutzerdatei), aber unübersehbar melden.“). Wer das Terminal/Log des Serverprozesses liest, ist derselbe Host-Account, der die .env lesen kann — es wird keine Vertrauensgrenze überschritten.\n\nEs bleibt ein echter, aber niedrig einzustufender Hygiene-Defekt der Klasse CWE-532 (Redaktion von `//user:secret@` in gitService.ts:64 wäre ein sinnvoller Fix); MEDIUM ist überzogen, ein vollständiger Angriffspfad mit angreifer-kontrollierter Quelle existiert nicht."
        }
      ]
    },
    {
      "id": "C12",
      "voters": 3,
      "refuted": 3,
      "kept": 0,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "reasoning": "Code-Wahrheit geprüft: Die Zitate stimmen wörtlich. `login()` steht in /Users/marco/projects/macvibes/apps/server/src/services/authService.ts:116-136; Zeile 122 macht den Lookup, 124-126 wirft `DomainError(LOGIN_FAILED_MESSAGE)` ohne vorheriges Verify, 127 ruft erst danach `Bun.password.verify`. Der unauthentifizierte Einstieg existiert ebenfalls wie behauptet: /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:297-309 (`login`-Mutation, kein `requireUser`, Aufruf in Zeile 309). Eine timing-normalisierende Schutzmaßnahme habe ich aktiv gesucht und nicht gefunden: kein Dummy-Hash (grep nach `Bun.password`/`DUMMY` liefert nur authService.ts:84 und :127), keine künstliche Verzögerung im Auth-Pfad. Insofern ist die Mechanik echt.\n\nDie Kette bricht aber beim Ergebnis, nicht beim Code: Der behauptete Gewinn (\"Enumeration gültiger Kontonamen trotz identischer Fehlermeldung\") ist auf derselben unauthentifizierten Fläche bereits per Design und eindeutiger verfügbar. `register` ist ohne Session erreichbar (/Users/marco/projects/macvibes/apps/server/src/schema/index.ts:260-268, nur `assertWithinLimit(registerLimiter, ...)`, kein Auth-Guard) und antwortet bei existierendem Namen deterministisch mit `DomainError('Benutzername ist bereits vergeben')` (/Users/marco/projects/macvibes/apps/server/src/services/authService.ts:66-72) — ein rauschfreies Ein-Anfrage-Orakel, während der Timing-Kanal Statistik über mehrere Messungen braucht. Die Schutzmaßnahme, die der Forscher selbst nennt, existiert zusätzlich real: /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:305-308 begrenzt via /Users/marco/projects/macvibes/apps/server/src/services/rateLimiter.ts:23-50 auf 20 Versuche/5 min je IP UND je Username, ausgewertet VOR dem argon2-Verify.\n\nDamit bleibt ein reines Best-Practice-/Defense-in-Depth-Manko ohne inkrementelle Angreiferfähigkeit: keine gefährliche Operation, kein Zugriffsgewinn, kein Bezug zur eigentlichen Sicherheitsgrenze (VM/Host). Nach dem Maßstab \"im Zweifel FALSE_POSITIVE, 'verletzt Best Practice' zählt nicht\" votiere ich refuted=true. Nicht abschließend prüfbar war die reale Zeitdifferenz, da Ausführen/Messen hier ausgeschlossen ist — der Forscher räumt das selbst ein (MEDIUM confidence).",
          "refuted": true,
          "confidence": "MEDIUM"
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Der Code stimmt wie zitiert: `apps/server/src/services/authService.ts:122-127` bricht bei unbekanntem Username in Zeile 125 ab, ohne `Bun.password.verify` (Zeile 127) auszuführen; eine Timing-Gegenmaßnahme (Dummy-Hash, künstliche Verzögerung) existiert nirgends — Grep über services/http/index.ts liefert keinen Dummy-Hash und keinen Delay. Die Mutation ist auch wirklich anonym erreichbar (`apps/server/src/schema/index.ts:297-312`, kein `requireUser`). Die Kette reißt aber beim SCHADEN, nicht beim Zugang: Die angeblich erbeutete Information (Existenz eines Kontos) gibt die Anwendung demselben anonymen Angreifer bereits BEWUSST und deterministisch preis. `register` ist unauthentifiziert (`apps/server/src/schema/index.ts:260-274`, nur `registerLimiter`) und wirft für einen belegten Namen die eindeutige Meldung `'Benutzername ist bereits vergeben'` (`apps/server/src/services/authService.ts:66-73`). Ein einziger Request beantwortet damit exakt die Frage, für die der Kandidat statistische Latenzmessungen braucht — und Selbstregistrierung lässt sich nicht abschalten: in `apps/server/src/config.ts` gibt es keinen Schalter dafür, nur `adminUsername` (config.ts:17). Der Angreifer gewinnt durch den Timing-Kanal also keine Fähigkeit, die er nicht schon hat; ein Fix an Zeile 125 änderte seinen Wissensstand um null. Auch der behauptete Folgeschaden ist gedeckelt: der Login-Limiter zählt zusätzlich pro Username (`apps/server/src/schema/index.ts:305-308`, `max: 20` je 5 min, `createRateLimiter` in `apps/server/src/services/rateLimiter.ts:23-50` zählt beide Keys), sodass gezieltes Online-Raten gegen ein 8+-Zeichen-Passwort (`packages/shared/src/validation/auth.ts:16`) ohnehin aussichtslos bleibt, und nicht freigeschaltete Konten (`authService.ts:131-133`) sind selbst mit korrektem Passwort wertlos. Zusatzpunkt: die tatsächliche Latenzdifferenz konnte ich nicht messen (Ausführen ist hier ausgeschlossen) — der Forscher räumt das selbst ein. Ergebnis: korrekte Beobachtung, aber redundanter Oracle-Kanal ohne zusätzlichen Angriffsnutzen — Härtungswunsch, kein ausnutzbares Finding."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Der Mechanismus stimmt code-genau: `login()` wirft bei unbekanntem Nutzer in /Users/marco/projects/macvibes/apps/server/src/services/authService.ts:124-125, bevor `Bun.password.verify` in Zeile 127 läuft; der Aufruf kommt unauthentifiziert aus schema/index.ts:297-309, und ich habe im gesamten Auth-Pfad KEINE Timing-Angleichung gefunden (kein Dummy-Hash, kein künstliches Delay in authService.ts, schema/index.ts, http/createAppYoga.ts). Der CSRF-Guard hilft nicht: originPolicy.ts:isCrossSiteRequest gibt bei fehlendem Origin und fehlendem Sec-Fetch-Site bewusst `false` zurück, curl kommt also durch.\n\nTrotzdem widerlege ich unter Linse 3 (Geltungsbereich/Minderung) den Sicherheitsnutzen dieses Befunds: die Information \"Konto existiert\" ist im aktuellen Code BEWUSST und explizit über einen zweiten, ebenfalls unauthentifizierten Pfad veröffentlicht. schema/index.ts:260-268 exponiert `register` ohne jede Auth-Prüfung, und authService.ts:71-72 wirft dort die eindeutige Klartext-Meldung 'Benutzername ist bereits vergeben'. Diese Meldung erreicht den Client auch wirklich — createAppYoga.ts:81-84 dokumentiert und implementiert, dass `DomainError` von der Fehlermaskierung ausgenommen bleibt. Ein Angreifer, der wissen will ob \"alice\" existiert, braucht also gar keine Latenzmessung; eine einzige register-Anfrage genügt und ist gegen die Limitierung (registerLimiter 10/h, schema/index.ts:110) für gezielte Abfragen völlig ausreichend. Das Login-Timing liefert dieselbe Informationsklasse, nur mit höherem Durchsatz (loginLimiter 20/5min pro IP und pro Username, schema/index.ts:109 und 305-308; clientIp stammt aus dem Socket, index.ts:330, ist also nicht per Header fälschbar).\n\nDamit existiert die Vertraulichkeitseigenschaft, die der Kandidat als gebrochen meldet, im System per Design nicht — die \"bewusst identische Fehlermeldung\" in login ist bereits durch die Registrierungs-UX ausgehebelt. Es bleibt eine Härtungsempfehlung (CWE-208) ohne eigenständige Sicherheitsfolge: kein zusätzlicher Zugriff, keine Rechteausweitung, keine neue Information gegenüber dem regulären, offenen Registrierungspfad. Ich habe außerdem nicht messen können, wie groß die Latenzdifferenz real ist (Ausführen ist außerhalb des Auftrags), was den ohnehin marginalen Impact nicht stützt. NAECHSTE-SESSION.md:59 führt das als offenen, ungeprüften Punkt F20 — bekannt, aber eben genau deshalb hier zu prüfen und nach meiner Bewertung nicht als eigenständiges Finding zu melden."
        }
      ]
    },
    {
      "id": "C13",
      "voters": 3,
      "refuted": 0,
      "kept": 3,
      "survives": true,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "Code-Wahrheit geprüft, alle Zitate existieren (eine Zeile um 1 verschoben):\n\nBELEGT: `apps/server/src/http/cookies.ts:60-68` stimmt wörtlich mit dem Snippet überein — `name: SESSION_COOKIE` (= 'macvibes_session', cookies.ts:1, kein `__Host-`-Präfix), `path: '/'`, `domain: null`, `httpOnly: true`, `sameSite: 'lax'`. `apps/server/src/schema/index.ts:272` (register) und `:310` (login) rufen genau dieses `writeSessionCookie`. `apps/server/src/http/previewGateway.ts:130-144` filtert tatsächlich NUR `Set-Cookie`-Antwortheader (`if (!HOST_COOKIES.has(name))`, Zeile 140) — der Body wird ungefiltert durchgereicht (previewGateway.ts:290), und im gesamten Server existiert kein CSP-Header (grep über apps/server/src ohne Treffer). `apps/web/src/models/PreviewModel.ts:52/59` baut die iframe-URL mit `${host}` = `window.location.hostname` (`apps/web/src/pages/ChatPage.tsx:212`), also derselbe Hostname wie die App, nur anderer Port. Das iframe steht in `ChatPage.tsx:539` (Kandidat sagt 538) mit `sandbox=\"allow-scripts allow-same-origin …\"` — mit `allow-same-origin` behält die Preview ihre echte Origin und damit vollen `document.cookie`-Zugriff auf den Host. Sessions sind an nichts gebunden (kein IP/UA-Check, `apps/server/src/services/authService.ts:236-263`), und `useCsrfGuard` (createAppYoga.ts:40-62) prüft nur Cross-Site-Schreibzugriffe, greift also bei den eigenen Same-Site-Requests des Opfers nicht.\n\nKORREKTUR AM PoC (der einzige echte Fehler des Forschers): Die wörtlich behauptete Zeile `document.cookie = 'macvibes_session=<token>; path=/'` funktioniert NICHT. Das Opfer besitzt beim Betrachten einer Preview zwingend bereits ein gültiges Session-Cookie (previewGateway.ts:238 lässt ohne gültige Session gar nichts durch), und dieses ist `httpOnly: true` (cookies.ts:66); Browser verwerfen einen Non-HTTP-API-Write auf ein bestehendes HttpOnly-Cookie mit gleichem Name/Domain/Path (RFC 6265 §5.3 Schritt 11). Die Aussage „httpOnly hilft nicht\" ist für diese Variante falsch.\n\nDie Kette bricht dadurch aber nicht: httpOnly schützt nur das identische Name/Domain/Path-Tripel. Ein `document.cookie = 'macvibes_session=<attacker>; path=/graphql'` aus der Preview-Origin erzeugt ein zweites, eigenständiges Cookie (Cookies ignorieren Ports, gleicher Host). Der Browser sendet längere Pfade zuerst (RFC 6265 §5.4), und der serverseitige Parser nimmt den ERSTEN Treffer: `node_modules/@whatwg-node/cookie-store/cjs/parse.js:42-45` (`const cookiesPerKey = map.get(key); if (!cookiesPerKey) map.set(...)`) — gelesen über `cookies.ts:31` aus `createAppYoga.ts:96`, Endpunkt `graphqlEndpoint: '/graphql'` (createAppYoga.ts:79). Damit authentifizieren die GraphQL-Aufrufe des Opfers als Angreifer — exakt der behauptete Impact, exakt die behauptete Ursache (geteilter Cookie-Scope wegen Port-Unabhängigkeit) und exakt die vorgeschlagene Behebung.\n\nKein akzeptiertes Restrisiko: Restrisiko 1 bzw. `security-haertung-plan.md:558-562` akzeptiert nur, dass Previews UNTEREINANDER Origin/Cookies teilen. Der App-Session-Scope war ausdrücklich als F11 („Weil Cookies portunabhängig sind, kann die VM Cookies für dieselbe Site setzen, auf der die App auf :4000 läuft\", security-haertung-plan.md:165-167) adressiert — geschlossen wurde aber nur der `Set-Cookie`-Header-Pfad, nicht der Skript-Pfad. Genau diese Lücke beschreibt der Kandidat.\n\nConfidence MEDIUM, weil der letzte Schritt (Browser-Sortierung nach Pfadlänge) spezifiziert, aber hier nicht ausführbar verifizierbar ist, und weil der wörtliche PoC des Forschers korrigiert werden muss."
        },
        {
          "lens": "exploitability",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "Kette vollständig im echten Code nachgezeichnet und sie hält — mit einer wichtigen Korrektur am Payload des Forschers.\n\nVORBEDINGUNGEN (alle erfüllbar):\n1. Angreifer = zugelassener Nutzer mit eigenem Projekt (`createProject`, apps/server/src/schema/index.ts:322). Agent-Output in der VM ist per Bedrohungsmodell angreiferkontrolliert.\n2. Opfer sieht das Projekt: `projects` liefert `listProjects(ctx.db)` OHNE Owner-Filter (apps/server/src/schema/index.ts:206-212); `enterProject` startet fremde Sandboxes bewusst (apps/server/src/schema/index.ts:378-405, Kommentar „Auch Nur-Lese-Besucher starten die Sandbox … (R10)\").\n3. Gleiche Hostname-Scope: apps/web/src/pages/ChatPage.tsx:212 übergibt `window.location.hostname` an `derivePreviewView`, das daraus `http(s)://<host>:4173|8443/p/<id>/` baut (apps/web/src/models/PreviewModel.ts:52 und :59). Cookies sind nicht portgebunden → gleicher Cookie-Scope wie die App auf 4000/443.\n4. Das iframe erlaubt Skripte in der ECHTEN Origin: `sandbox=\"allow-scripts allow-same-origin …\"` (apps/web/src/pages/ChatPage.tsx:539).\n\nKORREKTUR AM EXPLOIT: Der wörtliche Payload des Forschers (`document.cookie='macvibes_session=X; path=/'`) wird durch `httpOnly: true` (apps/server/src/http/cookies.ts:66) BLOCKIERT — RFC 6265 §5.3 Schritt 11.2 verwirft einen Non-HTTP-API-Write, der ein bestehendes HttpOnly-Cookie mit gleichem Name/Domain/Path ersetzen würde. Die Behauptung „httpOnly does not help\" ist für diese Variante falsch. Die Lücke selbst bleibt aber offen über Cookie-Shadowing mit abweichendem Path: `document.cookie='macvibes_session=<attacker-token>; path=/graphql'` legt ein ZWEITES Cookie an (kein Konflikt, also keine HttpOnly-Sperre). Der Browser sendet laut RFC 6265 §5.4.2 den längeren Path zuerst, und der Server nimmt deterministisch den ERSTEN Treffer: node_modules/@whatwg-node/cookie-store/esm/parse.js `if (!cookiesPerKey) { map.set(key, …) }` — spätere Duplikate werden verworfen. Von dort: apps/server/src/http/cookies.ts:30-32 → apps/server/src/http/createAppYoga.ts:96-97 → `resolveSession` ohne IP-/Fingerprint-Bindung (apps/server/src/services/authService.ts:236-264) → das Opfer arbeitet als Angreifer.\n\nWARUM KEINE DER VORHANDENEN ABWEHREN GREIFT:\n- `sanitizeDownstreamHeaders` (apps/server/src/http/previewGateway.ts:130-144, Filter in Zeile 140) blockt nur `Set-Cookie` der VM, nicht `document.cookie`.\n- `sanitizeUpstreamHeaders` (Zeile 105-122) schützt nur die Gegenrichtung.\n- Der CSRF-Guard (apps/server/src/http/createAppYoga.ts:40-62) greift nicht: nach der Fixierung stellt die App-Seite des Opfers selbst first-party-Requests.\n\nKEIN AKZEPTIERTES RESTRISIKO: security-haertung-plan.md:558-560 akzeptiert ausdrücklich nur, dass „Cookies und localStorage ZWISCHEN PREVIEWS nicht trennbar\" sind. Die Grenze Preview→App-Session haben die Entwickler gerade NICHT akzeptiert, sondern in beiden Header-Richtungen gehärtet (F2/F11) — der Skript-Pfad blieb übersehen.\n\nConfidence MEDIUM: der funktionierende Payload weicht vom beschriebenen ab, und die Browser-Sortierreihenfolge (RFC 6265 §5.4.2 ist ein SHOULD) konnte ich hier nicht ausführen; die serverseitige First-Wins-Auswertung habe ich dagegen im Quelltext gelesen. Severity MEDIUM passt: Session-Fixation zwischen Peers, keine Admin-Privilegieneskalation."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "Geltungsbereich: Der Kandidat fällt NICHT unter die vier akzeptierten Restrisiken. Das dokumentierte Restrisiko lautet wörtlich „Cookies und `localStorage` sind **zwischen Previews** nicht trennbar\" (security-haertung-plan.md:557-560, NAECHSTE-SESSION.md:72-73) — akzeptiert ist die Preview↔Preview-Isolation, nicht die Preview→App-Session. R10 deckt „fremde Projekte lesbar / fremde Sandboxes startbar\" ab, nicht die Übernahme der Sitzung eines Betrachters. Im Gegenteil: dieselbe Klasse steht als offener, zu prüfender Punkt F10 auf der Liste (NAECHSTE-SESSION.md:64, ausformuliert in CLAUDE-SECURITY-20260728-211158/CLAUDE-SECURITY-RESULTS.md:196-206).\n\nGesuchte Minderungen — alle gelesen, keine greift:\n- `sanitizeUpstreamHeaders` (previewGateway.ts:105-121) und `sanitizeDownstreamHeaders` (previewGateway.ts:130-141, HOST_COOKIES:93) filtern nur **Header**; ein `document.cookie` im Browser läuft nie durch das Gateway.\n- CSRF-/Origin-Guard (createAppYoga.ts:40-60, originPolicy.ts:68-78) blockt Cross-Site-Requests; bei Session-Fixation stellt das Opfer die Requests selbst same-origin — der Guard lässt sie durch.\n- iframe-`sandbox` (ChatPage.tsx:539) enthält `allow-same-origin`, `document.cookie` ist damit verfügbar.\n- Kein `__Host-`-Präfix, keine Bindung des Tokens (cookies.ts:61 Klarname; authService.ts:236-252 schlägt jeden wohlgeformten Token direkt in `sessions` nach).\n- Gemeinsamer Host ist erzwungen: die iframe-URL wird aus `window.location.hostname` gebaut (ChatPage.tsx:212, PreviewModel.ts:52/59), dokumentiertes Deployment `:443` App / `:8443` Preview (CLAUDE.md:136).\n\nEinzige gefundene echte Minderung ist `httpOnly` (cookies.ts:66) — sie blockiert aber nur das im Kandidaten wörtlich genannte `path=/`-Überschreiben (RFC 6265 §5.3/5.6: identisches name+domain+path-Tripel). Die Aussage des Kandidaten „httpOnly does not help\" ist insofern zu pauschal. Der Kern bleibt trotzdem stehen: ein Schatten-Cookie mit längerem Pfad (`path=/graphql`) ist kein äquivalentes Cookie, wird vom Browser nach RFC 6265 §5.4 zuerst gesendet, und der Server nimmt die **erste** Fundstelle — node_modules/@whatwg-node/cookie-store/cjs/parse.js:42-44 (`if (!cookiesPerKey) map.set(...)`), dieselbe First-Wins-Logik auch in previewGateway.ts:40. Das ist genau die Variante, die der repo-eigene Scan-2-Bericht bereits beschrieb.\n\nEinschränkung meiner Prüfung: das Browser-Verhalten (Cookie-Reihenfolge, httpOnly-Schattenbildung) konnte ich nur aus der Spezifikation und dem Parser-Quelltext ableiten, nicht ausführen. Deshalb MEDIUM statt HIGH. Für meine Linse steht das Urteil aber fest: weder akzeptiertes Restrisiko noch anderswo abgefangen — Schweregrad MEDIUM bleibt korrekt."
        }
      ]
    },
    {
      "id": "C14",
      "voters": 3,
      "refuted": 0,
      "kept": 3,
      "survives": true,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "confidence": "HIGH",
          "correctedSeverity": "MEDIUM",
          "reasoning": "Code-Wahrheit bestaetigt, kein uebersehener Guard gefunden.\n\n1) Zitat stimmt woertlich und zeilengenau: /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:305-308 enthaelt exakt assertWithinLimit(loginLimiter, [`ip:${ctx.clientIp ?? 'unbekannt'}`, `user:${args.username.toLowerCase()}`]); Zeile 307 ist genau die zitierte Zeile. Der Aufruf steht VOR login(...) (Zeile 309) und vor jeder Validierung.\n\n2) Quelle unauthentifiziert und ungefiltert: login ist Root-Mutation mit username: t.arg.string({ required: true }) (schema/index.ts:300); der Pothos-Builder hat KEIN Validierungs-Plugin (schema/builder.ts:19: new SchemaBuilder<{Context: GraphQLContext}>({})). authService.login (services/authService.ts:116-136) prueft die Laenge ebenfalls nicht; usernameSchema (packages/shared/src/validation/auth.ts:8) wird auf diesem Pfad nie aufgerufen.\n\n3) Sink existiert wie beschrieben: services/rateLimiter.ts:25 const hits = new Map<string, number[]>() (durch das modul-globale loginLimiter, schema/index.ts:109, prozessweit am Leben gehalten); rateLimiter.ts:39 hits.set(key, recent) speichert den angreiferkontrollierten String als Key; Aufraeumen nur unter if (hits.size > 1000) (rateLimiter.ts:41-45). Die Retention ist damit sogar laenger als behauptet: bei <=1000 Keys werden die Riesen-Keys nie entfernt, nicht nur 5 Minuten.\n\n4) Aktiv nach Gegenmassnahmen gesucht, keine gefunden: kein maxRequestBodySize in Bun.serve (apps/server/src/index.ts:313-346, repoweiter grep leer), Default laut node_modules/bun-types/serve.d.ts:676-678 = 128 MB; Yoga hat nur useCookies() und useCsrfGuard() als Plugins (http/createAppYoga.ts:94), kein Query-Size-Limit; der CSRF-Guard bricht die Kette nicht, da http/originPolicy.ts:77-79 Requests ohne Origin und ohne Sec-Fetch-Site bewusst durchlaesst (curl-Pfad); clientIp stammt aus server.requestIP(request) (index.ts:330), ist also nicht spoofbar und begrenzt den Angriff auf 20 Keys pro Quell-IP (max: 20, schema/index.ts:109, ip-Key wird zuerst geprueft, schema/index.ts:112-119) - 20 x bis zu 128 MB dauerhaft gepinnter Strings pro IP bleibt aber ein realer, sessionloser Speicherangriff.\n\nPasst zum Bedrohungsmodell-Vorbehalt zu DoS: ohne gueltige Session und mit trivialem Aufwand ausloesbar. Kleine Ungenauigkeiten des Kandidaten (hits ist Closure- statt modul-lokal; \"5-Minuten-Fenster\" statt faktisch unbegrenzter Retention) untertreiben die Wirkung, widerlegen sie nicht. Nicht verifizierbar ohne Ausfuehrung ist allein die exakte OOM-Schwelle des Bun-Prozesses - das betrifft die Schadenshoehe, nicht die Kette; MEDIUM bleibt angemessen."
        },
        {
          "lens": "exploitability",
          "reasoning": "Kette vollständig verifiziert, jeder Schritt am echten Code:\n\n1) QUELLE — unauthentifiziert und unbegrenzt: `login` hat keinerlei Auth-Gate und die Argumente sind rohe GraphQL-Strings ohne Validierung (`apps/server/src/schema/index.ts:297-302`, `username: t.arg.string({ required: true })`). Der Pothos-Builder lädt KEIN Validation-Plugin (`apps/server/src/schema/builder.ts:19`), und in `createAppYoga` sind nur `useCookies()` und `useCsrfGuard()` registriert — kein graphql-armor, kein Token-/Depth-/Größen-Limit (`apps/server/src/http/createAppYoga.ts:93`; Grep nach armor/maxTokens/validationRules ergibt nichts).\n\n2) ERREICHBARKEIT — anonym, ohne Session: Der CSRF-Guard lässt Requests OHNE `Origin` und OHNE `Sec-Fetch-Site` bewusst durch (`apps/server/src/http/originPolicy.ts:71-80`: \"Weder Origin noch Sec-Fetch-Site: kein Browser. Bewusst erlaubt\" → `return false`). Ein curl-POST auf `/graphql` (`apps/server/src/index.ts:329-332`) erreicht damit den Resolver ohne Cookie. Der Server bindet laut Bedrohungsmodell 0.0.0.0.\n\n3) KEIN GRÖSSENLIMIT: `Bun.serve` in `apps/server/src/index.ts:313-318` setzt kein `maxRequestBodySize`; der Default ist laut `node_modules/bun-types/serve.d.ts:674-678` 128 MB. Repo-weit kommt `maxRequestBodySize` nirgends vor. Ein ~100-MB-`username` (per GraphQL-Variable, JSON-Body) wird also akzeptiert.\n\n4) SENKE — dauerhafte Retention im prozessglobalen Map: `assertWithinLimit(loginLimiter, ['ip:…', 'user:${args.username.toLowerCase()}'])` ist die ERSTE Anweisung des Resolvers, vor jeder Prüfung (`apps/server/src/schema/index.ts:305-308`; Zeile 307 exakt wie zitiert). `loginLimiter` ist modulglobal (`schema/index.ts:109`, `windowMs 5min, max 20`), `hits` lebt über die Prozesslaufzeit (`apps/server/src/services/rateLimiter.ts:25`) und `hits.set(key, recent)` (`rateLimiter.ts:38`) hält den geflatteten Schlüsselstring stark referenziert. Aufgeräumt wird NUR, wenn `hits.size > 1000` UND alle Timestamps eines Keys bereits abgelaufen sind (`rateLimiter.ts:40-44`) — innerhalb des 5-Minuten-Fensters wird also nichts gelöscht, und unterhalb von 1000 Keys nie. Kein Timer, kein `reset()` im Produktivcode (Grep: nur Tests).\n\nAngreiferrolle: anonym, Netzzugriff auf Port 4000 (LAN/VPN). Da der IP-Schlüssel zuerst geprüft wird, sind pro Quell-IP 20 Keys je 5-Minuten-Fenster möglich — bei ~120 MB pro Request also ~2,4 GB gepinnter Speicher pro IP und Fenster, beliebig wiederholbar (jeder neue Username = neuer Key) und linear skalierend über zusätzliche Quelladressen (IP-Aliase/IPv6 sind im LAN trivial). Der \"Angreifer schadet nur sich selbst\"-Ausschluss greift nicht: keine Session nötig, und das Opfer ist der gemeinsame Host-Prozess (API, Agent-Gateway, alle Previews). Das DoS-Caveat des Bedrohungsmodells verlangt genau das (\"ohne gültige Session\") und ist erfüllt.\n\nNicht ausführbar prüfbar (kein Run erlaubt) war lediglich der finale OOM-Zeitpunkt; die unbegrenzte, angreiferkontrollierte Retention selbst ist rein statisch belegt. `ctx.clientIp` stammt aus `server.requestIP()` (`index.ts:331`), ist also nicht header-fälschbar — das begrenzt die Rate pro IP, bricht die Kette aber nicht. MEDIUM bleibt angemessen (reine Verfügbarkeit, LAN-Vorbedingung).",
          "refuted": false,
          "confidence": "HIGH"
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "Ich habe gezielt nach einer Minderung gesucht und keine gefunden; der Kandidat hält meinem Angriff stand.\n\nVerifizierte Kette:\n1. Quelle: `/Users/marco/projects/macvibes/apps/server/src/schema/index.ts:297-311` — `login` ist eine unauthentifizierte Mutation, `username: t.arg.string({ required: true })` (Zeile 300) ohne jede Längen-/Formatprüfung. Zeile 307 lautet exakt wie zitiert: `` `user:${args.username.toLowerCase()}` `` — Zeilennummer und Snippet des Forschers stimmen.\n2. Senke: `assertWithinLimit` (index.ts:112-119) reicht den Schlüssel an `rateLimiter.ts:39` (`hits.set(key, recent)`) in eine modulglobale Map (`rateLimiter.ts:25`, Limiter-Instanz index.ts:109). Die Aufräumlogik (`rateLimiter.ts:41-45`) läuft NUR bei `hits.size > 1000` — bei 20-40 Riesenschlüsseln wird also nie geräumt, die Retention ist faktisch prozesslebenslang und damit sogar schlimmer als vom Forscher beschrieben.\n3. Reihenfolge: Der Limiter läuft VOR `login(...)` (index.ts:309); `authService.ts:116-136` validiert den Username ohnehin nicht, und `usernameSchema` (`packages/shared/src/validation/auth.ts:8-13`, max. 32 Zeichen) wird ausschließlich im Register-Pfad (`authService.ts:57`) benutzt — er greift für `login` nicht.\n\nGesuchte, aber NICHT vorhandene Minderungen:\n- Kein `maxRequestBodySize` in `Bun.serve` (`apps/server/src/index.ts:313-322` enthält nur port/hostname/idleTimeout/websocket/fetch).\n- Keine Pothos-Validation-Plugin-Instanz (`apps/server/src/schema/builder.ts:19`: `new SchemaBuilder<{Context}>({})`).\n- Yoga-Plugins sind nur `useCookies()` und `useCsrfGuard()` (`apps/server/src/http/createAppYoga.ts:94`) — kein Größen-/Komplexitätslimit, kein graphql-armor in `apps/server/package.json`.\n- Der CSRF-Guard blockt curl NICHT: `originPolicy.ts:77-79` lässt Requests ohne Origin und ohne Sec-Fetch-Site bewusst durch.\n- Caddy ist laut `README.md:117` optional und der http-Backend-Port bleibt direkt erreichbar; keine `request_body max_size`-Konfiguration im Repo.\n\nGeltungsbereich: Fällt unter keines der vier akzeptierten Restrisiken (Preview-Origin, VM-Egress, Warmup-Abbruch, bypassPermissions) und nicht unter R10. Die DoS-Einschränkung der Vorgabe ist erfüllt, weil der Angriff OHNE gültige Session über den LAN-erreichbaren `/graphql` läuft; pro IP werden 20 beliebig große Schlüssel pro 5-Minuten-Fenster dauerhaft gepinnt, danach erneut 20.\n\nEinschränkung meiner Sicherheit (daher MEDIUM): Bun's Default-`maxRequestBodySize` und das tatsächliche OOM-Verhalten konnte ich nur aus dem Code ableiten, nicht messen — ausführen ist hier nicht erlaubt. Der code-belegbare Kern (unbegrenzter, angreiferbestimmter, dauerhaft gehaltener Map-Schlüssel aus einem unauthentifizierten Endpunkt ohne jedes Längenlimit) steht jedoch fest."
        }
      ]
    },
    {
      "id": "C15",
      "voters": 3,
      "refuted": 3,
      "kept": 0,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": true,
          "confidence": "MEDIUM",
          "correctedSeverity": "LOW",
          "reasoning": "CODE-WAHRHEIT: Die Zitate stimmen wörtlich. `copyProject` steht in /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:357-368, das Snippet liegt exakt auf Zeile 364; der fehlende Ownership-Check ist real und in :353-355 als gewollt dokumentiert (\"Bewusst KEINE Ownership-Prüfung\"). /Users/marco/projects/macvibes/apps/server/src/services/projectsService.ts:161-200 prüft nur `getProject(source)` und ruft `forkBranch` (gitService.ts:187-200 → `git branch <neu> <quelle>`, also Quell-HEAD). Der Workspace wird daraus geklont (workspaceService.ts:76-86). Agent-geschriebene Dateien landen tatsächlich im Branch (autoCommitService.ts:23 `add -A`, :38 `push origin HEAD`). daemon/main.ts:72/76/77 (`settingSources: ['project']`, `permissionMode: 'bypassPermissions'`) und der Systemprompt \"Lies zuerst AGENTS.md im Projekt und HALTE DICH DARAN\" (daemon/main.ts:41) existieren wörtlich; die SDK-Typen bestätigen die Semantik (node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts:1864 \"Must include 'project' to load CLAUDE.md files\"). Ich habe aktiv nach einem übersehenen Guard gesucht (Filter beim Fork, Strippen von AGENTS.md/.claude, Baseline-statt-Branch-Workspace) — es gibt keinen. Die Kette ist also code-wahr.\n\nTrotzdem refuted, weil der Sicherheits-Delta gegenüber der bewusst akzeptierten Basislinie null ist: Wer ein fremdes Projekt forkt, führt dessen Code ohnehin sofort und ohne jeden Umweg über den Agenten in der EIGENEN VM aus. copyProject übernimmt `devCommand` des Originals (projectsService.ts:184), `enterProject` startet damit die Sandbox (schema/index.ts:390-397), und vmServices.ts:63/68 führt in /work `bun install` sowie `sh -c \"bun run dev\"` aus — das \"dev\"-Skript und der gesamte Quellcode stammen vom Angreifer (templates/templates.json: devCommand \"bun run dev\"). Ein manipuliertes package.json/vite-Config ist also der direktere, garantiert wirksame Pfad; die AGENTS.md-Injection ist davon vollständig subsumiert. Zudem bleibt der behauptete Schaden innerhalb der erklärten Grenzen: nichts erreicht Host, andere VMs oder Host-Credentials; die genannten Wirkungen fallen in akzeptiertes Restrisiko #1 (geteilte Preview-Origin 4173) und #2 (freier Egress aus der VM), und \"Agent in der VM ist nicht vertrauenswürdig / bypassPermissions\" ist Restrisiko #4 bzw. Produktzweck. Das Strippen von AGENTS.md/CLAUDE.md beim Fork ist eine sinnvolle Härtung (LOW), aber kein Bruch einer Sicherheitsgrenze."
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Die Kette reißt an zwei Stellen — die Technik stimmt, aber der Angreifer gewinnt nichts, was er nicht schon per Design hat.\n\n1) Kein Push-Vektor: `copyProject` legt das Projekt IMMER beim Aufrufer an (`apps/server/src/schema/index.ts:364` -> `copyProject(..., requireUser(ctx), ...)`, Owner ist der `owner`-Parameter in `apps/server/src/services/projectsService.ts:161-186`). Der Angreifer kann keinem Opfer einen Fork unterschieben; das Opfer muss den fremden Workspace bewusst importieren (\"Kopieren und Anpassen\", Doku-Kommentar `apps/server/src/services/projectsService.ts:155-159`).\n\n2) Die Prompt-Injection ist strikt SCHWÄCHER als das, was der Import ohnehin bewirkt: `forkBranch` kopiert den kompletten HEAD des Quell-Branches (`apps/server/src/services/projectsService.ts:174`), `ensureWorkspace` klont diesen Branch in den Workspace (`apps/server/src/services/workspaceService.ts:76-86`), und das VM-Boot-Skript führt darin `bun install` sowie den devCommand aus (`apps/server/src/sandbox/vmServices.ts:63` und `:68`; devCommand ist laut `templates/templates.json` `bun run dev`, also das vom Angreifer geschriebene package.json-Skript). Angreifer-Code läuft im Opfer-VM also schon beim `enterProject`, ohne dass ein Modell irgendeine AGENTS.md befolgen muss. Die zitierten Zeilen `apps/server/src/agent/daemon/main.ts:41,76,77` (settingSources/bypassPermissions) stimmen zwar wörtlich, fügen aber keine neue Fähigkeit hinzu.\n\n3) Kein Überschreiten der Sicherheitsgrenze: Der Schaden bleibt in der MicroVM des Opfers, die laut Bedrohungsmodell (und akzeptiertem Restrisiko 4) ohnehin untrusted ist. Die VM-Env enthält keinerlei nutzerspezifisches Geheimnis — `apps/server/src/index.ts:154-165` mintet nur ein Pro-Sandbox-Token plus Egress/Gateway-URL; die Claude-Credentials im Proxy sind plattformweit (`apps/server/src/http/anthropicProxy.ts:60-64`), die eigene VM des Angreifers hat exakt denselben Zugang.\n\n4) Die behaupteten Impacts sind bereits akzeptierte Eigenschaften: \"Backdoor an LAN-Betrachter über das geteilte Preview-Gateway\" braucht keinen Fork — jede Preview teilt sich dieselbe Origin (`apps/server/src/http/previewGateway.ts:5-16`, akzeptiertes Restrisiko 1), der Angreifer kann dasselbe aus seinem EIGENEN Projekt ausliefern. \"Exfiltration des Opfer-Workspace\" ist ebenfalls kein Zugewinn: `copyProject` hat bewusst keine Ownership-Prüfung (R10), jeder darf jedes fremde Projekt forken und lesen.\n\nDamit: bewusst durch den Nutzer importierter Fremd-Code, dessen Wirkung die VM nicht verlässt — kein eigenständiges Finding."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Die technischen Einzelfakten des Kandidaten stimmen, die Sicherheitsfolge fällt aber vollständig in den bewusst akzeptierten Bereich.\n\nVerifiziert im echten Code:\n- apps/server/src/schema/index.ts:353-368 — `copyProject` ohne Ownership-Prüfung, ausdrücklich dokumentiert (\"Bewusst KEINE Ownership-Prüfung: jeder darf kopieren\", Zeile 355). Der vom Forscher genannte Zeilenbereich (364) ist korrekt.\n- apps/server/src/services/projectsService.ts:161-174 → apps/server/src/services/gitService.ts:187-200: `forkBranch` macht `git branch <neu> <quelle>`, übernimmt also tatsächlich den HEAD samt aller Dateien (inkl. AGENTS.md/CLAUDE.md/.claude).\n- apps/server/src/agent/daemon/main.ts:76-77 — `settingSources: ['project']`, `permissionMode: 'bypassPermissions'` ist real vorhanden, ebenso die Systemprompt-Anweisung \"Lies zuerst AGENTS.md ... HALTE DICH DARAN\" (main.ts:40-44).\n\nWarum trotzdem kein zu meldendes Finding:\n1. Der Payload wirkt ausschließlich INNERHALB der VM des Opfers. Der Agent dort ist laut Bedrohungsmodell per Design nicht vertrauenswürdig; akzeptiertes Restrisiko Nr. 4 (\"bypassPermissions in der VM bleibt — das ist der Produktzweck\"). Es entsteht keine neue Reichweite auf Host, andere VMs oder Host-Credentials: der Produktivpfad zwingt den echten Agenten in die MicroVM (apps/server/src/sandbox/backendSelection.ts:52-65 — Host-Agent nur mit explizitem `MACVIBES_ALLOW_HOST_AGENT=1`, sonst `UnsafeBackendError`), das Gateway/Proxy-Token ist pro Sandbox gemintet und gehasht (apps/server/src/sandbox/vmTokens.ts:33-70), und der Anthropic-Token verlässt den Host nie (Credential-Proxy).\n2. Die behauptete Exfiltration ins Netz ist akzeptiertes Restrisiko Nr. 2 (VM darf ungefiltert ins öffentliche Netz).\n3. Die behauptete \"Backdoor für LAN-Betrachter über das Preview-Gateway\" gewinnt durch `copyProject` nichts: der Angreifer kann bösartiges JS trivial in seinem EIGENEN Projekt ausliefern, alle Previews teilen ohnehin die Origin auf 4173 — akzeptiertes Restrisiko Nr. 1. Der Fork ist für diesen Impact kein notwendiges Glied.\n4. Der Rest ist der explizit gewollte Feature-Umfang aus R10 (REQUIREMENTS.md:270-278: fremde Projekte lesend zugänglich): das Opfer muss aktiv \"Kopieren und Anpassen\" eines fremden Projekts wählen und importiert damit bewusst fremden Code — funktional identisch zum `git clone` eines fremden Repos, auf dem man Claude Code laufen lässt.\n\nDie Kette ist also nicht gebrochen, aber sie endet in einer Vertrauenszone, die das Bedrohungsmodell bereits als untrusted deklariert. Kein Übertritt der Sicherheitsgrenze (VM-Grenze) nachweisbar. Empfehlung wäre allenfalls Hardening/Produktentscheidung, kein Sicherheitsbefund.",
          "correctedSeverity": "NONE"
        }
      ]
    },
    {
      "id": "C16",
      "voters": 3,
      "refuted": 0,
      "kept": 3,
      "survives": true,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "confidence": "HIGH",
          "reasoning": "Alle Zitate des Kandidaten halten der Prüfung am echten Code stand, und ich habe keinen übersehenen Guard gefunden, der die Kette bricht.\n\nCode-Wahrheit (Zeilen stimmen exakt):\n- /Users/marco/projects/macvibes/apps/server/src/services/authService.ts:88-103 — `db.transaction((tx) => { const admins = ...where(eq(users.role,'admin')).all(); const istErsterAdmin = admins.length === 0 && (bootstrapName === null || usernameResult.data === bootstrapName);` und Zeile 98 `role: istErsterAdmin ? 'admin' : 'user',`, Zeile 99 `approved: istErsterAdmin,`. Danach 112: `const session = await createSession(...)` — der Erst-Admin bekommt sofort eine Session.\n- /Users/marco/projects/macvibes/apps/server/src/config.ts:155 `adminUsername: Bun.env.MACVIBES_ADMIN_USERNAME || undefined` (Default also `null` in authService.ts:85), und :151 `hostname: Bun.env.HOST ?? '0.0.0.0'`. In apps/server/.env.example:53 ist `# MACVIBES_ADMIN_USERNAME=marco` auskommentiert → Default-Deployment ist der verwundbare Pfad.\n- Der Kandidat nennt den Resolver-Pfad ungenau (\"graphql/schema/index.ts\"); real ist /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:260-275. Inhalt und Zeilennummern (266 `resolve: async`, 268 `await register(ctx.db, ctx.config, args)`, 272 `writeSessionCookie`) stimmen. Der Resolver ruft KEIN `requireUser`/`requireAdmin` (vgl. requireUser/requireAdmin ab schema/index.ts:120/127, die hier fehlen) — die Mutation ist unauthentifiziert.\n\nAktiv gesuchte, aber nicht greifende Schutzmaßnahmen:\n- Rate-Limiter schema/index.ts:267 (`assertWithinLimit(registerLimiter, ...)`, 10/Stunde/IP) — ein einziger Aufruf genügt, kein Hindernis.\n- CSRF/Origin-Guard http/createAppYoga.ts:40-62 greift nicht: originPolicy.ts:77-79 lässt Requests ohne `Origin` UND ohne `Sec-Fetch-Site` bewusst durch (curl vom LAN).\n- ensureAdmin (authService.ts:207-230) befördert nur einen bereits existierenden, per Env konfigurierten Namen und verweigert bei vorhandenem Admin (Zeilen 220-228) — es verhindert die Erstvergabe nicht, sondern zementiert sie sogar (der Operator würde später ohne MACVIBES_FORCE_ADMIN=1 nicht mehr befördert).\n- Keine Admin-Seed-Migration: grep über apps/server/src/db/migrate.ts und drizzle/ liefert keine User-/Rollen-Insertion; db/client.ts:25 `new Database(path, { create: true })` legt eine leere DB stillschweigend an (bestätigt die Precondition \"neuer DB_PATH/MACVIBES_HOME\"); kein DB-File ist im Git getrackt (git ls-files apps/server/data leer).\n\nDie Kommentare authService.ts:75-83 (\"ohne Konfiguration bleibt der erste Nutzer aus Bequemlichkeit Admin\") und der Test authService.test.ts:265-270 bestätigen die Restlücke ausdrücklich als bewusst belassenen Komfortpfad — das ist ein Kommentar/Test, keine wirksame Minderung im Default. Der Kandidat überzeichnet nur einen Nebenpunkt (\".env.example verrät den Namen\": dort ist die Zeile auskommentiert, der echte Wert steht in der gitignorierten .env) — der Hauptpfad (Env ungesetzt = Default) bleibt davon unberührt.\n\nEinschränkung, die ich offen benenne: Der Angriff verlangt das Zeitfenster \"noch kein Admin-Datensatz\" (frische Installation/DB-Reset). Das ist ein realistisches, aber enges Race gegen den Operator; die Einstufung MEDIUM halte ich für vertretbar (Auswirkung: voller Plattform-Admin über schema/index.ts approveUser/rejectUser/deleteProject), eher am unteren Rand."
        },
        {
          "lens": "exploitability",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "Die Kette ist im echten Code lückenlos nachvollziehbar und ich habe keine Verteidigung gefunden, die sie bricht.\n\n1) Quelle / Rolle: anonym, kein Login nötig. `apps/server/src/schema/index.ts:260-276` — die `register`-Mutation ruft weder `requireUser` noch `requireAdmin` auf; einzige Vorschaltung ist `assertWithinLimit(registerLimiter, ['ip:...'])` (index.ts:267) mit `windowMs 60*60_000, max 10` (index.ts:110). Zehn Versuche pro Stunde und IP verhindern keinen einzelnen, gut getimten Request.\n\n2) Kein CSRF-/Origin-Riegel für nicht-Browser-Clients: `apps/server/src/http/originPolicy.ts` — `isCrossSiteRequest` liefert `false`, wenn weder `Origin` noch `Sec-Fetch-Site` gesetzt ist (\"Weder Origin noch Sec-Fetch-Site: kein Browser. Bewusst erlaubt\"). curl aus dem LAN passiert den Guard aus `createAppYoga.ts:41-61` also ungehindert. Erreichbarkeit: `apps/server/src/config.ts:151` `hostname: Bun.env.HOST ?? '0.0.0.0'`.\n\n3) Senke: `apps/server/src/services/authService.ts:88-103` — in der Transaktion wird `istErsterAdmin = admins.length === 0 && (bootstrapName === null || …)`, daraus `role: istErsterAdmin ? 'admin' : 'user'` (Zeile 98) und `approved: istErsterAdmin` (Zeile 99). Danach `createSession` (Zeile 112) und Cookie-Ausgabe in `schema/index.ts:272`. Das Snippet und die Zeilennummern des Forschers stimmen exakt.\n\n4) Vorbedingung erfüllbar und Default: `config.ts:155` `adminUsername: Bun.env.MACVIBES_ADMIN_USERNAME || undefined`, in `apps/server/.env.example:53` ist die Variable auskommentiert → `bootstrapName === null` im Default. Der Zustand \"keine Admin-Zeile\" tritt bei jedem frischen Deployment auf (README.md:66-68 dokumentiert genau das) und entsteht still bei geändertem `DB_PATH`/`MACVIBES_HOME`: `config.ts:108-115` plus `db/client.ts:26` (`new Database(path, { create: true })`) legen wortlos eine leere DB an, `runMigrations` seedet keinen Admin (kein `INSERT INTO` in `apps/server/drizzle/*.sql`).\n\n5) Der AP7-Fix (Commit 7f6b79d) hat nur die Atomarität und den Bootstrap-Namen-Fall geschlossen; der Kommentar `authService.ts:81-83` räumt den Restpfad selbst ein (\"ohne Konfiguration bleibt der erste Nutzer aus Bequemlichkeit Admin\"). Ein Kommentar ist keine Mitigation. Der in der Planung genannte Unique-Index `users_single_admin` existiert nicht (grep über `apps/server/drizzle` ohne Treffer).\n\n6) Schaden ist real und klebrig: Admin schaltet Nutzer frei/lehnt ab (`schema/index.ts:283,292`) und umgeht Ownership bei Projekten (`projectsService.ts:222,256`); ein einmal etablierter Admin lässt sich nicht mehr entfernen (`authService.ts:194`) und `ensureAdmin` befördert den Betreiber ohne `MACVIBES_FORCE_ADMIN=1` nicht mehr (`authService.ts:220-228`). Zusätzlich bekommt der Angreifer als einziger freigeschalteter Nutzer Zugriff auf Projekte/VMs und damit auf die Claude-Credentials des Hosts über den Proxy.\n\nEinziger Schwachpunkt bleibt das Zeitfenster (Wettlauf gegen die Erst-Registrierung des Betreibers) — das ist eine Wahrscheinlichkeits-, keine Ketten-Unterbrechung, und rechtfertigt MEDIUM statt HIGH. Keiner der vier bewusst akzeptierten Restrisiken deckt diesen Fall ab.",
          "correctedSeverity": "MEDIUM"
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "Der Kandidat hält meiner Widerlegung stand; die Kette ist im echten Code vollständig belegbar und fällt unter keines der vier akzeptierten Restrisiken.\n\n1) Quelle (unauthentifiziert): /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:260-275 — die `register`-Mutation hat KEIN `requireUser`/`requireAdmin` (zum Vergleich: `users`/`approveUser`/`rejectUser` mit `requireAdmin` in index.ts:208,283,292). /graphql ist in index.ts:328-330 ungeschützt an Yoga durchgereicht, Bind-Default `0.0.0.0` (config.ts:151).\n\n2) Senke (Privilegienvergabe): /Users/marco/projects/macvibes/apps/server/src/services/authService.ts:90-99 — `istErsterAdmin = admins.length === 0 && (bootstrapName === null || usernameResult.data === bootstrapName)` und daraus `role: 'admin'`, `approved: true`. Die zitierte Zeile 98 stimmt exakt. Danach sofort Session-Cookie (schema/index.ts:271-273). Der Kommentar authService.ts:80-83 sagt selbst: \"ohne Konfiguration bleibt der erste Nutzer aus Bequemlichkeit Admin\" — die F8-Härtung hat nur die Atomarität (db.transaction, :87) behoben, nicht die Ableitung aus der Registrierungsreihenfolge.\n\n3) Gesuchte Minderungen — alle gelesen, keine greift:\n- Rate-Limit `registerLimiter` (schema/index.ts:110, angewendet :267): 10 Registrierungen/Stunde/IP; für den Angriff genügt eine erfolgreiche.\n- `ensureAdmin` (authService.ts:207-229) legt keinen Admin an, sondern befördert nur einen existierenden Namen und verweigert das seit F21, sobald irgendein Admin existiert (:219-227) — ein vom Angreifer belegter Admin wird dadurch sogar zementiert (Rückholung nur per MACVIBES_FORCE_ADMIN=1).\n- MACVIBES_ADMIN_USERNAME schützt nicht: Bedingung :91 ist erfüllt, wenn der Angreifer genau diesen Namen registriert; .env.example:53 nennt den wahrscheinlichen Wert.\n- Kein Unique-Index/Setup-Gate: in apps/server/src/db/migrate.ts kein `users_single_admin`, kein Registrierungs-Schalter, kein Localhost-Zwang. CORS-Allowlist (index.ts:303-311) betrifft nur Browser-Origins, nicht einen direkten POST.\n\n4) Geltungsbereich: Weder eines der vier akzeptierten Restrisiken (NAECHSTE-SESSION.md:70-78 und security-haertung-plan.md:554-566: Preview-Origin 4173, VM-Public-Egress, Warmup-Abbruch, bypassPermissions) noch R10 deckt das ab. Im Gegenteil steht der Punkt in NAECHSTE-SESSION.md:53 ausdrücklich als OFFEN (\"F5 — Unauthentifizierte Registrierung samt Admin-Bootstrap … bitte gezielt prüfen, ob sie real sind\").\n\n5) Die Vorbedingung ist breiter als \"nur beim Erstinstall\": config.ts:108-115 prüft die Alt-DB über den RELATIVEN Pfad `./data/app.db` (:111). Bei Start aus anderem Arbeitsverzeichnis oder geändertem MACVIBES_HOME/DB_PATH entsteht still eine leere DB, die Instanz läuft weiter auf 0.0.0.0 ohne Admin-Zeile, und der erste LAN-Registrant wird Admin samt Session.\n\nEinzige Unsicherheit (daher MEDIUM statt HIGH): der Angriff braucht das Zeitfenster ohne Admin-Zeile, der Angreifer muss pollen. Das begrenzt die Ausnutzbarkeit, ist aber keine Minderung im Code — die Einstufung MEDIUM ist angemessen."
        }
      ]
    },
    {
      "id": "C17",
      "voters": 3,
      "refuted": 3,
      "kept": 0,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Code-Wahrheit: Das Zitat stimmt wörtlich. /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:190-193 enthält `templates: t.field({ type: [TemplateRef], resolve: (_root, _args, ctx) => loadTemplates(ctx.config.templatesDir) })` ohne `requireUser`; der Vergleichspunkt bei :214-216 (`requireUser(ctx); return listProjects(ctx.db);`) stimmt ebenfalls. Auch die Erreichbarkeit habe ich bestätigt: createAppYoga.ts:94 lädt nur `useCookies()` und `useCsrfGuard(...)` — kein globaler Auth-Plugin; der Context (createAppYoga.ts:95-107) setzt `currentUser` nur, erzwingt nichts, und originPolicy.ts:77-79 lässt Requests ohne Origin/Sec-Fetch-Site bewusst durch, d.h. ein curl ohne Cookie erreicht den Resolver. Ein übersehener Guard existiert also nicht; nur die Zeilenangaben zu templatesService.ts sind um eins verschoben (existsSync/statSync steht dort auf Zeile 42, nicht 41).\n\nWiderlegt wird der Kandidat an der Wirkung, nicht am Zitat. Offengelegt werden exakt die Felder aus schema/index.ts:47-51 (name, description, dir, devCommand, previewPort), gespeist aus /Users/marco/projects/macvibes/templates/templates.json: zwei Einträge mit `\"devCommand\": \"bun run dev\"` und `\"previewPort\": 5173`, dazu relative Ordnernamen \"pwa\"/\"fullstack\". Keine Credentials, keine Hostpfade, keine Nutzerdaten — die behaupteten \"internen Build-/Run-Details\" sind der dokumentierte Standardbefehl und ein Standardport; der Gateway-Port (4173) ist per Design öffentlich erreichbar (akzeptiertes Restrisiko 1). Der behauptete \"CPU/IO-Amplifier\" ist ein Bun.file-Read einer ~1 KB JSON plus ein readdirSync über ein Verzeichnis mit zwei Einträgen — weniger Arbeit als das ohnehin unauthentifizierte Ausliefern des Web-Bundles im selben fetch-Handler (index.ts:337). Damit fehlt die \"real dangerous operation\": es bleibt eine Konsistenz-/Härtungslücke (requireUser fehlt), kein echtes Finding im Bedrohungsmodell."
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Der technische Teil des Kandidaten stimmt: `/Users/marco/projects/macvibes/apps/server/src/schema/index.ts:190-192` ruft im `templates`-Resolver kein `requireUser` auf (Kontrast: :215 `requireUser(ctx)` bei `projects`), und der Weg dorthin ist anonym offen — `/Users/marco/projects/macvibes/apps/server/src/index.ts:328-331` reicht `/graphql` ohne HTTP-seitige Auth an Yoga durch, und der CSRF-Guard (`/Users/marco/projects/macvibes/apps/server/src/http/originPolicy.ts:77-79`) lässt Requests ohne `Origin` und ohne `Sec-Fetch-Site` bewusst passieren — ein curl-Aufruf wie im Exploit-Szenario wird also nicht geblockt. Die Kette reißt aber beim SCHADEN, und darauf kommt es an:\n\n1. Keine sensiblen Daten. `TemplateRef` exponiert exakt name/description/dir/devCommand/previewPort (`apps/server/src/schema/index.ts:44-52`). Die konkreten Werte stehen in `/Users/marco/projects/macvibes/templates/templates.json`: `dir` = \"pwa\"/\"fullstack\", `devCommand` = \"bun run dev\", `previewPort` = 5173. Das ist eingechecktes, öffentliches Repo-Inhalt, keine Deployment-Geheimnisse; Hostpfade werden nicht zurückgegeben (`templatesDir` selbst bleibt serverseitig, `apps/server/src/services/templatesService.ts:18-20`). Der behauptete Impact \"Preisgabe der Kommandos, die die Plattform in Sandboxes ausführt\" ist damit die Preisgabe von \"bun run dev\".\n\n2. `previewGatewayPort` ist kein Geheimnis: das Gateway lauscht selbst auf `0.0.0.0` (`/Users/marco/projects/macvibes/apps/server/src/http/previewGateway.ts:232`), ein Portscan liefert dieselbe Information. Zudem ist Introspection nicht deaktiviert (Plugins in `/Users/marco/projects/macvibes/apps/server/src/http/createAppYoga.ts:94` sind nur `useCookies()` und `useCsrfGuard`), Recon-Wert also ohnehin null.\n\n3. Kein realer DoS-Verstärker. `loadTemplates` liest eine kleine JSON-Datei, macht pro Eintrag ein `existsSync`/`statSync` und ein `readdirSync` über ein Verzeichnis mit zwei Ordnern (`apps/server/src/services/templatesService.ts:19-53`) — weniger Arbeit als das anonyme Ausliefern des Web-UI-Bundles über denselben Server (`apps/server/src/index.ts:336`). Nach dem Bedrohungsmodell (\"DoS nur bei realistischem Aufwand\") ist das kein Finding.\n\nFazit: vollständige Kette anonym → Antwort existiert, aber am Ende steht nur öffentlich bekannte, generische Metadaten. Fehlender `requireUser` ist hier eine Konsistenz-/Härtungsempfehlung (\"violates best practice\"), keine ausnutzbare Schwachstelle — der Reporter räumt selbst ein, dass nichts davon Funktionalität oder Geheimnisse berührt."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "confidence": "HIGH",
          "correctedSeverity": "NONE",
          "reasoning": "Der Faktenteil stimmt: `templates` in /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:190-193 ruft — anders als `agentModels` (186), `projects` (215) oder `users` — kein `requireUser` auf; ebenso `previewGatewayPort` (197-199) und `previewGatewayHttpsPort` (201-204). Die Kausalkette zu einem meldepflichtigen Problem bricht aber an Umfang und Wirkung:\n\n1) Nichts Schützenswertes wird offengelegt. `TemplateRef` (schema/index.ts:44-53) exponiert genau name/description/dir/devCommand/previewPort. Der Inhalt kommt aus /Users/marco/projects/macvibes/templates/templates.json — zwei Einträge, `devCommand: \"bun run dev\"`, `previewPort: 5173`, `dir: \"pwa\"|\"fullstack\"` — statischer, im Repo (github.com/vanmeegen/macvibes) mitgelieferter Manifest-Inhalt, keine Nutzerdaten, keine Secrets. Der Host-Pfad `templatesDir` wird NICHT zurückgegeben; Inkonsistenz-Warnungen mit absoluten Pfaden gehen ausschließlich an `logger.warn` (services/templatesService.ts:21, 43, 52), nicht in die GraphQL-Antwort. Die \"internen Build-/Run-Details\" des Kandidaten sind damit Public-Repo-Defaults.\n\n2) Der einzig relevante Angreifer aus dem Bedrohungsmodell (Gast/Preview) ist bereits gemindert. Der CSRF-/Origin-Guard `useCsrfGuard` (http/createAppYoga.ts:41-61) prüft über `isCrossSiteRequest` (http/originPolicy.ts:66-83) jede Anfrage; die erlaubten Origins werden aus dem Host-Header abgeleitet (originPolicy.ts:32-47), d. h. Origin `http://<host>:4173` (Preview) ist nicht enthalten → 403. Browser-getriebene Lesezugriffe aus einer Preview scheitern also, und die VM erreicht Host/LAN ohnehin nicht (Egress-Policy, akzeptiertes Restrisiko Nr. 2 sperrt LAN + Loopback). Es verbleibt ein nicht angemeldeter LAN-Client mit curl, der genau die oben genannten Public-Defaults erfährt.\n\n3) Die mitgeleakten Gateway-Ports sind wertlos: das Preview-Gateway verlangt selbst eine Session (http/previewGateway.ts:205-239, `unauthorized()` in Zeile 190), und ein offener Port ist per Portscan ohnehin sichtbar.\n\n4) Der behauptete \"CPU/IO-Verstärker\" ist keiner: `loadTemplates` macht pro Aufruf einen kleinen Dateiread + JSON.parse, zwei `statSync` und ein `readdirSync` auf dem Templates-Ordner (services/templatesService.ts:18-55) — konstante, winzige Arbeit, geringer als das GraphQL-Parsing des Requests selbst. Nach der DoS-Regel des Auftrags kein Finding.\n\nErgebnis: eine Konsistenz-/Defense-in-Depth-Lücke ohne Sicherheitsfolge (kein sensitiver Sink), keine echte Autorisierungsverletzung. Empfehlung `requireUser` bleibt sinnvoll als Code-Hygiene, ist aber nicht meldepflichtig."
        }
      ]
    },
    {
      "id": "C18",
      "voters": 3,
      "refuted": 2,
      "kept": 1,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "correctedSeverity": "LOW",
          "confidence": "MEDIUM",
          "reasoning": "CODE-WAHRHEIT: Alle Zitate des Kandidaten stimmen zeilengenau mit /Users/marco/projects/macvibes/apps/server/src/http/previewGateway.ts überein. `interface WsProxyData` steht bei 163, `pending: (string | Buffer)[]` bei 170, `pending: []` in den Upgrade-Daten bei 259, `open(ws: ServerWebSocket<WsProxyData>)` bei 293, `const url = 'ws://127.0.0.1:${ws.data.vmPort}${ws.data.path}'` bei 294, das `new WebSocket(url, ws.data.protocol)` ohne try/catch bei 295-296, `message(...)` bei 309 und der Sink `else ws.data.pending.push(message);` exakt bei 312. Nichts halluziniert.\n\nAktive Suche nach dem übersehenen Guard — nichts gefunden: (a) Kein Cap im Handler 309-313, kein Drop, kein Byte-Zähler; geleert wird `pending` nur im upstream-`open`-Listener (300-301). (b) Kein `maxPayloadLength`/`backpressureLimit`/`closeOnBackpressureLimit` irgendwo gesetzt (repoweiter grep über apps/ und packages/ liefert nur node_modules/bun-types/serve.d.ts:432/439) — es gelten Bun-Defaults 16 MiB. (c) Der einzige potentielle Auto-Close, Buns `idleTimeout` (Default 120 s, node_modules/bun-types/serve.d.ts:454), greift laut eigener Doku nur \"due to no messages or pings\" — ein aktiv flutender Client hält die Verbindung offen. (d) Kein Connect-Timeout und kein `ws.close()` im Fehlerfall des Konstruktors in 293-307. (e) Kein Rate-Limit/Connection-Cap vor dem Gateway; es lauscht per Default auf 0.0.0.0:4173 (config.ts:151, config.ts:172, index.ts:216-220).\n\nDer einzige Guard auf dem Pfad ist die Session-Prüfung bei previewGateway.ts:238 (`isAuthenticated(...)` vor dem Upgrade) — die bricht die Kette nicht, der Forscher nennt sie selbst als Vorbedingung. Ownership wird bewusst nicht geprüft (index.ts:219-225, R10).\n\nKettenglied, das ich NICHT durch Ausführen verifizieren konnte: dass `ready` dauerhaft false bleibt. Das braucht einen Guest-Dev-Server, der TCP annimmt und den WS-Handshake hängen lässt (bei Reset/HTTP-Fehler feuert `error` → `ws.close()` bei 306). Unter dem macvibes-Modell ist der Gast per Design nicht vertrauenswürdig und vom angemeldeten Nutzer über den Agenten frei beschreibbar, also erfüllbar. Die zweite behauptete Variante (Konstruktor-Throw bei krummem `sec-websocket-protocol`) ist reines Bun-Laufzeitverhalten und von mir nicht belegbar.\n\nSeverity-Korrektur auf LOW: Es ist ein reines Availability-Problem, das eine gültige Session UND einen kooperierenden Gast braucht, und die Plattform räumt angemeldeten Nutzern ohnehin sanktioniert 8 × 4096 MiB Host-RAM über Sandbox-Starts ein (config.ts:162, config.ts:171, sandboxManager.ts:241, microsandboxProvider.ts:190). Kein Selbst-DoS im Sinne des akzeptierten Restrisikos, da der Host-Prozess alle VMs, die SQLite-DB und die Proxies trägt — deshalb kein Refute, aber MEDIUM ist zu hoch."
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Die Code-Behauptung stimmt, die Ausnutzungskette hält dem Bedrohungsmodell aber nicht stand.\n\nVerifiziert am echten Code: `pending` ist tatsächlich ungedeckelt (apps/server/src/http/previewGateway.ts:170, Push ohne Cap in Zeile 312, Drain nur im upstream-`open`-Listener Zeile 298-302). Kein `maxPayloadLength`/`backpressureLimit` gesetzt (Bun.serve-Optionen in previewGateway.ts:230-232).\n\nBruchstellen der Kette:\n\n1) JEDER Weg zur Senke passiert die Session-Prüfung previewGateway.ts:238 — vor `srv.upgrade` (Zeile 252). Das ist auch getestet (\"auch der WebSocket-Upgrade verlangt eine Session\", apps/server/src/http/__tests__/previewGateway.test.ts:230-266). Eine Session ist nicht frei erhältlich: nur der ERSTE Nutzer bekommt beim `register` sofort eine Session, alle anderen sind pending und brauchen `approveUser` (apps/server/src/schema/index.ts:266-286). Anonyme Auslösung ist damit ausgeschlossen.\n\n2) Der Gast selbst (das eigentliche Angreifer-Subjekt des Modells) kann :4173 gar nicht direkt erreichen: die Egress-Policy blockt Loopback und alle privaten Netze des Hosts (apps/server/src/http/egressPolicy.ts:52-60, `a === 127`, `192.168`, `10.`, `172.16-31`) — und selbst wenn, hätte die VM kein Session-Cookie (previewGateway.ts:238; das Cookie wird der VM zusätzlich entzogen, sanitizeUpstreamHeaders Zeile 105-121). Bleibt nur der Umweg \"VM-JS im Browser eines angemeldeten Betrachters\", der das Opfer zwingt, minuten- bis stundenlang zweistellige GB-Mengen hochzuladen.\n\n3) Die zwingende Vorbedingung \"upstream wird nie `open`\" ist praktisch nur durch einen vom Angreifer selbst geschriebenen, den Handshake hängen lassenden Dev-Server im EIGENEN Projekt herstellbar. Ein nicht erreichbarer/normal antwortender Upstream feuert `error`/`close` und schließt den Client (previewGateway.ts:306-307). Die Variante \"`new WebSocket(url, protocol)` wirft bei krummem `sec-websocket-protocol`\" (previewGateway.ts:296) konnte ich statisch nicht belegen — dazu müsste ich Bun ausführen; ich werte sie deshalb nicht als bestätigt.\n\n4) Keine Amplifikation und kein Privilegiengewinn: um N Bytes Host-Heap zu binden, muss der Angreifer N Bytes senden (Frames werden 1:1 abgelegt, Zeile 312); beim Schließen räumt `close` (Zeile 314-316) auf und `ws.data` wird unerreichbar, d. h. der Angreifer muss die Verbindung dauerhaft aktiv fluten (Bun-Default-idleTimeout schließt sonst).\n\nDamit ist das exakt der im Auftrag ausgenommene Fall: ein angemeldeter (admin-freigeschalteter) Nutzer, der mit einem selbst gebauten Stall-Dev-Server und einem byte-für-byte teuren Flood den Server lahmlegt, den er selbst benutzt — DoS ohne Session oder mit Amplifikation ist nicht gegeben. Realer Härtungsvorschlag bleibt sinnvoll, ein Finding im Sinne des Modells ist es nicht."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Der Code-Befund selbst stimmt: `pending: (string | Buffer)[]` (previewGateway.ts:170) wird in `message()` ohne Größen-, Frame- oder Byte-Limit befüllt (previewGateway.ts:311-312), es gibt keinen Connect-Timeout und kein `ws.close()`, falls `upstream` nie `open` feuert. Ein Rate-Limit greift hier nicht — `createRateLimiter` wird ausschließlich in schema/index.ts:109-110 für `login`/`register` verwendet, nicht im Gateway. Trotzdem ist das kein zu meldendes Finding, aus Geltungsbereichs-Gründen:\n\n1) Gültige Session ist zwingend. previewGateway.ts:238 prüft `isAuthenticated(...)` VOR `srv.upgrade` (Zeile 252); der Callback ist real verdrahtet (`index.ts:223-224`, `resolveSession`) und fail-closed bei `token === null` (previewGateway.ts:211 + index.ts:224). Neue Nutzer sind zudem erst nach Admin-Freigabe login-fähig (authService.ts:47-50). Der Angreifer ist damit exakt der Fall, den das Bedrohungsmodell ausklammert: „Ein angemeldeter Nutzer, der seinen eigenen Server langsam macht, ist kein Finding.\"\n\n2) Der Impact-Kern („Ausbruch aus `config.sandbox.memoryMib`\") ist bereits durch eine bewusst akzeptierte Design-Entscheidung dominiert: derselbe angemeldete Nutzer darf laut security-haertung-plan.md:562-563 („Fremde Sandboxes dürfen weiter gestartet werden … kein Rate-Limit darauf\") bis `maxSandboxes` = 8 VMs à `memoryMib` = 4096 MiB starten (config.ts:162, config.ts:171, sandboxManager.ts:241). Host-Speicher-Erschöpfung durch einen authentifizierten Nutzer ist also schon auf einem einfacheren, explizit akzeptierten Weg möglich — die WS-Queue eröffnet keine neue Grenze.\n\n3) Der „ohne bösartige VM\"-Pfad ist nicht belegt. Der Normalfall endet sauber: liefert der VM-Dev-Server keinen WS-Handshake, feuert `error`/`close` und die Client-Verbindung wird geschlossen (previewGateway.ts:306-307). Der Dauer-Stau setzt voraus, dass der Nutzer den Dev-Server in der eigenen VM vorher per Agent zum Hängen bringt. Die Alternative „`new WebSocket(url, protocol)` wirft bei malformed `sec-websocket-protocol` und lässt `upstream` für immer null\" (previewGateway.ts:296) ist eine reine Laufzeitbehauptung über Buns WebSocket-Client und über Buns Verhalten bei einer Exception im `open`-Handler — das lässt sich hier ohne Ausführung nicht bestätigen, und ich erfinde weder Verteidigung noch Lücke.\n\n4) Es liegt zudem kein dauerhaftes Leck vor: `close()` (previewGateway.ts:314-316) verwirft `ws.data` samt `pending`, der Speicher ist nur solange gebunden, wie die Verbindung lebt.\n\nErgebnis: authentifizierte, admin-freigeschaltete Ressourcen-Erschöpfung auf dem eigenen lokalen Host, mit vom selben Nutzer kontrolliertem Upstream als Voraussetzung — fällt unter die DoS-Ausklammerung des Bedrohungsmodells. Code-Qualitativ wäre ein Cap sinnvoll (Empfehlung des Forschers ist vernünftig), sicherheitsrelevant im hiesigen Modell ist es nicht."
        }
      ]
    },
    {
      "id": "C19",
      "voters": 3,
      "refuted": 0,
      "kept": 3,
      "survives": true,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "confidence": "HIGH",
          "reasoning": "Code-Wahrheit geprüft, alle Zitate halten stand:\n\n1) Sink exakt wie zitiert: /Users/marco/projects/macvibes/apps/server/src/http/anthropicProxy.ts:206 `const outgoingBody = hasBody ? injectThinkingDisplay(await request.text()) : undefined;` und :207 `const route = routeFor(modelFromBody(outgoingBody));`. Der Body wird also vollständig als String materialisiert und danach ZWEIMAL geparst: anthropicProxy.ts:39 (`parsed = JSON.parse(bodyText)` in injectThinkingDisplay) und anthropicProxy.ts:152 (`const parsed: unknown = JSON.parse(bodyText)` in modelFromBody). Einziger Zitatfehler: der Evidence-Block schreibt „151\" statt 152 — Off-by-one, die Rationale selbst nennt 152 korrekt.\n\n2) Quelle wirklich angreiferkontrolliert: apps/server/src/agent/vmAgentEnv.ts:30-31 setzt `ANTHROPIC_BASE_URL: http://host.microsandbox.internal:<port>/anthropic` und `ANTHROPIC_CUSTOM_HEADERS: x-macvibes-proxy-token: <vmToken>` — die per Bedrohungsmodell untrusted VM besitzt also ein gültiges Token. Der Token-Gate an anthropicProxy.ts:197 (`config.verifyToken(...) === null → 401`) steht zwar VOR dem Body-Lesen, ist aber für den Gast kein Hindernis, sondern genau sein Normalpfad. Routing: apps/server/src/index.ts:333-335 ruft `anthropicProxy(request, upstreamPath)` ohne jede Zwischenschicht auf.\n\n3) Aktiv nach dem übersehenen Guard gesucht — es gibt keinen: grep über apps/server/src findet KEIN `maxRequestBodySize` (nur die Typdeklaration in node_modules/bun-types/serve.d.ts:676-678 mit `@default 1024*1024*128`), also gilt Buns 128-MiB-Default; `Bun.serve` in apps/server/src/index.ts:313-316 setzt nur `port/hostname/idleTimeout: 255`. Keine content-length-Prüfung im Repo (die einzigen Treffer sind `headers.delete('content-length')` in anthropicProxy.ts:226/256). Rate-Limiting existiert nur für GraphQL-Login/Register (apps/server/src/schema/index.ts:109-110), nicht für /anthropic/*. Caddy (:443→4000) liegt nicht im Pfad, weil die VM laut vmAgentEnv.ts:30 direkt http auf Port 4000 spricht. Einziger Produktiv-Aufrufer ist index.ts:335, kein Wrapper.\n\n4) Kette vollständig: Ein 128-MiB-Body aus `[{},{},...]` überlebt injectThinkingDisplay (Rückgabe unverändert an anthropicProxy.ts:45, da `thinking` fehlt) und wird an :207 sofort ein zweites Mal geparst — beides synchron auf dem einzigen JS-Thread des Servers, der zugleich Agent-Gateway, GraphQL und DB hält. Das ist eine echte Gast→Host-Grenzüberschreitung (VM-Speicherlimit wird umgangen) mit realistischem Aufwand (ein paar curl-Aufrufe), fällt unter keines der vier akzeptierten Restrisiken und ist kein „Nutzer verlangsamt seinen eigenen Server\".\n\nEinschränkung: Ob das konkret in einen OOM-Kill mündet oder „nur\" in Event-Loop-Blockade plus massiven Speicherdruck, konnte ich ohne Ausführung nicht messen; die Availability-Auswirkung auf den Host besteht in beiden Fällen. MEDIUM bleibt angemessen."
        },
        {
          "lens": "exploitability",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "Die Kette ist im echten Code lückenlos nachvollziehbar und die benötigte Rolle ist genau die, die das Bedrohungsmodell als untrusted deklariert (Code in der VM).\n\n1) Angreifer-Rolle und Zugang: Der Gast bekommt Token und Ziel-URL frei Haus — apps/server/src/agent/vmAgentEnv.ts:30-31 setzt `ANTHROPIC_BASE_URL=http://host.microsandbox.internal:<port>/anthropic` und `ANTHROPIC_CUSTOM_HEADERS: x-macvibes-proxy-token: <token>` in die VM. Das Token ist gültig (Registry apps/server/src/sandbox/vmTokens.ts:44-49) und passiert die einzige Schranke apps/server/src/http/anthropicProxy.ts:197. Netzweg ist offen: apps/server/src/sandbox/microsandboxProvider.ts:186 `allow@public,allow@172.16.0.0/12` erlaubt genau den Host-Gateway; Route ohne weitere Prüfung in apps/server/src/index.ts:333-336. Kein Rate-Limit auf diesem Pfad (Limiter existieren nur für Login/Register, apps/server/src/schema/index.ts:109-110).\n\n2) Sink: apps/server/src/http/anthropicProxy.ts:206 puffert den kompletten Body (`await request.text()`), parst ihn synchron in injectThinkingDisplay (anthropicProxy.ts:39) und — weil bei fehlendem `thinking` der Originaltext zurückkommt (anthropicProxy.ts:45) — in derselben Microtask ein zweites Mal in modelFromBody (anthropicProxy.ts:152, Kandidat nennt 151, faktisch 152). Nirgends eine Längen-/Content-Length-Prüfung; `maxRequestBodySize` ist im gesamten Repo nicht gesetzt (grep über apps/ und packages/ ohne Treffer), also greift nur Buns Default von 128 MiB (dokumentiert in node_modules/bun-types/serve.d.ts:675-678).\n\n3) Schaden jenseits der VM-Grenze: Ein Body wie `[{},{},…]` (128 MiB ≈ 4,4·10^7 Objekte) erzeugt pro Parse ein Vielfaches der Bodygröße auf dem JS-Heap des Host-Prozesses, und zwar zweimal je Request; zusätzlich blockiert jeder synchrone Parse den einzigen Event-Loop von Bun.serve (apps/server/src/index.ts:313-320), der GraphQL, Agent-Gateway und Preview-Status für ALLE Projekte bedient. Das umgeht die per-VM-Grenze von 4096 MiB (apps/server/src/config.ts:171) und trifft fremde Nutzer, ist also kein \"Nutzer verlangsamt seinen eigenen Server\". Kein Punkt der akzeptierten Restrisiken (Port 4173, Egress, Warmup, bypassPermissions) deckt Host-Ressourcenerschöpfung ab.\n\nEinschränkung, die meine Confidence auf MEDIUM hält: dass daraus zwingend ein OOM-Kill wird (so die Impact-Formulierung des Forschers), kann ich ohne Ausführung nicht messen; belegbar ist die Verstärkung (128 MiB Eingabe → GB-Bereich JS-Objekte) und die deterministische Event-Loop-Blockade. Beides ist ein realer Gast→Host-Verfügbarkeitsangriff, MEDIUM bleibt damit vertretbar."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "Linse 3 (Geltungsbereich/Minderung) findet keine Entlastung.\n\nCode verifiziert (Zeilen stimmen bis auf eine Off-by-one):\n- `apps/server/src/http/anthropicProxy.ts:206` puffert den Body ungebremst: `const outgoingBody = hasBody ? injectThinkingDisplay(await request.text()) : undefined;`\n- Doppeltes Parsen bestätigt: `anthropicProxy.ts:39` (`JSON.parse(bodyText)` in `injectThinkingDisplay`) und `anthropicProxy.ts:152` (`const parsed: unknown = JSON.parse(bodyText);` in `modelFromBody`, Forscher schrieb 151). `injectThinkingDisplay` gibt bei fehlendem `thinking` den Originaltext zurück (Z. 45) — derselbe 128-MiB-String wird also ein zweites Mal geparst.\n- Quelle ist der per Bedrohungsmodell untrusted Gast: `apps/server/src/agent/vmAgentEnv.ts:30-31` setzt `ANTHROPIC_BASE_URL: http://host.microsandbox.internal:<port>/anthropic` und legt den gültigen Proxy-Token als `ANTHROPIC_CUSTOM_HEADERS` in die VM-Env; `NO_PROXY` (Z. 37) hält den Pfad direkt (also an Caddy vorbei). Der Agent mit bypassPermissions kann den Token trivial lesen und beliebige POSTs abfeuern.\n\nGezielte Suche nach Minderung — nichts gefunden:\n- Kein Größenlimit irgendwo: `grep maxRequestBodySize|content-length|maxBody` über apps/packages liefert nur die Header-Löschungen in `anthropicProxy.ts:226/256`, kein `maxRequestBodySize` in `Bun.serve` (`apps/server/src/index.ts:313-320`, nur `idleTimeout: 255`). Der einzige Deckel ist Buns dokumentierter Default von 128 MB (`node_modules/bun-types/serve.d.ts:677-678`).\n- Kein Rate-Limit auf dem Pfad: `createRateLimiter` (`apps/server/src/services/rateLimiter.ts`) wird ausschließlich in `apps/server/src/schema/index.ts` für login/register verwendet; `/anthropic/*` wird in `index.ts:333` roh an den Proxy gereicht.\n- Der Token-Check `anthropicProxy.ts:197` liegt zwar VOR dem Body-Lesen und schützt gegen unauthentifizierte LAN-Clients — gegen den Angreifer dieses Findings (die VM selbst) ist er per Definition wirkungslos.\n- Kein Reverse-Proxy dazwischen: VM→Host läuft laut `vmAgentEnv.ts:30` plain http auf Port 4000.\n\nGeltungsbereich: Keines der vier akzeptierten Restrisiken (Preview-Origin 4173, freier Public-Egress, host-seitiger Warmup-Abbruch, bypassPermissions) und auch R10 decken das ab. Der DoS-Vorbehalt greift nicht: der Auslöser ist kein „angemeldeter Nutzer, der seinen eigenen Server langsam macht\", sondern der explizit untrusted Gast, und der Schaden trifft den Host-Prozess mit allen VMs, DB und Credential-Store — also die VM-Grenze in der Verfügbarkeitsdimension; der Aufwand (ein paar POSTs) ist realistisch. Auch ist es nicht reine Code-Qualität: das doppelte Parsen ist Qualität, das fehlende Limit hat Sicherheitsfolge.\n\nRestzweifel (deshalb nur MEDIUM, Impact ist Linse 2): Buns 128-MiB-Default begrenzt jeden Einzelrequest, ein echter OOM braucht also anhaltende Parallelität, und beide `JSON.parse` stecken in `try/catch` (`anthropicProxy.ts:40` und `:156`), die einen JS-seitigen RangeError schlucken würden. Die vom Forscher behauptete Härte („OOM-kill\" pro Handvoll Requests) konnte ich ohne Ausführung nicht bestätigen; MEDIUM als Einstufung erscheint aber vertretbar."
        }
      ]
    },
    {
      "id": "C20",
      "voters": 3,
      "refuted": 2,
      "kept": 1,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Code-Wahrheit: Die Zitate stimmen exakt. `interface ConnState { buf; upstream: Socket|null; established: boolean }` steht in /Users/marco/projects/macvibes/apps/server/src/http/egressProxy.ts:93-97, `client.data.upstream = up;` in :148, das Established-Gate `if (state.established && state.upstream)` in :179, `state.established = true` in :222 (bzw. :243) und `socket.data.upstream?.end();` in :253. Auch der Startpunkt stimmt: /Users/marco/projects/macvibes/apps/server/src/index.ts:54-58 startet den Proxy auf Default-Port 4010, Bind auf '0.0.0.0' in egressProxy.ts:170. Einen übersehenen Guard gibt es tatsächlich nicht: `state.buf` wird in :191 geleert, es existiert kein `pending`-Flag, also wird ein zweiter Kopf im asynchronen Fenster von `checkTargetFn` (:128) erneut geparst, authentisiert und via `connectChecked` verbunden — der erste Upstream wird in :148 überschrieben und nie geschlossen. Der beschriebene Socket-Leak ist als Code-Defekt real.\n\nTrotzdem widerlegt: Die Kausalkette zum behaupteten Impact bricht. (a) Die \"Verstärkung von einer eingehenden auf viele ausgehende Verbindungen\" setzt voraus, dass eingehende Verbindungen begrenzt wären — sind sie nicht: `Bun.listen` in egressProxy.ts:169-172 setzt keinerlei Connection-Cap, und im gesamten Repo existiert kein maxConnections/Rate-Limit (grep über apps/server/src, packages: kein Treffer). Ein Token-Inhaber kann also schon ohne den Bug beliebig viele Host-Upstream-Sockets dauerhaft offenhalten, indem er einfach viele CONNECT-Tunnel aufmacht und hält; der Leak spart ihm bestenfalls einen konstanten Faktor. (b) Der geleakte Socket kann per Policy ausschließlich ein öffentliches Ziel auf 80/443 sein — /Users/marco/projects/macvibes/apps/server/src/http/egressPolicy.ts:11-12 (DEFAULT_ALLOWED_PORTS) und :49-60 (isBlockedIp sperrt Loopback/172.16/8/192.168/169.254/CGNAT). Genau dieses \"VM darf ungefiltert ins öffentliche Netz\" ist bewusst akzeptiertes Restrisiko Nr. 2. (c) Die \"zwei Upstreams schreiben in denselben Client-Socket\"-Konfusion (Handler :151-153) betrifft ausschließlich die vom Angreifer selbst geöffnete Verbindung — keine Tenant-Grenze, kein Datenabfluss zwischen VMs; beide Köpfe durchlaufen den vollen Policy- und Auth-Pfad (:205, :128), also auch kein Policy-Bypass/Smuggling. (d) Voraussetzung ist ein gültiges Per-Sandbox-Token (Prüfung in :205 gegen vmTokens.lookup, /Users/marco/projects/macvibes/apps/server/src/sandbox/vmTokens.ts:55-70) — ohne Token liefert der Proxy 407 und beendet (:206-211), ein LAN-Angreifer ohne Token kommt gar nicht in den Pfad. Damit ist es ein DoS-artiger Befund eines bereits privilegierten Akteurs, der dieselbe Ressourcenwirkung mit geringerem Aufwand auf dem vorgesehenen Weg erzielt — kein eigenständiges Finding (Hygiene-/Robustheitsdefekt, sinnvoll zu fixen, aber ohne zusätzliche Angreiferfähigkeit)."
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Der Code-Defekt existiert, die daraus abgeleitete Angriffskette liefert dem Angreifer aber keine neue Fähigkeit.\n\nVerifiziert (Mechanik stimmt):\n- /Users/marco/projects/macvibes/apps/server/src/http/egressProxy.ts:148 weist `client.data.upstream = up` bedingungslos zu; die einzige Aufräumstelle ist :253 (`socket.data.upstream?.end()`) bzw. :257 und schließt nur die zuletzt zugewiesene Referenz.\n- Das Rennfenster ist real: :176-191 verarbeitet pro `data`-Event genau einen Kopf und setzt `state.buf` zurück; `established` wird erst im async `onOpen` (:222 bzw. :243) gesetzt, und `checkTargetFn` (:128) macht immer mindestens einen DNS-Roundtrip (:45). Zwei Köpfe in zwei getrennten Reads erzeugen also zwei Upstreams, der zweite verwaist den ersten.\n- Angreiferrolle erfüllbar: Code in der VM besitzt das Sandbox-Token via `HTTP_PROXY` (/Users/marco/projects/macvibes/apps/server/src/agent/vmAgentEnv.ts:35-36), Port 4010 bindet 0.0.0.0 (egressProxy.ts:170, index.ts:54-58).\n\nWarum die Kette für den behaupteten Schaden trotzdem reißt:\n1. Der Proxy hat NIRGENDS ein Limit für gleichzeitige Client- oder Upstream-Verbindungen — in egressProxy.ts:169-260 wird nichts gezählt, kein Cap pro Token, kein globaler Zähler. Ein Gast kann daher bereits durch völlig reguläre Nutzung N Verbindungen zu 4010 öffnen, N CONNECTs absetzen und sie einfach nie schließen. Die behauptete FD-Erschöpfung des Hosts ist ohne den Bug identisch erreichbar; der Leak spart dem Angreifer lediglich den client-seitigen FD (Faktor ~2), er schaltet nichts frei.\n2. Die behauptete Verstärkung \"eine eingehende Verbindung → viele gehaltene ausgehende\" ist symmetrisch wirkungslos: jeder verwaiste Upstream muss von einem angreiferkontrollierten Public-Host auf Port 80/443 offengehalten werden (egressPolicy.ts:12 `DEFAULT_ALLOWED_PORTS`, checkTarget :127-149). Für N gehaltene Host-Sockets braucht der Angreifer so oder so N Sockets auf seiner eigenen Gegenstelle — die Ressourcenbilanz ändert sich nicht.\n3. Unbegrenzter Public-Egress der VM ist laut Bedrohungsmodell bewusst akzeptiertes Restrisiko (Punkt 2); die FD-Kosten dieser Tunnel auf dem Host sind inhärente Folge davon, nicht Folge dieser Zeile.\n4. Der zweite behauptete Impact (\"zwei Upstreams schreiben in denselben Client-Socket\") betrifft ausschließlich die eigene Verbindung des Angreifers: `ConnState` wird pro Socket in `open` neu angelegt (egressProxy.ts:173-175), es gibt keine geteilte Struktur zwischen Verbindungen oder VMs. Ein Policy-Bypass entsteht nicht, weil jeder Kopf einzeln durch `verifyToken` (:205) und `checkTargetFn` (:128) läuft — Selbstschaden.\n\nDamit ist es ein Hygiene-/Ressourcenfehler ohne eigenständige Sicherheitswirkung; nach der DoS-Regel des Auftrags (nur real bei fehlender Session oder echter Verstärkung) kein Finding. Nicht abschließend prüfbar ohne Ausführung war nur, ob Bun einen unreferenzierten offenen TCPSocket per GC-Finalizer doch schließt — das würde den Kandidaten zusätzlich schwächen, nicht stärken."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "Ich habe gezielt nach einer Minderung und nach einem Scope-Ausschluss gesucht und keinen gefunden.\n\n1) Der zitierte Code stimmt exakt: /Users/marco/projects/macvibes/apps/server/src/http/egressProxy.ts:147-149 weist `client.data.upstream = up` bedingungslos zu (kein `?.end()` des Vorgängers), `ConnState` hält genau eine Referenz (Zeile 93-97), und der einzige Aufräumpfad ist `socket.data.upstream?.end()` in Zeile 253 bzw. 257 — er erreicht immer nur die zuletzt zugewiesene Socket.\n\n2) Der Sperr-/Guard-Zustand `state.established` wird erst im async `onOpen` gesetzt (Zeile 222 bzw. 243); bis dahin fällt jeder weitere Chunk in den Parser-Zweig ab Zeile 183 und kann eine zweite `connectChecked`-Kette starten. Es gibt keinen `pending`-Guard, kein Limit auf gleichzeitige Upstreams, keinen `socket.timeout(...)`-Aufruf (in der gesamten Datei nicht vorhanden — bun-types belegt in node_modules/bun-types/bun.d.ts:5840, dass ein Idle-Timeout auf rohen TCP-Sockets explizit gesetzt werden müsste), kein Rate-Limit (`grep` über apps/server/src/http/*.ts findet Rate-Limit nur in createAppYoga.ts:15 für GraphQL). Auch `vmTokens.revoke` (apps/server/src/sandbox/vmTokens.ts:49-54) schließt bestehende Sockets nicht.\n\n3) Es gibt sogar einen deterministischen, zeitfensterfreien Pfad zum selben Sink: Client verbindet, sendet den CONNECT-Kopf und schließt sofort. `close(socket)` (Zeile 252-254) läuft mit `upstream === null` ins Leere, danach fährt die noch laufende `Bun.connect`-Kette in Zeile 147 fort und weist die frische Upstream-Socket einem bereits toten Client zu; deren `close`/`error`-Handler (Zeile 154-160) rufen nur `client.end()` — ein No-Op. Die Host-Socket bleibt offen, solange die (vom Gast frei wählbare, per Policy nur auf 80/443 und öffentliche IPs begrenzte, egressPolicy.ts:12/143-152) Gegenstelle sie hält. Die Race-Variante des Forschers ist also die schwerere von zwei Varianten desselben Defekts an derselben Zeile.\n\n4) Scope: Der Angreifer ist der VM-Gast, der Token und Proxy-Adresse per Design in der Env hat (apps/server/src/agent/vmAgentEnv.ts:34-36) und laut Bedrohungsmodell nicht vertrauenswürdig ist; die Folge (FD-/Socket-Erschöpfung des Host-Prozesses, der alle Projekte, Previews und das Agent-Gateway bedient) trifft Host und fremde VMs, nicht nur den Verursacher — das ist kein \"angemeldeter Nutzer, der seinen eigenen Server langsam macht\". Keines der vier akzeptierten Restrisiken deckt das ab (geprüft: NAECHSTE-SESSION.md:70-80 und security-haertung-plan.md:554-571 — dort geht es um Preview-Origin, fremde Sandboxes/R10, Public-Egress-Ziele, bypassPermissions, Submodule, Audit-Log); im Gegenteil steht derselbe Defekt in NAECHSTE-SESSION.md:56 als F15 ausdrücklich noch als offen und zu prüfen.\n\nEinschränkung meiner Sicherheit: Das exakte Race-Timing des Forschers lässt sich ohne Ausführung nicht beweisen, und die reine FD-Erschöpfung erfordert viele tausend gehaltene Sockets plus kooperierende Gegenstelle — deshalb MEDIUM und die vom Forscher gewählte Einstufung LOW ist angemessen."
        }
      ]
    },
    {
      "id": "C21",
      "voters": 3,
      "refuted": 2,
      "kept": 1,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "confidence": "HIGH",
          "reasoning": "Code-Wahrheit vollständig bestätigt, kein übersehener Guard.\n\n1) Zitat/Zeile stimmen exakt: `/Users/marco/projects/macvibes/apps/server/src/agent/agentGateway.ts:165` deklariert `private onClose(ws, code?, reason?)`, und Zeile 171 lautet wortgleich `console.error(\\`Agent-Gateway: ${sandbox} getrennt (Code ${code ?? '?'}, ${reason ?? ''})\\`)`. Der im Evidence-Block mitzitierte Guard `if (this.connections.get(sandbox) !== ws) return;` (Zeile 169) bricht die Kette NICHT: er filtert nur ersetzte Alt-Verbindungen; die zuletzt in `onOpen` registrierte Verbindung des Angreifers (Zeile 138) passiert ihn und wird geloggt.\n\n2) Quelle ist wirklich gast-kontrolliert: `reason` ist die Nutzlast des Close-Frames des Clients. Der Handler ist real verdrahtet — `apps/server/src/index.ts:322` (`websocket: agentGateway.websocket`) und `:325-326` (Upgrade auf AGENT_GATEWAY_PATH); `agentGateway.ts:67` mappt `close: (ws, code, reason) => this.onClose(...)`. Bun liefert genau den Client-Reason: `node_modules/bun-types/serve.d.ts:409` `close?(ws, code: number, reason: string)` mit JSDoc \"The close reason\". Das nötige Token liegt per Design in der VM-Umgebung: `apps/server/src/index.ts:157-162` schreibt `MACVIBES_AGENT_GATEWAY_URL=...&token=<vmToken>` in die VM — Gastcode (bypassPermissions) kann damit selbst einen Gateway-Socket öffnen und mit beliebigem Reason schließen.\n\n3) Keine Minderung gefunden: Es gibt keinen console-Override/Logger-Wrapper im Server (Suche nach console-Zuweisung/pino/winston: nichts). Sanitisierung existiert im Repo nachweislich nur an anderer Stelle und nur für den HTTP-Header — `apps/server/src/http/egressProxy.ts:116` `reason.replace(/[\\r\\n]/g, ' ')` —, während dieselbe Datei den Reason eine Zeile darüber (`:114`) ebenfalls ungefiltert loggt. Das belegt, dass es keine zentrale Log-Sanitisierung gibt.\n\nEinschränkung (Ungenauigkeit im Kandidaten, ohne die Kette zu brechen): \"no length bound\" stimmt nicht ganz — RFC 6455 begrenzt Control-Frames auf 125 Byte, also ≤123 Byte Reason pro Close; das reicht aber für die im Szenario genannte gefälschte Zeile, und durch wiederholtes Connect/Close sind mehrere Zeilen möglich. Einstufung LOW (reine Log-Integrität, keine Logdatei/Log-Pipeline im Repo auffindbar) bleibt korrekt; keines der vier akzeptierten Restrisiken deckt Log-Fälschung ab."
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Zitat und Zeile stimmen: `console.error(\\`Agent-Gateway: ${sandbox} getrennt (Code ${code ?? '?'}, ${reason ?? ''})\\`)` steht tatsächlich in /Users/marco/projects/macvibes/apps/server/src/agent/agentGateway.ts:171, ohne CR/LF-Filter. Die Vorbedingungen sind auch erfüllbar: das Gateway-Token liegt im Gast lesbar unter /opt/macvibes/etc/daemon.env.sh (erzeugt in apps/server/src/sandbox/vmServices.ts:47-52 als `export MACVIBES_AGENT_GATEWAY_URL=...token=...`, ro gemountet in apps/server/src/sandbox/microsandboxProvider.ts:171), das Netz-Regelwerk erlaubt den Host-Gateway (`allow@public,allow@172.16.0.0/12`, microsandboxProvider.ts:186), und die Auth-Prüfung in agentGateway.ts:43-54 lässt eine selbstgebaute Verbindung mit genau diesem Token durch. Technisch kann VM-Code also eine Zeile in stderr des Hosts schreiben.\n\nDie Kette reißt aber beim SCHADEN, und daran scheitert der Kandidat in diesem Bedrohungsmodell:\n1. Es gibt im Repo keinen persistierten Server-Log und keinen maschinellen Log-Konsumenten. Der einzige Logfile-Pfad ist `local-router.log` eines separaten Subprozesses (apps/server/src/index.ts:90); alle Gateway-/Sandbox-Meldungen gehen ausschließlich per `console.log/error` auf eine flüchtige Entwickler-Konsole (apps/server/src/index.ts:179-370, agentGateway.ts:100/114/132/153/171). Das GraphQL-Schema exponiert keine Logs (apps/server/src/schema/index.ts). Die behauptete „Korruption einer späteren Incident-Timeline\" setzt eine Auswertungspraxis voraus, die es hier nicht gibt.\n2. Der Angreifer erreicht damit weder Host-Ressourcen noch fremde VMs noch Host-Credentials — das ist das ausdrückliche Kriterium des Bedrohungsmodells für ein echtes Finding. Er erzeugt nur kosmetischen Text über sich selbst; der Operator behandelt den Gast ohnehin als feindlich (bypassPermissions ist Produktzweck).\n3. Die Begründung „no ... length bound\" ist zusätzlich falsch: der Close-Reason ist Payload eines Control-Frames und damit per RFC 6455 §5.5 auf 125 Byte (Reason ≤ 123 Byte) begrenzt — kein Log-Flooding, maximal eine gefälschte Kurzzeile pro Verbindung.\n4. Es existiert keine projektweite Log-Sanitisierungs-Konvention, die hier verletzt würde; das einzige CR/LF-Stripping (apps/server/src/http/egressProxy.ts:116) schützt einen HTTP-Header vor Response-Splitting, nicht Logs.\n\nNicht verifizierbar ohne Ausführung: dass Bun/uWS Steuerzeichen im Close-Reason unverändert an den Handler durchreicht (Typdeklaration node_modules/bun-types/serve.d.ts:409 `close?(ws, code: number, reason: string)` legt es nahe, beweist es aber nicht) — auch das spricht gegen ein bestätigtes Finding. Ergebnis: gültige Best-Practice-Anmerkung (CWE-117), aber kein ausnutzbarer Schaden im macvibes-Bedrohungsmodell."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Der Code-Befund selbst stimmt: in /Users/marco/projects/macvibes/apps/server/src/agent/agentGateway.ts:171 wird `reason` (WS-Close-Payload des Gastes) ungefiltert in `console.error` interpoliert; eine Sanitisierung existiert dort nicht (die einzige CR/LF-Filterung im Repo ist apps/server/src/http/egressProxy.ts:116, und die schuetzt einen HTTP-Header, nicht das Log). Die Kette endet aber ohne Sicherheitsfolge:\n\n1. Kein Konsument des Logs. Der Wert wird nirgends weitergereicht: agentGateway.ts:172 sendet nur `{ kind: 'disconnected' }`, und der einzige Abnehmer (apps/server/src/agent/daemonRunner.ts:68-73) erzeugt daraus den festen String 'Verbindung zum Agent-Daemon verloren' — kein zweiter Sink, kein DB-/UI-Pfad. Im gesamten apps/server gibt es keine Logdatei, keinen Log-Parser und keine Audit-Pipeline; die launchd-Plist fuer den Produktionsbetrieb (die erst eine Logdatei erzeugen wuerde) ist in architektur.md:106 ausdruecklich als \"Offen (optional, spaeter)\" gefuehrt. Es bleibt das Terminal-Scrollback des Betreibers; keine Sicherheitsentscheidung haengt daran.\n\n2. Die Voraussetzung ist genau die bereits akzeptierte Faehigkeit. Der Angreifer braucht root-Code in der VM und das Gateway-Token aus der read-only gemounteten Env-Datei (apps/server/src/sandbox/vmServices.ts:52 schreibt `daemon.env.sh` nach /opt/macvibes/etc). Wer das hat, besitzt per Produktzweck beliebige Codeausfuehrung (Restrisiko 4) und ungefilterten Public-Egress (Restrisiko 2). Der Zugewinn ist eine gefaelschte Konsolenzeile — kein Zugriff auf Host, fremde VMs oder Host-Credentials, also nicht die Grenze, die das Bedrohungsmodell als echtes Finding definiert.\n\n3. Ein LAN-Angreifer ohne Token erreicht den Sink nicht: agentGateway.ts:43-46 antwortet vor jeder Log-Ausgabe mit 401; auch das in der Recommendation genannte Log des `claimed`-Parameters (agentGateway.ts:50-52) liegt hinter diesem Token-Gate. Ein unauthentifizierter Pfad existiert nicht.\n\nZusatz: die Behauptung \"no length bound\" ist falsch gerahmt — die Close-Reason ist durch das WebSocket-Frameformat auf 123 Byte begrenzt; Bun reicht genau diese Payload durch (node_modules/bun-types/serve.d.ts:409 `close?(ws: ServerWebSocket<T>, code: number, reason: string)`). Damit bleibt reine CWE-117-Loghygiene/Code-Qualitaet ohne belegbare Sicherheitsfolge."
        }
      ]
    },
    {
      "id": "C22",
      "voters": 3,
      "refuted": 0,
      "kept": 3,
      "survives": true,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "confidence": "HIGH",
          "reasoning": "Alle Zitate stimmen wörtlich mit dem Code: /Users/marco/projects/macvibes/apps/server/src/sandbox/microsandboxProvider.ts:177-181 enthält exakt den Kommentar \"0.0.0.0: Preview ist im LAN erreichbar (R7/NFR). Bewusst das EINZIGE Port-Mapping\" und die Zeile `0.0.0.0:${hostPort}:${context.previewPort}` als -p-Argument von `msb run` (unbedingt in jedem start()). previewGateway.ts:238 ist tatsächlich der Session-Check (`if (!(await isAuthenticated(cookieValue(...SESSION_COOKIE)))) return unauthorized();`), previewGateway.ts:271 der Upstream-fetch auf `http://127.0.0.1:${vmPort}` — das Gateway braucht also nur Loopback. config.ts:151 (`hostname: Bun.env.HOST ?? '0.0.0.0'`), templates/templates.json:8,15 (previewPort 5173) und PortAllocator.allocate (sandbox/portService.ts:41-48, bevorzugt den Wunschport) belegen die Vorbedingungen; microsandbox ist im Realbetrieb Pflicht (sandbox/backendSelection.ts:45-54: ohne msb wirft der Start UnsafeBackendError außer beim Fake-Agent).\n\nAktive Suche nach einem übersehenen Guard blieb ergebnislos: kein Firewall-/pf-Setup im Repo, keine Loopback-Bindung an anderer Stelle (grep \"0.0.0.0\" über *.ts/*.sh/*.md), und der direkte Zugriff auf den gemappten Host-Port läuft komplett an previewGateway.ts:238 vorbei. Die LAN-Bindung ist funktional nicht mehr nötig: die iframe-URL zeigt aufs Gateway (apps/web/src/models/PreviewModel.ts, `http://${host}:${gatewayPort}/p/${projectId}/`, verdrahtet in apps/web/src/pages/ChatPage.tsx:213 via previewGatewayPort); previewHostPort wird im Web-Client außerhalb der GraphQL-Feldliste nicht verwendet, und CLAUDE.md:39-47 sagt, für Remote werde nur 4173 geforwardet.\n\nVom bewusst akzeptierten Restrisiko ist das nicht gedeckt: security-haertung-plan.md:554-571 und NAECHSTE-SESSION.md:70-79 nennen nur geteilte Origin auf 4173, Public-Egress, Warmup und bypassPermissions — nicht die direkte 0.0.0.0-Veröffentlichung der VM-Dev-Server.\n\nEinzige im Repo nicht verifizierbare Kette: das reale Bind-Verhalten der externen msb-CLI (hier nicht ausführbar). Die repo-eigenen Belege stützen es aber: REQUIREMENTS.md:184-193 (das iframe zeigte früher direkt auf den gemappten Port mit dem Aufrufer-Hostnamen, \"LAN-tauglich\") und docs/adr/0001-preview-status-ueber-daemon-websocket.md:26,54 (\"das 0.0.0.0-Mapping braucht es für den LAN-Zugriff ohnehin\") beschreiben genau diese LAN-Erreichbarkeit als funktionierenden Ist-Zustand."
        },
        {
          "lens": "exploitability",
          "refuted": false,
          "confidence": "HIGH",
          "reasoning": "Kette vollständig nachvollzogen, Rolle des Angreifers: ANONYM im LAN (laut Bedrohungsmodell in-scope).\n\n1) Sink existiert wie zitiert: `/Users/marco/projects/macvibes/apps/server/src/sandbox/microsandboxProvider.ts:180-181` übergibt `-p 0.0.0.0:${hostPort}:${context.previewPort}` an `msb run`. Der Kommentar :177-179 behauptet die LAN-Erreichbarkeit selbst (\"Preview ist im LAN erreichbar (R7/NFR)\"), ebenso `docs/adr/0001-preview-status-ueber-daemon-websocket.md:54` (\"das 0.0.0.0-Mapping braucht es für den LAN-Zugriff ohnehin\").\n\n2) Gast-seitig lauscht der Dev-Server tatsächlich auf allen Interfaces, das Mapping trifft also etwas: `templates/pwa/server.ts:10` (`hostname: '0.0.0.0'`) und `templates/fullstack/apps/web/vite.config.ts` (`host: true`).\n\n3) Kein Auth auf diesem Pfad. Die einzige Session-Prüfung liegt im Gateway auf dem separaten Port 4173: `apps/server/src/http/previewGateway.ts:236-239` (`isAuthenticated(...)` → `unauthorized()`), und das Gateway spricht die VM ausschließlich über Loopback an (`previewGateway.ts:271` `http://127.0.0.1:${vmPort}...`). Auch die Statusprobe nutzt nur Loopback (`microsandboxProvider.ts:215` `http://localhost:${hostPort}/`). Die 0.0.0.0-Bindung erfüllt im heutigen Produkt keine Funktion mehr: die iframe-URL zeigt aufs Gateway (`apps/web/src/models/PreviewModel.ts:59`), `previewHostPort` wird zwar per GraphQL exponiert (`apps/server/src/schema/index.ts:74-76`) und abgefragt, aber in keiner Web-Komponente verwendet.\n\n4) Vorbedingungen erfüllbar: microsandbox ist der Default sobald `msb` da ist (`apps/server/src/sandbox/backendSelection.ts:45-46`, Config-Default `'auto'` in `apps/server/src/config.ts:163-166`); ohne msb verweigert der Start mit echtem Agenten sogar (`backendSelection.ts:60-65`) — Produktivbetrieb heißt also microsandbox. Der Port ist vorhersagbar: `PortAllocator.allocate` bevorzugt den Wunsch-Port (`apps/server/src/sandbox/portService.ts:40-46`), und beide Templates haben `previewPort: 5173` (`templates/templates.json:8,15`); in Produktion (`bun run start`, Web auf 4000) ist 5173 frei, die erste VM bindet also 0.0.0.0:5173.\n\n5) Nicht vom akzeptierten Restrisiko gedeckt: Punkt 1 der Liste betrifft die geteilte Origin *auf 4173* hinter dem authentifizierten Gateway, Punkt 2 den Egress der VM. Hier geht es um unauthentifizierten Ingress komplett am Gateway vorbei — genau das Gegenteil des in diesem Branch bewusst eingeführten Controls F19 (`apps/server/src/index.ts:220`, `previewGateway.ts:152`) und von R10 (\"Ohne gültige Session wird jede Seite auf die Login-Seite umgeleitet\", REQUIREMENTS.md:254).\n\n6) Kein Selbstschaden: ein Nicht-angemeldeter liest die laufende App und den transformierten Quellcode *fremder* Projekte.\n\nEinzige nicht in Code verifizierbare Stelle: das tatsächliche Socket-Binding von `msb` (kein Ausführen erlaubt). Das wird aber durch die Repo-Historie gestützt — vor dem Gateway zeigte das iframe Browser anderer LAN-Geräte direkt auf `http://${host}:${port}/` dieses Mappings (`git show e92ff48:apps/web/src/models/PreviewModel.ts`, `start(host, port)`), das Feature hat also nachweislich über das Netz funktioniert."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": false,
          "confidence": "HIGH",
          "reasoning": "Linse 3 (Geltungsbereich/Minderung) findet keine Entlastung — der Kandidat hält.\n\n1) Der zitierte Code stimmt exakt: `apps/server/src/sandbox/microsandboxProvider.ts:181` startet jede VM mit `-p 0.0.0.0:${hostPort}:${context.previewPort}` (Kommentar :177-179). Microsandbox ist der Produktions-Default (`apps/server/src/sandbox/backendSelection.ts:46`: `auto` + vorhandene msb-CLI → `microsandbox`).\n\n2) Die zu umgehende Kontrolle existiert und ist echt: `apps/server/src/http/previewGateway.ts:238` verweigert ohne Session-Cookie (401, `unauthorized()`), verdrahtet in `apps/server/src/index.ts:222-224` („Previews sind nur für angemeldete Nutzer sichtbar (F19)\"), abgesichert durch `apps/server/src/http/__tests__/previewGateway.test.ts:203` („ohne Session: 401, und die VM wird nicht kontaktiert\"). Der direkte Host-Port kennt diese Prüfung nicht.\n\n3) Kein akzeptiertes Restrisiko: Die vier Punkte in `NAECHSTE-SESSION.md:70-79` betreffen die geteilte Preview-Origin auf 4173, Egress, Warmup-Abbruch und bypassPermissions — keiner deckt ein zusätzliches, ungeschütztes Port-Mapping ab (hier hat sogar jede VM eine eigene Origin, also nicht Punkt 1). R10 (`REQUIREMENTS.md:273-278`) gewährt Lesezugriff auf fremde Previews ausdrücklich *angemeldeten* Nutzern (Registrierung sogar erst nach Admin-Approval, `REQUIREMENTS.md:190-194`) — der Bypass wirkt völlig unauthentifiziert.\n\n4) Keine Minderung gefunden: Kein Firewall-/pf-Setup in `scripts/` oder README; Caddy terminiert laut `README.md:100-106` nur vor 4000/4173 und entfernt das rohe 0.0.0.0-Binding nicht. Der Hinweis in `README.md:77` („nur 4173 forwarden\") gilt für Remote/VPN — im LAN, das das Bedrohungsmodell ausdrücklich nennt, braucht es kein Forwarding.\n\n5) Die LAN-Bindung ist funktional nicht mehr nötig, die Empfehlung also tragfähig: Das iframe zeigt heute ausschließlich auf das Gateway (`apps/web/src/models/PreviewModel.ts:16-19` und :92: `http://${host}:${gatewayPort}/p/<id>/`), `previewHostPort` wird im Web nur noch abgefragt, nie zu einer URL verbaut (`apps/web/src/models/ProjectsStore.ts:28,68`). Host-seitig laufen alle Zugriffe über Loopback (`previewGateway.ts:271` `http://127.0.0.1:${vmPort}`, `microsandboxProvider.ts:216` `httpProbe('http://localhost:${hostPort}/')`). Dass das Mapping tatsächlich LAN-erreichbar ist, belegt die frühere Produktivnutzung: Commit e92ff48 baute die Preview-URL im Client aus eigenem Hostnamen + `previewHostPort` (R7, `REQUIREMENTS.md:191-195`), plus `docs/adr/0001-...:54`.\n\n6) Der Vorbericht bestätigt die Lücke statt sie abzuhaken: `CLAUDE-SECURITY-20260725-095652/CLAUDE-SECURITY-RESULTS.md:371` refutete das 0.0.0.0-Mapping seinerzeit nur, weil das Gateway damals selbst unauthentifiziert war, und warnte ausdrücklich: „fixing the gateway without also binding that mapping to 127.0.0.1 would leave the bypass intact\". Der Härtungsplan AP3 (`security-haertung-plan.md`, Abschnitt „Wie es geschlossen wird\") führte das Binding nicht nach — genau dieser Restzustand liegt heute vor.\n\nNicht ausführbar prüfbar (kein Code-Lauf erlaubt): das konkrete Bind-Verhalten der msb-CLI. Alle Repo-internen Belege (Flag-Wert, Kommentar :177, ADR, historische LAN-iframe-URL) sprechen einheitlich für ein Host-weites Binding."
        }
      ]
    },
    {
      "id": "C23",
      "voters": 3,
      "refuted": 3,
      "kept": 0,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Code-Wahrheit: Die Zitate stimmen wörtlich. `/Users/marco/projects/macvibes/apps/server/src/http/previewGateway.ts:271` enthält exakt `upstream = await fetch(\\`http://127.0.0.1:${vmPort}${target.forwardPath}${url.search}\\`, {`, `resolveTarget(...)` steht bei :241, `previewPortFor(target.projectId)` bei :247, der Set-Cookie-Namensfilter bei :140. Auch die Prämisse „echte Origin\" stimmt: `/Users/marco/projects/macvibes/apps/web/src/pages/ChatPage.tsx:539` setzt bewusst `sandbox=\"allow-scripts allow-same-origin …\"`, und `/Users/marco/projects/macvibes/apps/server/src/config.ts:172` fixiert den einen Gateway-Port (Default 4173). Der Kandidat ist also nicht halluziniert.\n\nTrotzdem kein Finding: Er beschreibt wortgleich das dokumentierte, bewusst akzeptierte Restrisiko Nr. 1 des Bedrohungsmodells. `/Users/marco/projects/macvibes/security-haertung-plan.md:558-561` („Das Preview-Gateway bleibt eine geteilte Origin (`host:4173`). Cookies und `localStorage` sind zwischen Previews nicht trennbar. Echter Fix wären Wildcard-Hostnamen pro Projekt plus Wildcard-TLS — eigenes Vorhaben.\") und `/Users/marco/projects/macvibes/NAECHSTE-SESSION.md:72-73` benennen es explizit als „nicht als Finding melden\". Die Empfehlung des Kandidaten („per-project hostname … with a wildcard cert\") ist genau dieses vertagte Vorhaben. Der Teilimpact „read another user's dev server output/source\" fällt zusätzlich unter R10 (fremde Projekte bewusst lesbar, fremde Sandboxes bewusst startbar, NAECHSTE-SESSION.md:64-67).\n\nZwei übersehene Guards schwächen die Kausalkette zusätzlich: (a) `sanitizeUpstreamHeaders` (previewGateway.ts:104-118, HOST_COOKIES bei :89) entfernt `macvibes_session` und `mvp` vor dem Upstream-Fetch — die fremde VM erhält den Session-Token des Betrachters gerade NICHT, entgegen der Formulierung „with the viewer's ambient session\"; die Session dient nur der Gateway-Authentifizierung bei :238-240 (Auth vor jeder Auflösung, inkl. WS-Upgrade). (b) `sanitizeDownstreamHeaders` (:127-142) verhindert, dass die VM Routing-/Session-Cookies setzt. Der im Impact-Feld eingestreute `document.cookie`-Shadowing-Pfad ist ein anderer, separat geführter Kandidat (CANDIDATES-RAW.json:273 bzw. :563) und trägt diesen Kandidaten, wie geschrieben, nicht."
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Die Kette ist technisch korrekt beschrieben, trifft aber exakt das dokumentierte, bewusst akzeptierte Restrisiko und ein explizites Requirement — daher kein Finding.\n\nVerifikation der Kette (Code stimmt): `previewGateway.ts:238` prüft nur die Session (`isAuthenticated`), `previewGateway.ts:241-245` löst das Ziel aus Pfad/`Referer`/`mvp`-Cookie auf, `previewGateway.ts:247` holt den VM-Port und `previewGateway.ts:271` proxied. Ownership wird nirgends geprüft — das ist ausdrücklich so gebaut: `apps/server/src/index.ts:220-224` („Ownership wird bewusst NICHT geprüft — fremde Projekte dürfen live betrachtet werden (R10)\"), gedeckt von `REQUIREMENTS.md:271-273` („Fremde Projekte sind lesend zugänglich: Projektliste, Chat-Verlauf und Preview dürfen betrachtet werden — live\"). Das Lesen fremder Preview-Inhalte ist also gewolltes Verhalten, kein Autorisierungsfehler.\n\nDer eigentliche Kern (eine geteilte Origin für alle Previews, inkl. Service-Worker mit Scope `/`) ist wörtlich das akzeptierte Restrisiko: `security-haertung-plan.md:558-561` — „Das Preview-Gateway bleibt eine geteilte Origin (`host:4173`). Cookies und `localStorage` sind zwischen Previews nicht trennbar. Echter Fix wären Wildcard-Hostnamen pro Projekt plus Wildcard-TLS — eigenes Vorhaben.\" Die Empfehlung des Kandidaten („per-project hostname … with a wildcard cert\") ist genau dieses vertagte Vorhaben.\n\nEskalation über die Preview-Origin hinaus reißt zusätzlich ab: die GraphQL-API lehnt Requests mit Origin `http://<host>:4173` ab, weil `allowedOrigins` (`originPolicy.ts:34-49`) nur `http(s)://<Host-Header>` (= App-Port) plus konfigurierte/Dev-Origins enthält und `isCrossSiteRequest` (`originPolicy.ts:74-76`) alles andere als cross-site verwirft. Das Session-Cookie ist httpOnly (`cookies.ts:writeSessionCookie`, `httpOnly: true`), also nicht auslesbar; für das behauptete Path-Shadowing bräuchte der Angreifer bereits ein gültiges fremdes Session-Token, dessen Beschaffung aus der VM (Egress blockt LAN/Loopback) nicht gezeigt und nicht plausibel ist. Das iframe unterbindet zudem Top-Navigation/Phishing (`apps/web/src/pages/ChatPage.tsx:539`, kein `allow-top-navigation`).\n\nDamit bleibt als realer Effekt genau das, was R10 erlaubt (fremde Preview lesen) plus die bewusst in Kauf genommene fehlende Origin-Trennung."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Der Kandidat beschreibt exakt das bewusst akzeptierte Restrisiko Nr. 1. Der Code stimmt zwar (/Users/marco/projects/macvibes/apps/server/src/http/previewGateway.ts:241-248 löst das Ziel über Pfad `/p/<id>/`, Referer oder `mvp`-Cookie auf, :271 proxyt auf `http://127.0.0.1:${vmPort}`, alle Previews laufen unter der einen Origin `http://<host>:4173`, dokumentiert in /Users/marco/projects/macvibes/CLAUDE.md:40-47), aber genau das ist im Repo als Nicht-Ziel festgehalten: /Users/marco/projects/macvibes/security-haertung-plan.md:558-561 (\"Das Preview-Gateway bleibt eine geteilte Origin (host:4173). Cookies und localStorage sind zwischen Previews nicht trennbar. Echter Fix wären Wildcard-Hostnamen pro Projekt plus Wildcard-TLS — eigenes Vorhaben.\") und /Users/marco/projects/macvibes/NAECHSTE-SESSION.md:72-73. Die Empfehlung des Forschers (\"Give each project its own origin ... wildcard cert\") ist wörtlich dieses vertagte Vorhaben. Der zweite Teil des Impacts (Lesen fremder Projektquellen über `/p/<victimId>/...`) fällt zusätzlich unter R10: das Gateway authentifiziert bewusst nur (previewGateway.ts:238, Kommentar :152-155 \"ohne Prüfung erreicht jeder mit einer Projekt-ID jede laufende Preview\") und autorisiert absichtlich nicht pro Projekt. Vorhandene Minderungen habe ich gelesen: Session-/Auth-Header gelangen nie in die VM (sanitizeUpstreamHeaders previewGateway.ts:105-122 mit HOST_COOKIES :93), die VM darf `macvibes_session`/`mvp` nicht per Set-Cookie setzen (:130-144) und keine site-weiten Direktiven diktieren (STRIP_FROM_UPSTREAM :96), und das iframe läuft mit `sandbox=\"allow-scripts allow-same-origin allow-forms allow-modals allow-popups allow-downloads\"` ohne allow-top-navigation (/Users/marco/projects/macvibes/apps/web/src/pages/ChatPage.tsx:534-539) — genau die im Plan genannte Restminderung. Der Zusatzvektor \"document.cookie-Shadowing des Session-Cookies\" ist eine Folge derselben akzeptierten Origin-/Port-Teilung (Cookies sind nicht portgebunden) und kein separat vollständig belegter Pfad; das echte httpOnly-Session-Cookie mit gleichem Pfad kann JS nicht überschreiben. Damit: kein zu meldendes Finding.",
          "correctedSeverity": "NONE"
        }
      ]
    },
    {
      "id": "C24",
      "voters": 3,
      "refuted": 0,
      "kept": 3,
      "survives": true,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "confidence": "HIGH",
          "correctedSeverity": "LOW",
          "reasoning": "Linse 1 (Code-Wahrheit): Jedes Zitat des Kandidaten stimmt wörtlich mit dem Repo-Stand (HEAD 34939c69, Working Tree sauber bis auf den Scan-Ordner) überein, und ich habe keinen übersehenen Guard gefunden.\n\n1) Sink existiert wörtlich: /Users/marco/projects/macvibes/apps/server/src/http/previewGateway.ts:40 — `if (part.slice(0, eq).trim() === name) return decodeURIComponent(part.slice(eq + 1).trim());`. Kein try/catch in `cookieValue` (Z. 35-43). Die beiden weiteren ungeschützten Decodes stehen tatsächlich in Z. 32 (`idFromPPath`) und Z. 59 (`resolveTarget`) — die liegen allerdings HINTER der Auth (Aufruf erst in Z. 241), sind also nur der sekundäre Teil.\n\n2) Quelle ist wirklich pre-auth und attacker-controlled: previewGateway.ts:238 — `if (!(await isAuthenticated(cookieValue(req.headers.get('cookie'), SESSION_COOKIE))))`. Das Argument wird VOR dem Auth-Aufruf ausgewertet, der Wurf passiert also vor jeder Prüfung. `SESSION_COOKIE = 'macvibes_session'` (apps/server/src/http/cookies.ts:1), d. h. `Cookie: macvibes_session=%` trifft exakt den Namensvergleich in Z. 40 und `decodeURIComponent('%')` wirft URIError. Der Listener ist unauthentifiziert im LAN erreichbar: previewGateway.ts:232 `hostname: options.hostname ?? '0.0.0.0'`, index.ts:216-224 übergibt `config.hostname` / `config.sandbox.previewGatewayPort`, Defaults `'0.0.0.0'` (config.ts:151) und `4173` (config.ts:172).\n\n3) Keine Minderung gefunden — aktiv gesucht: Der `Bun.serve<WsProxyData>`-Block (previewGateway.ts:230-318) hat KEINEN `error`-Handler; im `fetch` (Z. 233-291) gibt es nur ein try/catch um den Upstream-`fetch` (Z. 270-281), nicht um Z. 238. Ein globaler Fallback existiert nicht: `grep process.on(` liefert nur SIGINT/SIGTERM (index.ts:95, 380, 381), kein `uncaughtException`/`unhandledRejection`. Auch kein Wrapper beim einzigen Produktiv-Aufruf (index.ts:216).\n\n4) Impact-Detail bestätigt aus den im Repo gepinnten bun-types (1.3.14): node_modules/bun-types/serve.d.ts:680-684 dokumentiert `development` mit `@default process.env.NODE_ENV !== 'production'`, und serve.d.ts:1110-1116: \"In development mode, `Bun.serve()` returns rendered error messages with stack traces instead of a generic 500 error ... you will risk leaking sensitive information.\" `NODE_ENV` wird nirgends gesetzt (grep über apps/, package.json ergibt null Treffer); Produktionsstart ist `bun run build:web && cd apps/server && bun run start` → `bun run src/index.ts` (package.json root + apps/server/package.json). Damit läuft der Gateway-Listener default mit development=true.\n\nEinschränkung (ehrlich benannt): Ich konnte nichts ausführen, also nicht empirisch verifizieren, wie die Bun-Fehlerseite konkret aussieht (\"Code-Snippet\" im Kandidaten ist gegenüber der dokumentierten \"stack trace\" leicht überzogen). Ein Server-Crash entsteht nicht — Bun fängt die Rejection pro Request ab. Der reale Effekt ist also: unauthentifizierter Netzwerk-Client erzwingt 500 statt 401 samt Stacktrace mit absoluten Host-Pfaden. Das ist keine der vier akzeptierten Restrisiken und kein Session-gebundener Selbst-DoS. LOW ist die zutreffende Einstufung."
        },
        {
          "lens": "exploitability",
          "refuted": false,
          "confidence": "HIGH",
          "correctedSeverity": "LOW",
          "reasoning": "Die Kette ist vollständig und ohne Vorbedingungen auslösbar — Rolle: ANONYM, ein einziger HTTP-Request.\n\n1) Quelle/Erreichbarkeit: `/Users/marco/projects/macvibes/apps/server/src/config.ts:151` setzt `hostname: Bun.env.HOST ?? '0.0.0.0'`, `config.ts:172` den Gateway-Port `4173`; `apps/server/src/index.ts` startet `startPreviewGateway({ port: config.sandbox.previewGatewayPort, hostname: config.hostname, ... })` unbedingt (unabhängig vom Sandbox-Backend). `previewGateway.ts:230-232` bindet entsprechend `0.0.0.0`. Laut CLAUDE.md ist genau dieser Port der bewusst geforwardete. Der Angreifer braucht keine Session.\n\n2) Senke vor der Auth: `previewGateway.ts:238` ruft `cookieValue(req.headers.get('cookie'), SESSION_COOKIE)` als Argument von `isAuthenticated(...)` auf — das Argument wird ausgewertet, BEVOR irgendeine Prüfung greift. `previewGateway.ts:40` ist exakt wie zitiert: `return decodeURIComponent(part.slice(eq + 1).trim());` ohne try/catch. `SESSION_COOKIE = 'macvibes_session'` (`apps/server/src/http/cookies.ts:1`), der Name ist also trivial bekannt. `Cookie: macvibes_session=%` matcht den Namensvergleich und lässt `decodeURIComponent('%')` einen URIError werfen. `%` ist ein legaler Header-Byte, wird vom HTTP-Parser nicht abgewiesen.\n\n3) Keine Minderung dazwischen: Ich habe die gesamte Datei gelesen — der `fetch`-Handler (`previewGateway.ts:233-291`) hat keinen umschließenden try/catch (nur der Upstream-`fetch` bei :270-281 ist gekapselt), und dem `Bun.serve`-Aufruf (:230-318) fehlt jeder `error`-Handler. Die async-Funktion rejected also nach außen. Vorherige Härtungs-Commits (17ec0a1, f198a6e) haben das nicht angefasst; ein Test dafür existiert nicht (`apps/server/src/http/__tests__/previewGateway.test.ts` prüft nur wohlgeformtes `%20`).\n\n4) Folge: Statt der 401 (`unauthorized()`, :190) antwortet Bun mit seiner Default-Fehlerbehandlung. `development` ist nirgends gesetzt und default laut mitgeliefertem `node_modules/bun-types/serve.d.ts:681-684` auf `process.env.NODE_ENV !== 'production'`; `NODE_ENV=production` wird im gesamten Repo nirgends gesetzt (grep: nur `templates/pwa/build.ts:9` für das Client-Bundle), `apps/server/package.json` startet mit blankem `bun run src/index.ts`. `serve.d.ts:1110-1117` dokumentiert für diesen Modus explizit „returns rendered error messages with stack traces … you will risk leaking sensitive information\".\n\nKein Punkt der akzeptierten Restrisiken (geteilte Preview-Origin, VM-Egress, Warmup, bypassPermissions) deckt das ab, und es ist kein Selbstschaden eines angemeldeten Nutzers.\n\nEinschränkung, die ich offenlege: den genauen Inhalt der Bun-Fehlerseite konnte ich nicht beobachten (kein Ausführen erlaubt) — belegt ist er nur über die Vendor-Doku im Repo. Das Entkommen der Exception selbst und die daraus folgende 500-statt-401-Antwort sind rein aus dem Code sicher. Severity LOW bleibt korrekt: kein Auth-Bypass (die VM wird nicht kontaktiert, der Wurf passiert vor `resolveTarget`/`previewPortFor`), kein Prozess-Crash, nur Fehlerbehandlungs-/Informationsleck."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "Kette im echten Code vollständig nachvollzogen, Minderung gezielt gesucht und KEINE gefunden.\n\n1) Quelle/Sink: `/Users/marco/projects/macvibes/apps/server/src/http/previewGateway.ts:40` enthält exakt das zitierte `decodeURIComponent(part.slice(eq + 1).trim())` ohne try/catch. Aufgerufen wird `cookieValue` mit dem ROHEN Client-Header in `previewGateway.ts:238` — `if (!(await isAuthenticated(cookieValue(req.headers.get('cookie'), SESSION_COOKIE))))`. Der Decode passiert bei der Argument-Auswertung, also VOR jeder Auth-Entscheidung; `SESSION_COOKIE = 'macvibes_session'` (`apps/server/src/http/cookies.ts:1`). `Cookie: macvibes_session=%` löst dort einen URIError aus.\n\n2) Erreichbarkeit: Das Gateway lauscht per Default auf `0.0.0.0` (`previewGateway.ts:232`), Start in `apps/server/src/index.ts:216-225` mit `config.hostname`, und CLAUDE.md dokumentiert Port 4173 ausdrücklich als DEN einen für Remote/VPN geforwardeten Port. Kein Vorschalt-Guard: Zeile 238 ist die erste Anweisung nach `new URL(req.url)`.\n\n3) Gesuchte Minderungen — nicht vorhanden: Der `Bun.serve<WsProxyData>({...})`-Block `previewGateway.ts:230-318` hat KEINEN `error`-Handler (nur `port`, `hostname`, `fetch`, `websocket`). Es gibt repoweit keinen safe-decode-Wrapper (`grep decodeURIComponent` liefert nur previewGateway.ts:32/40/59; `URIError|safeDecode` = 0 Treffer). Kein Zod-Schema, keine Middleware, kein Auth-Wrapper vor Zeile 238. Caddy (`:8443 → 4173`) reicht Cookies nur durch und der Klartextport 4173 bleibt offen.\n\n4) Bun-Default bestätigt aus den gepinnten Typen/Docs: `node_modules/bun-types/serve.d.ts:681-684` (`development` @default `process.env.NODE_ENV !== 'production'`) und `serve.d.ts:1110-1117` (\"returns rendered error messages with stack traces instead of a generic 500 error ... you will risk leaking sensitive information\"), dazu `node_modules/bun-types/docs/runtime/http/error-handling.mdx` (\"built-in error page\"). NODE_ENV=production wird nirgends gesetzt: `package.json` root `\"start\": \"bun run build:web && cd apps/server && bun run start\"` und `apps/server/package.json` `\"start\": \"bun run src/index.ts\"`; der einzige NODE_ENV-Treffer im Repo ist `templates/pwa/build.ts:9` (irrelevant).\n\nEinordnung: fällt unter KEINES der vier akzeptierten Restrisiken (geteilte Preview-Origin, VM-Egress, Warmup-Abbruch, bypassPermissions) und nicht unter R10 — es geht nicht um Zugriff auf fremde Projekte, sondern darum, dass ein UNAUTHENTIFIZIERTER Client die 401-Antwort (`previewGateway.ts:190-195`) in Buns Dev-Fehlerseite mit Stacktrace/Host-Pfaden verwandelt. Auch kein DoS-Artefakt: der Prozess stürzt nicht ab, es ist Pre-Auth-Informationspreisgabe (CWE-209/248), LOW — die Einstufung des Forschers passt. Was ich nicht verifizieren konnte: den exakten Inhalt der Bun-Fehlerseite (kein Ausführen erlaubt); ich stütze mich auf die Doku der gepinnten Bun-Version, daher MEDIUM statt HIGH. Kleine Ungenauigkeit im Kandidaten: die Decodes in `resolveTarget` (Zeilen 32/59) liegen NACH dem Auth-Gate (Zeile 241) und sind damit nur für angemeldete Nutzer erreichbar; der Kernbefund bei Zeile 40/238 bleibt davon unberührt."
        }
      ]
    },
    {
      "id": "C25",
      "voters": 3,
      "refuted": 1,
      "kept": 2,
      "survives": true,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "confidence": "HIGH",
          "reasoning": "Code-Wahrheit bestätigt, Zitat ist wörtlich echt (Arbeitsbaum sauber, identisch zu `git show HEAD:`).\n\n1) Quelle: `/Users/marco/projects/macvibes/apps/server/src/agent/agentGateway.ts:42` `const claimed = url.searchParams.get('sandbox');` — WHATWG-`URLSearchParams` liefert percent-dekodiert, `%0A` überlebt also als echtes LF. Erreichbar über den öffentlichen Listener: `/Users/marco/projects/macvibes/apps/server/src/index.ts:325-326` (`if (url.pathname === AGENT_GATEWAY_PATH) return agentGateway.handleUpgrade(...)`), `AGENT_GATEWAY_PATH = '/agent'` (`apps/server/src/agent/daemon/protocol.ts:12`), Server auf `config.port`/`config.hostname` (index.ts:313-315).\n\n2) Senke: `agentGateway.ts:50-52` — `console.warn(\\`AgentGateway: Token von ${identity.sandbox} beansprucht Sandbox ${claimed} — abgelehnt.\\`)`. Zeilennummer 51 des Kandidaten stimmt exakt; Template-Literal, keine Formatierung/Escaping durch Bun.\n\n3) Guards aktiv gesucht, keiner bricht die Kette: Der einzige Guard davor ist `identity === null → 401` (`agentGateway.ts:44-46`). Das erfordert ein gültiges Per-Sandbox-Token — genau das steckt per Design in der untrusted VM: `apps/server/src/index.ts:157-162` schreibt `MACVIBES_AGENT_GATEWAY_URL=ws://host.microsandbox.internal:<port>/agent?sandbox=…&token=<vmToken>` in die VM-Env (Token aus `vmTokens.mint`, `apps/server/src/sandbox/vmTokens.ts:38-46`). Der Angreifer in der VM ist laut Bedrohungsmodell untrusted und hält das Token also legitim. Die zweite Bedingung `claimed !== identity.sandbox` (Zeile 49) ist bei injiziertem Wert automatisch erfüllt. Kein globaler Console-/Logger-Wrapper vorhanden (grep nach `globalThis.console`/`patchConsole`/`sanitizeLog` in `apps/server/src`: kein Treffer). Der einzige CRLF-Filter im Repo, `apps/server/src/http/egressProxy.ts:116`, schützt nur den HTTP-Header-Write — die dortige `console.warn` (Zeile 114) bekommt den Rohwert, es gibt also gerade keine Log-Sanitisierungs-Konvention, die hier greifen könnte.\n\nVollständige Fälschung einer Zeile ist möglich, weil der Angreifer nach einem zweiten `%0A` die feste Endung „ — abgelehnt.“ in eine Restzeile abschieben kann. Impact bleibt reine Log-Fälschung auf dem Host-Konsolenlog — die vom Kandidaten gewählte Einstufung LOW (CWE-117) ist korrekt und nicht überzeichnet; kein Punkt der vier akzeptierten Restrisiken trifft zu, und es ist kein DoS-Fall."
        },
        {
          "lens": "exploitability",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "Die Kette hält unter der Ausnutzbarkeits-Linse vollständig; ich konnte sie an keiner Stelle brechen.\n\n1) Quelle/Rolle: Angreifer ist Code in der MicroVM — laut Bedrohungsmodell die per Design untrusted Partei, also keine unrealistische Vorbedingung. Das nötige Token liegt für ihn frei lesbar in der VM: `apps/server/src/sandbox/vmServices.ts` schreibt `daemon.env.sh` mit `export MACVIBES_AGENT_GATEWAY_URL='ws://host.microsandbox.internal:<port>/agent?sandbox=…&token=…'`, und `apps/server/src/sandbox/microsandboxProvider.ts:172` mountet dieses Verzeichnis als `${etcDir}:/opt/macvibes/etc:ro` in die VM. `ro` heißt lesbar; der Agent läuft dort mit bypassPermissions. Der Kommentar in vmServices.ts (\"Proxy-Token darf NICHT an den devCommand leaken\") schützt nur den devCommand, nicht den Agenten.\n\n2) Erreichbarkeit: `apps/server/src/index.ts:160-162` baut genau diese URL, d.h. VM→Host:4000 ist per Netzpolicy offen. `apps/server/src/index.ts:325` routet `/agent` an `agentGateway.handleUpgrade`. Ein simples `curl` aus der VM genügt — der Log-Aufruf liegt VOR `server.upgrade`, ein echter WS-Handshake ist nicht nötig.\n\n3) Senke: `apps/server/src/agent/agentGateway.ts:42` liest `url.searchParams.get('sandbox')` (WHATWG-URL dekodiert `%0A`/`%0D`/`%1B`), :43-46 lässt das gültige Eigen-Token passieren, :49 trifft bei abweichendem Namen zu, :50-52 interpoliert `claimed` roh in `console.warn`. Keine Sanitisierung, kein Logger-Wrapper, keine Kürzung — die einzige CRLF-Filterung im Repo steht in `apps/server/src/http/egressProxy.ts:117` (`reason.replace(/[\\r\\n]/g, ' ')`) und gilt nur dem HTTP-Header, nicht dem daneben stehenden `console.warn` in Zeile 114.\n\n4) Schaden ist kein Selbstschaden: Der Gast fälscht Zeilen im Operator-Log des Hosts. Ausgerechnet diese Zeile ist laut Kommentar :47-48 der einzige Hinweis auf einen Cross-Sandbox-Impersonations-Versuch; sie lässt sich damit gezielt zumüllen oder plausibel umlügen.\n\nEinschränkung, die die Einstufung LOW rechtfertigt (nicht höher): Es gibt im Repo keine Log-Datei, keinen Scraper und noch kein launchd-Plist (architektur.md:106 führt es als Vorhaben), die Ausgabe landet im Terminal des Operators. Kein Code-Exec, keine Credentials, kein Cross-VM-Zugriff. Anonyme LAN-Angreifer scheitern an :44-46 (401 vor dem Log) — die Precondition \"gültiges VM-Token\" ist korrekt angegeben. Die LOW-Einstufung des Forschers ist damit zutreffend, ebenso der Zusatzhinweis auf die ebenfalls gast-kontrollierte Close-Reason in :171."
        },
        {
          "lens": "scope-and-mitigation",
          "reasoning": "Quelle und Senke sind technisch korrekt beschrieben, aber die Sicherheitsfolge fehlt.\n\nVerifiziert: `/Users/marco/projects/macvibes/apps/server/src/agent/agentGateway.ts:42` liest `url.searchParams.get('sandbox')` roh; `:44-46` verlangt vorher ein gültiges VM-Token, `:50-52` loggt `claimed` unmaskiert per `console.warn`. Die VM besitzt das Token tatsächlich (`/Users/marco/projects/macvibes/apps/server/src/index.ts:160-162` setzt `MACVIBES_AGENT_GATEWAY_URL` inkl. Token in die Gast-Env), Routing ohne weitere Auth über `/Users/marco/projects/macvibes/apps/server/src/index.ts:325-327`. Eine Sanitisierung existiert an dieser Stelle nicht (die einzige CRLF-Filterung im Repo ist header-, nicht log-bezogen: `/Users/marco/projects/macvibes/apps/server/src/http/egressProxy.ts:116`).\n\nEntscheidend ist der Geltungsbereich der Senke: Das Ziel ist reine, flüchtige Terminal-Ausgabe des Vordergrundprozesses (`/Users/marco/projects/macvibes/package.json` \"start\" → `apps/server/package.json` \"start\": `bun run src/index.ts`). Im gesamten Repo gibt es keine Logdatei, keine Rotation, keinen Parser und kein Monitoring: kein `appendFile`/`createWriteStream`/`tee` für Serverlogs, und die launchd-Plist für den Produktionsbetrieb ist ausdrücklich noch offen (`/Users/marco/projects/macvibes/architektur.md:106`: \"Offen (optional, später): Host-seitig `launchd`-Plist\"). VM-Stdout wird ebenfalls nicht auf die Host-Konsole gespiegelt (`/Users/marco/projects/macvibes/apps/server/src/sandbox/msb.ts:13` pipet nur, `microsandboxProvider.ts:239/244` loggt nur Fehlermeldungen).\n\nDamit setzt der behauptete Impact (\"eigene Aktivität verbergen\", \"log-scraping tool\") eine Audit-/Log-Auswertung voraus, die das Projekt bewusst nicht hat: `/Users/marco/projects/macvibes/security-haertung-plan.md:569` listet unter \"Nicht-Ziele und bewusstes Restrisiko\" Punkt 7 explizit \"Kein Audit-Log, keine Session-Verwaltung im UI, kein 2FA\". Es wird keine Sicherheitskontrolle unterlaufen, kein Host-, Fremd-VM- oder Credential-Zugriff erlangt — der Gast kann höchstens eine kosmetisch verwirrende Zeile im Terminal des Betreibers erzeugen. Zudem darf derselbe Gast per Design beliebigen Text in seinen eigenen Chat schreiben, den der Betreiber ohnehin liest.\n\nDas Bein \"gefährliche Operation\" der Kette ist damit nicht erfüllt: Hygiene-/Code-Qualitätsbefund (CWE-117) ohne Sicherheitsfolge im Bedrohungsmodell → refuted. (Anmerkung, ohne Auswirkung auf das Votum: der gleiche unmaskierte Pfad besteht bei `agentGateway.ts:171` mit der VM-gesteuerten Close-Reason; auch dort ist die Senke dieselbe folgenlose Konsole.)",
          "refuted": true,
          "confidence": "MEDIUM"
        }
      ]
    },
    {
      "id": "C26",
      "voters": 3,
      "refuted": 0,
      "kept": 3,
      "survives": true,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "CODE-WAHRHEIT: Alle sechs Zitate stimmen wörtlich mit dem Repo überein, keines ist halluziniert.\n\n1) `/Users/marco/projects/macvibes/apps/server/src/http/cookies.ts:30-33` — `readSessionToken` ist exakt `const cookie = await cookieStoreOf(request).get(SESSION_COOKIE); return cookie?.value ?? null;`. Kein Dedup, keine Prüfung auf mehrfache Einträge.\n2) `cookies.ts:60-69` — Set mit `path: '/'`, `httpOnly: true`, `sameSite: 'lax'`, Name `macvibes_session` (Zeile 1), also KEIN `__Host-`-Präfix. Der Kommentar `cookies.ts:40-42` bestätigt sogar selbst, dass Cookies nicht portgebunden sind.\n3) `apps/server/src/http/createAppYoga.ts:96-97` — `const token = await readSessionToken(request); const currentUser = token ? await resolveSession(db, config, token) : null;` — Zeilennummer stimmt exakt.\n4) `apps/server/src/http/previewGateway.ts:93` — `const HOST_COOKIES = new Set([SESSION_COOKIE, COOKIE_NAME]);` stimmt; die Menge wird nur in `sanitizeUpstreamHeaders` (105-122, Cookie-Header Richtung VM) und `sanitizeDownstreamHeaders` (130-144, `Set-Cookie` aus der VM) benutzt. Damit ist der HTTP-Header-Weg gesperrt — der `document.cookie`-Weg aus dem gelieferten Skript NICHT.\n5) `apps/web/src/pages/ChatPage.tsx:539` — `sandbox=\"allow-scripts allow-same-origin allow-forms allow-modals allow-popups allow-downloads\"` wörtlich; `allow-same-origin` gibt dem Agenten-Inhalt die echte Origin und damit Zugriff auf `document.cookie`.\n6) `node_modules/@whatwg-node/cookie-store/esm/parse.js:39-42` — `const cookiesPerKey = map.get(key); if (!cookiesPerKey) map.set(key, ...)` → der ERSTE Duplikat-Eintrag gewinnt; `CookieStore.js:21` `getAll(init).then(cookies => cookies[0])`. `server-plugin-cookies/esm/useCookies.js` baut den Store direkt aus `request.headers.get('cookie')`.\n\nSame-Host-Voraussetzung ist im Code erzwungen, nicht angenommen: `ChatPage.tsx:210-221` übergibt `window.location.hostname`, `apps/web/src/models/PreviewModel.ts:52,59` baut daraus `http(s)://${host}:${gatewayPort}/p/<id>/` — Preview und App unterscheiden sich nur im Port, Cookies sind portblind. `apps/web/src/api/graphqlClient.ts:15-17` ruft relativ `/graphql` mit `credentials: 'include'`; `createAppYoga.ts:74` `graphqlEndpoint: '/graphql'` → ein per JS gesetztes `macvibes_session; path=/graphql` wird mitgeschickt.\n\nGesuchte, aber NICHT vorhandene Guards: `grep SESSION_COOKIE` über apps/packages liefert nur cookies.ts und previewGateway.ts — nirgends eine Duplikat-Ablehnung. Der CSRF-Guard (`createAppYoga.ts:38-62`) prüft nur Origin/Sec-Fetch-Site; die Requests des Opfers sind same-origin und passieren ungehindert. `resolveSession` (`apps/server/src/services/authService.ts:236-264`) bindet die Session weder an IP noch User-Agent, der Angreifer-Token löst also im Browser des Opfers auf den Angreifer-User auf. Auch der Gateway-Auth-Check (`previewGateway.ts:238`) hilft nicht — das Opfer ist ja angemeldet, genau deshalb lädt die Preview.\n\nNicht das akzeptierte Restrisiko Nr. 1: dort geht es um fehlende Trennung der Previews UNTEREINANDER; hier springt der Angriff aus der Preview-Origin in den Cookie-Jar der Host-App. Dass diese Grenze als in-scope gilt, zeigen die bereits umgesetzten Härtungen F2/F11/F19 in previewGateway.ts.\n\nNicht verifizierbar ohne Ausführung (Grund für MEDIUM statt HIGH): das reine Browserverhalten — (a) dass ein `document.cookie`-Write mit abweichendem Path neben dem HttpOnly-Cookie akzeptiert wird (RFC 6265bis §5.6 bricht nur bei Übereinstimmung von name+domain+host-only+path ab) und (b) die Sortierung längerer Pfade zuerst (RFC 6265 §5.4). Beides ist spezifiziert und in allen großen Browsern implementiert, konnte hier aber nicht ausgeführt werden."
        },
        {
          "lens": "exploitability",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "Kette ist im echten Code durchgehend belegbar und bricht an keiner Stelle.\n\n1) Angreifer-Rolle: freigeschalteter Nutzer (Threat Model: angemeldete Nutzer sind nicht privilegiert). Er beauftragt den Agenten seiner eigenen VM, in die Preview-App `document.cookie = 'macvibes_session=<sein Token>; path=/graphql'` zu schreiben. Seinen eigenen Token bekommt er trivial (Login per curl liest das Set-Cookie; HttpOnly ist dabei irrelevant).\n\n2) Opfer-Vorbedingung ist erfüllbar: `listProjects` (apps/server/src/services/projectsService.ts:33-39) liefert JEDEM angemeldeten Nutzer ALLE Projekte; das Öffnen der fremden Projektseite startet die fremde Sandbox automatisch (apps/web/src/pages/ChatPage.tsx:192-194, bewusst per R10). Das iframe wird dann ohne weitere Interaktion gerendert (ChatPage.tsx:528-539) und zeigt per `window.location.hostname` auf denselben Host, nur anderen Port (ChatPage.tsx:211, apps/web/src/models/PreviewModel.ts:47-60). Das Gateway autorisiert nicht, es prüft nur irgendeine gültige Session (apps/server/src/http/previewGateway.ts:238) – die Preview des Angreifers wird dem Opfer also ausgeliefert.\n\n3) Der Skriptkontext ist echt, nicht opak: `sandbox=\"allow-scripts allow-same-origin ...\"` (ChatPage.tsx:539) → das iframe behält die Origin `http(s)://<host>:4173`, `document.cookie` funktioniert. Cookies sind nicht portgebunden, das Schreiben landet im Jar von `<host>` und wird an `<host>:4000/graphql` gesendet.\n\n4) Die vorhandenen Gegenmaßnahmen greifen nachweislich NUR auf dem Header-Pfad: `HOST_COOKIES` filtert `macvibes_session` aus dem Cookie-Header zur VM und aus deren `Set-Cookie` (previewGateway.ts:93, 105-127, 131-146). Der JS-Pfad `document.cookie` wird davon nicht berührt. Ein `__Host-`-Präfix existiert nicht (grep über apps/: kein Treffer), das Cookie wird mit `path: '/'` gesetzt (apps/server/src/http/cookies.ts:64,66) – ein JS-Write mit abweichendem Path kollidiert deshalb nicht mit dem HttpOnly-Cookie (RFC 6265 §5.3 Schritt 11 matcht auf name+domain+path).\n\n5) Sink verifiziert: `readSessionToken` → `CookieStore.get` → `getAll(...)[0]` (node_modules/@whatwg-node/cookie-store/esm/CookieStore.js:15-22, 78-91), und `parse.js` behält pro Name den ERSTEN Eintrag (`if (!cookiesPerKey) map.set(...)`). Da Browser laut RFC 6265 §5.4 längere Paths zuerst senden, gewinnt der eingeschleuste `path=/graphql`-Eintrag. `resolveSession` (apps/server/src/services/authService.ts:236-264) bindet weder IP noch UA, `createAppYoga.ts:97` setzt daraus `currentUser`. Der CSRF-Guard (createAppYoga.ts:41-60) greift nicht, weil die Requests vom Opfer selbst same-site abgehen.\n\nKein akzeptiertes Restrisiko: security-haertung-plan.md:557-560 akzeptiert ausdrücklich nur „Cookies und localStorage … zwischen Previews\" – hier wird der Session-Cookie-Namensraum der HOST-App überschrieben, also genau das, was F2/F10 auf dem Header-Pfad bereits schließen sollten. Auch der dort genannte „echte Fix\" (Wildcard-Hostnamen) würde Cookie-Tossing nicht beheben, `__Host-` schon.\n\nSchaden ist fremdgerichtet, nicht Selbstschaden: das Opfer arbeitet danach still in der Session des Angreifers (alle Folge-Eingaben landen in dessen Konto/Projekten), und ein Logout löscht per `readSessionToken` die Session des ANGREIFERS, während `clearSessionCookie`/`writeSessionCookie` nur `path:'/'` (cookies.ts:64,77) anfassen – der `/graphql`-Schatteneintrag überlebt Logout und Re-Login und sperrt das Opfer dauerhaft aus, bis es Cookies manuell löscht.\n\nNicht abschließend prüfbar (kein Browser ausführbar): die Reihenfolge im Cookie-Header und die HttpOnly-Kollisionsregel sind Web-Plattform-Standardverhalten (RFC 6265 §5.3/§5.4), nicht Repo-Code – daher Confidence MEDIUM statt HIGH. Severity MEDIUM ist angemessen: Cross-User-Session-Fixation + persistenter Lockout, aber keine Rechteausweitung (die Richtung ist Opfer→Angreifer-Identität) und Angreifer braucht ein freigeschaltetes Konto plus einen Klick des Opfers."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "Linse 3 (Geltungsbereich/Minderung) konnte den Kandidaten nicht kippen.\n\nKette im echten Code bestätigt:\n- `/Users/marco/projects/macvibes/apps/server/src/http/cookies.ts:1` `SESSION_COOKIE = 'macvibes_session'` (kein `__Host-`-Prefix), `:31` `readSessionToken` liest via CookieStore, `:60-67` setzt `path:'/'`, `httpOnly:true`, `sameSite:'lax'`.\n- `node_modules/@whatwg-node/cookie-store/esm/CookieStore.js:20` `get()` → `getAll(...)[0]`, und `esm/parse.js` (`const cookiesPerKey = map.get(key); if (!cookiesPerKey) map.set(...)`) — bei doppeltem Cookie-Namen gewinnt der ERSTE Eintrag des rohen Cookie-Headers. Kein Duplikat-Reject irgendwo.\n- `apps/server/src/http/createAppYoga.ts:96-97` macht daraus direkt `currentUser`; `apps/server/src/services/authService.ts:236-264` `resolveSession` bindet die Session an nichts (keine IP-/UA-Bindung).\n- Quelle ist angreiferkontrolliert und erreichbar: `apps/web/src/models/PreviewModel.ts:59` baut die iframe-URL als `http://${host}:${gatewayPort}/p/...` — GLEICHER Hostname, nur anderer Port (Cookies sind nicht portgebunden); `apps/web/src/pages/ChatPage.tsx:539` rendert das iframe mit `allow-scripts allow-same-origin`, d. h. echte Origin `http://<host>:4173`, `document.cookie` funktioniert. `apps/server/src/schema/index.ts:379-397` (`enterProject`, bewusst ohne Ownership) sorgt dafür, dass auch ein Nur-Lese-Besucher die fremde Preview startet und angezeigt bekommt.\n\nGesuchte Minderungen — alle geprüft, keine greift:\n- CSRF-Guard `createAppYoga.ts:40-62` + `originPolicy.ts:68-80`: prüft Origin/Sec-Fetch-Site. Der Angriff läuft aber über den FIRST-PARTY-Tab des Opfers (Origin `http://host:4000`, same-origin) — der Guard sieht nichts Auffälliges.\n- CORS `createAppYoga.ts:86-93`: irrelevant, es wird kein Cross-Origin-Request gebraucht.\n- `previewGateway.ts:93` `HOST_COOKIES` + `sanitizeUpstreamHeaders` (105-122) / `sanitizeDownstreamHeaders` (130-144): filtern ausschließlich HTTP-Header (Cookie/Set-Cookie). Ein `document.cookie`-Write im Browser umgeht diesen Filter vollständig. Genau diese Stellen zeigen zugleich, dass das Projekt `macvibes_session` als vor der VM zu schützendes Gut behandelt (F2/F11) — es ist kein akzeptiertes Restrisiko.\n- Kein `__Host-`-Prefix, keine Ablehnung mehrfacher `macvibes_session`-Einträge (Grep über apps/packages/docs zeigt nur die vier Fundstellen in cookies.ts/previewGateway.ts).\n\nRestrisiko-/R10-Prüfung: Das dokumentierte akzeptierte Restrisiko Nr. 1 ist in `security-haertung-plan.md:558-561` explizit auf die Trennung ZWISCHEN Previews beschränkt (\"Cookies und localStorage sind zwischen Previews nicht trennbar\"); der dort genannte \"echte Fix\" (Wildcard-Hostnamen + TLS) würde Cookie-Tossing auf die Parent-Domain gar nicht schließen — nötig wären `__Host-`/Duplikat-Reject, also eine andere, lokale Maßnahme. R10 erlaubt Lesen fremder Projekte und Starten fremder Sandboxes, nicht die Session-Fixierung des Betrachters. Reine Code-Qualität ist es ebenfalls nicht.\n\nOffen geblieben (Grund für MEDIUM statt HIGH): Die entscheidende Reihenfolge im Cookie-Header (RFC 6265 §5.4.2, längerer Path zuerst) und die Browser-Regel, dass ein `document.cookie`-Write mit abweichendem Path nicht mit dem HttpOnly-Cookie kollidiert, sind Browserverhalten, das ich hier nicht ausführen kann; es ist die bekannte Grundlage klassischer Cookie-Tossing-Angriffe, aber nicht im Repo verifizierbar. Severity MEDIUM ist angemessen (Fixierung/Verwirrung des Opfers + persistente Sperre der eigenen API-Nutzung, da Logout nur den Path=/-Cookie löscht; keine direkte Exfiltration bestehender Opferdaten)."
        }
      ]
    },
    {
      "id": "C27",
      "voters": 3,
      "refuted": 3,
      "kept": 0,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "reasoning": "Code-Wahrheit: Die Zitate stimmen fast wörtlich. `packages/shared/src/validation/auth.ts:15` lautet tatsächlich `export const passwordSchema = z.string().min(8, 'Passwort: mindestens 8 Zeichen');` — kein `.max()`, und ein repo-weiter grep zeigt, dass das Schema nur an einer einzigen Stelle angewandt wird (`apps/server/src/services/authService.ts:61`). `apps/server/src/services/authService.ts:84` ist exakt `const passwordHash = await Bun.password.hash(passwordResult.data);`, Zeile 127 exakt `const valid = await Bun.password.verify(password, user.passwordHash);` — der Login-Pfad (authService.ts:116-127) validiert das Passwort wirklich nicht. `apps/server/src/schema/index.ts:264` ist das `password`-Arg der `register`-Mutation, `:305` der `assertWithinLimit(loginLimiter, ...)`-Aufruf. Kein `maxRequestBodySize` bei `Bun.serve` (`apps/server/src/index.ts:313`), kein Reverse-Proxy/Body-Limit im Repo, CSRF-Guard greift bei fehlendem Origin/Sec-Fetch-Site nicht (`apps/server/src/http/originPolicy.ts:72-79`) — der Pfad ist also unauthentifiziert erreichbar.\n\nTrotzdem refuted: Die Kausalkette zum behaupteten Impact (\"CPU- und Memory-Amplification\") bricht an der Stelle, die der Forscher selbst nur als \"bounded but not eliminated\" abtut. Das Rate-Limit (`apps/server/src/services/rateLimiter.ts:24-50`, aufgerufen VOR argon2 in `apps/server/src/schema/index.ts:267` bzw. `:305`, Limits `:109-110`) erlaubt 10 Registrierungen/Stunde/IP und 20 Logins/5min pro IP UND pro Username; abgeschaltet ist es ausschließlich in `apps/web/playwright.config.ts:58` (E2E), nicht in irgendeinem Produktionspfad. Der marginale Zusatzaufwand eines überlangen Passworts in argon2id ist ein einziger linearer Blake2b-Durchlauf über den String, während die fixen Kosten (Bun-Default argon2id, 64 MiB Speicher, t=2) pro Aufruf ohnehin anfallen — der Angreifer muss also Größenordnungen mehr Bytes hochladen, als er CPU-Sekunden erzeugt (De-Amplification statt Amplification), und das gedeckelt auf 20 Requests/5min/IP. Der einzig relevante Hebel wäre die fehlende `maxRequestBodySize` (Bun-Default 128 MB), die jeden Endpunkt gleichermaßen betrifft und unabhängig von `passwordSchema` ist — das ist ein anderes Finding als das gemeldete. Nach dem Bedrohungsmodell (\"fehlende Längenlimits nur real bei realistischem Aufwand\") bleibt hier ein Hardening-Hinweis (`.max(128)` wäre richtig), kein belegter Angriffspfad.",
          "refuted": true,
          "confidence": "MEDIUM"
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Die zitierten Zeilen stimmen (packages/shared/src/validation/auth.ts:15 hat wirklich nur .min(8); apps/server/src/services/authService.ts:84 hash, :127 verify; login validiert das Passwort tatsaechlich gar nicht, authService.ts:116-127). Die Kette erreicht die Senke auch anonym: apps/server/src/http/originPolicy.ts:71-77 laesst Requests ohne Origin/Sec-Fetch-Site (curl) bewusst durch, apps/server/src/index.ts:330 leitet /graphql an Yoga, apps/server/src/schema/index.ts:297-309 ist die login-Mutation ohne Auth. Die Kette reisst aber beim SCHADEN, nicht beim Zugang.\n\n1) Der teure Teil ist explizit vorgelagert gedrosselt: apps/server/src/schema/index.ts:304-308 ruft assertWithinLimit VOR login() auf, mit ZWEI Schluesseln (ip + username); loginLimiter = 20/5min (schema/index.ts:109), registerLimiter = 10/h (schema/index.ts:110), Sliding Window in apps/server/src/services/rateLimiter.ts:23-51. Die IP stammt aus server.requestIP(request) (apps/server/src/index.ts:330), NICHT aus X-Forwarded-For — der Zaehler ist also nicht faelschbar.\n\n2) Die behauptete \"Verstaerkung\" existiert nicht. Bei argon2id waechst mit der Passwortlaenge nur der initiale Blake2b-Pass ueber H0 (~1 GB/s), waehrend der dominante Kostenblock der fixe speicherharte Fill ist, der bei JEDEM Loginversuch ohnehin anfaellt. Der Angreifer muss N Bytes hochladen, um dem Server ~N Bytes Blake2b zu kaufen — bei Buns Default-Body-Limit von 128 MiB (node_modules/bun-types/serve.d.ts:675-678, \"@default 1024 * 1024 * 128\") sind das ~0,13 s CPU pro 128-MiB-Upload. Das ist negative Verstaerkung; mit 20 Versuchen/5min/IP bleiben wenige Sekunden CPU pro Fenster.\n\n3) Die Vorbedingung \"Rate Limit deaktiviert\" ist keine reale Deployment-Konfiguration: MACVIBES_RATE_LIMIT_DISABLED=1 kommt im Repo ausschliesslich in apps/web/playwright.config.ts:58 vor (Testharness), gelesen in apps/server/src/services/rateLimiter.ts:56.\n\n4) Die Kausalzuordnung ist falsch. Der einzige nennenswerte Ressourcendruck (Puffern + JSON/GraphQL-Parsen eines 128-MiB-Bodys) entsteht VOR jedem Resolver und trifft jeden anonymen POST auf /graphql unabhaengig vom Passwortfeld. Die empfohlene Korrektur (.max(128) in packages/shared/src/validation/auth.ts:15 bzw. passwordSchema im login-Pfad) wuerde daran nichts aendern; sie spart nur den vernachlaessigbaren Blake2b-Anteil. Der berichtete Mechanismus ist also nicht die Ursache des einzigen realen Effekts.\n\n5) Der register-Pfad ist noch harmloser: 10 hash-Operationen pro Stunde pro IP (apps/server/src/schema/index.ts:110, :267), davor usernameSchema (authService.ts:57) und ein DB-Select.\n\nUnter der DoS-Regel des Bedrohungsmodells (realistischer Aufwand noetig) kein Finding: der Angreifer zahlt Gigabytes Bandbreite fuer Sekundenbruchteile Server-CPU, und der bewusst vorgelagerte Limiter deckelt genau die teure Operation."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Die Faktenlage stimmt, die Sicherheitsfolge nicht — der Kandidat ist bereits gemindert und reine Defense-in-Depth.\n\nBestaetigt: packages/shared/src/validation/auth.ts:15 setzt tatsaechlich nur `.min(8)` ohne `.max()`, und der Login-Pfad validiert das Passwort gar nicht, bevor /Users/marco/projects/macvibes/apps/server/src/services/authService.ts:127 `Bun.password.verify(password, user.passwordHash)` aufruft.\n\nDrei bereits vorhandene Minderungen brechen die behauptete Kette:\n\n1. Harte Body-Obergrenze: apps/server/src/index.ts:313 nutzt `Bun.serve` ohne `maxRequestBodySize`; der Default ist laut node_modules/bun-types/serve.d.ts:675-678 (`@default 1024 * 1024 * 128 // 128MB`) eine feste 128-MiB-Deckelung. Die Eingabe ist also NICHT \"unbounded\", wie Rationale/Impact behaupten — sie ist per Framework-Default begrenzt.\n\n2. Rate-Limit exakt fuer dieses Szenario, VOR der teuren Operation: apps/server/src/schema/index.ts:109-110 definiert `loginLimiter` (20/5 min) und `registerLimiter` (10/h); apps/server/src/schema/index.ts:267 bzw. 305-308 rufen `assertWithinLimit` vor `register(...)`/`login(...)` auf — beim Login mit zwei Schluesseln (IP und Username). apps/server/src/services/rateLimiter.ts:1-8 benennt genau diesen Zweck (F14: \"jeder unauthentifizierte login erzwingt ein argon2id-verify ... ohne Bremse ist das eine CPU-Senke\").\n\n3. Der Verify-Pfad ist zusaetzlich gefiltert: apps/server/src/services/authService.ts:116-121 macht erst den DB-Lookup und wirft bei unbekanntem Username, bevor `Bun.password.verify` erreicht wird.\n\nDie Praekondition \"mit MACVIBES_RATE_LIMIT_DISABLED=1 unbegrenzt\" traegt nicht: der Schalter wird ausschliesslich in apps/web/playwright.config.ts:58 (E2E-Harness) gesetzt und in security-haertung-plan.md:301 explizit als E2E-Konfiguration beschrieben — keine Deployment-Konfiguration.\n\nDie behauptete \"CPU- und Speicher-Amplifikation\" existiert quantitativ nicht: der laengenabhaengige Anteil von argon2id ist eine einzige Blake2b-Absorption ueber das Passwort, waehrend die fixen ~64 MiB Speicherfuellung dominieren und laengenunabhaengig sind. Ein Angreifer muss bis zu 128 MiB hochladen, um Bruchteile einer Sekunde Zusatz-CPU zu kaufen — De-Amplifikation; die verbleibende laengenunabhaengige Grundlast pro Versuch ist genau das, wogegen das Rate-Limit gebaut wurde. Damit greift die Regel des Bedrohungsmodells fuer DoS-artige Befunde (kein realistischer Aufwandsvorteil). Bleibt ein Haertungswunsch (`.max(128)` in packages/shared/src/validation/auth.ts:15, `passwordSchema` auch im Login) ohne eigenstaendige Sicherheitsfolge."
        }
      ]
    },
    {
      "id": "C28",
      "voters": 3,
      "refuted": 2,
      "kept": 1,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "LINSE CODE-WAHRHEIT: Alle Zitate stimmen mit dem echten Code überein, ein die Kette brechender Guard existiert nicht.\n\nVerifiziert:\n- /Users/marco/projects/macvibes/packages/shared/src/domain/branchName.ts:13-21: `deriveBranchSlug` hat KEINE Längenprüfung; Zeile 15 lautet wörtlich `const transliterated = [...lowered].map((ch) => TRANSLITERATIONS[ch] ?? ch).join('');` — Zitat echt, Zeilennummer korrekt.\n- /Users/marco/projects/macvibes/packages/shared/src/validation/auth.ts:17-25: `projectNameSchema` = trim/min(1)/max(60)/refine(deriveBranchSlug) — exakt wie zitiert; `.max(60)` ist NICHT als `abort` markiert.\n- zod 4.4.3 (node_modules/zod/package.json): node_modules/zod/v4/core/checks.js:342 pusht das too_big-Issue mit `continue: !def.abort` (Forscher nannte :341 — belanglose Off-by-one). node_modules/zod/v4/core/util.js:524-533 liefert dann `aborted()===false`; in node_modules/zod/v4/core/schemas.js:33-46 (runChecks) bleibt `isAborted` false, und der `$ZodCustom`-Check aus `.refine()` (node_modules/zod/v4/core/schemas.js:2209-2224, erzeugt über classic/schemas.js:115 → core/api.js:952) besitzt kein `when`, fällt also in den `else if (isAborted) continue`-Zweig und LÄUFT. Der Refine ruft `deriveBranchSlug` folglich auf dem ungekappten String auf. Kein übersehener Guard.\n- Aufrufer: /Users/marco/projects/macvibes/apps/server/src/services/projectsService.ts:212 `projectNameSchema.safeParse(newName)` steht tatsächlich VOR der Ownership-Prüfung :222; zweiter Pfad :130 in `resolveNewProjectName` (createProject/copyProject).\n- Resolver: /Users/marco/projects/macvibes/apps/server/src/schema/index.ts:331, :364, :377 reichen `args.name` roh durch; `requireUser` (index.ts:121-126) prüft nur Session-Existenz, `resolveSession` (apps/server/src/services/authService.ts:259) verlangt `approved` — also genau die vom Kandidaten genannte Vorbedingung.\n- Aktiv nach Schutz gesucht und KEINE gefunden: kein Pothos-Validation-Plugin (apps/server/src/schema/builder.ts:19 `new SchemaBuilder({})`), keine Body-Größen-/Rate-Limit-Plugins in apps/server/src/http/createAppYoga.ts:94 (nur `useCookies()` + CSRF-Guard), `assertWithinLimit` nur auf register/login (apps/server/src/schema/index.ts:267, :305), kein `maxRequestBodySize` an Bun.serve (apps/server/src/index.ts:313-317).\n\nEinschränkung (Grund für MEDIUM statt HIGH und für die Severity-Korrektur): Der tatsächliche OOM-Kill ist nicht ohne Ausführung belegbar, und die Plattform gestattet jedem approved User ohnehin per Design das Starten von bis zu 8 Sandboxes à 4096 MiB (apps/server/src/config.ts:162, :171) — die marginale Ressourcenwirkung eines ~2-GB-Heap-Peaks liegt damit in der Größenordnung dessen, was legitim möglich ist. Der Code-Defekt selbst (60-Zeichen-Cap gated die teure Ableitung nicht) ist jedoch real und unbestritten.",
          "correctedSeverity": "LOW"
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Die technische Kette des Forschers stimmt weitgehend, reißt aber an der Rollen-Vorbedingung des Bedrohungsmodells.\n\nVerifiziert am echten Code:\n- /Users/marco/projects/macvibes/packages/shared/src/validation/auth.ts:17-25 enthält exakt das zitierte Schema (.trim().min(1).max(60).refine(name => deriveBranchSlug(name).length > 0)).\n- /Users/marco/projects/macvibes/packages/shared/src/domain/branchName.ts:14-15 spreadet ungekappt: `const lowered = name.toLowerCase(); const transliterated = [...lowered].map(...).join('')`, danach noch `.normalize('NFKD')` und zwei Regex-Passes (branchName.ts:16-20). Kein Längen-Guard.\n- zod-4.4.3-Semantik bestätigt: $ZodCheckMaxLength pusht `continue: !def.abort` (node_modules/zod/v4/core/checks.js:336-343), util.aborted() liefert bei continue:true false (node_modules/zod/v4/core/util.js:524-533), und $ZodCustom (node_modules/zod/v4/core/schemas.js:2209-2224) hat kein `when`, läuft in runChecks also trotzdem (node_modules/zod/v4/core/schemas.js:32-46). Das Refine wird tatsächlich auf dem ungekappten String ausgeführt.\n- Kein maxRequestBodySize bei Bun.serve (apps/server/src/index.ts:313; Default 128 MB laut node_modules/bun-types/serve.d.ts:674-678), keine Body-/Query-Limit-Plugins in apps/server/src/http/createAppYoga.ts (nur useCookies + useCsrfGuard), assertWithinLimit nur bei register (apps/server/src/schema/index.ts:267) und login (:305).\n\nWarum die Kette dennoch nicht trägt:\n1. Rolle: Alle drei Einstiege hängen an requireUser (apps/server/src/schema/index.ts:324-331 createProject, :359-364 copyProject, :371-377 renameProject; requireUser bei :121-126). register legt Nutzer als pending an, nur der allererste Admin ist sofort freigeschaltet (apps/server/src/services/authService.ts:99), Login blockt nicht freigeschaltete Konten (:131). Der Angreifer braucht also ein vom Admin bewusst freigeschaltetes Konto — anonym ist der Pfad nicht erreichbar.\n2. Der Browser-/Gast-Pfad ist zu: Preview-JS auf Port 4173 wird vom CSRF-Guard abgelehnt, weil allowedOrigins nur http(s)://<Host-Header> enthält (apps/server/src/http/originPolicy.ts:35-49) und isCrossSiteRequest fremde Origins verwirft (:71-77). Es bleibt allein der angemeldete Nutzer mit curl und Session-Cookie.\n3. Der Schaden ist reine Verfügbarkeit (Heap-Druck/Event-Loop-Block), keine Rechteausweitung, kein Datenabfluss, kein VM→Host-Übertritt. Damit fällt der Kandidat genau in die im Auftrag benannte Ausschlussklasse: DoS durch fehlende Längenlimits ist nur real, wenn er OHNE gültige Session auslösbar ist.\n4. Die konkrete Schadensbehauptung „Prozess stirbt an OOM“ ist zusätzlich unbelegt: 128 MB Body ergeben je nach Zeichen ~20-64 Mio. Codepoints, also grob 1-2 GB transiente Allokation und einen mehrsekündigen Event-Loop-Block — ein Stall, kein sicherer Kill; ein verlässlicher OOM bräuchte mehrere parallele 128-MB-Uploads. Nachweisen ließe sich das nur durch Ausführen, was hier ausgeschlossen ist.\n\nDer Code-Smell (teure Ableitung ohne eigenen Guard, .max(60) nicht abort) ist real und die Empfehlung sinnvoll, als Finding im gegebenen Bedrohungsmodell trägt der Kandidat aber nicht."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Die technischen Einzelbehauptungen stimmen, aber der Kandidat fällt aus dem meldbaren Geltungsbereich.\n\nVerifiziert im echten Code:\n- /Users/marco/projects/macvibes/packages/shared/src/validation/auth.ts:17-25 enthält exakt die zitierte Kette `.trim().min(1).max(60).refine(deriveBranchSlug(name).length > 0)`.\n- /Users/marco/projects/macvibes/packages/shared/src/domain/branchName.ts:14-15 spreizt tatsächlich `[...lowered].map(...).join('')` pro Code Point.\n- node_modules/zod/v4/core/checks.js:342 setzt `continue: !def.abort` (abort undefined -> true), und die Check-Schleife in node_modules/zod/v4/core/schemas.js:33-46 überspringt Folge-Checks nur bei `isAborted`. Die `.refine` läuft also wirklich noch auf dem ungekürzten String.\n- /Users/marco/projects/macvibes/apps/server/src/index.ts:313-320 setzt kein `maxRequestBodySize`; Rate-Limits gibt es nur auf register/login (apps/server/src/schema/index.ts:267, :305).\n\nWarum trotzdem refuted:\n1. Ohne gültige, admin-freigeschaltete Session ist die Kette unerreichbar. Alle drei Einstiege werten `requireUser` VOR jeder Validierung aus: apps/server/src/schema/index.ts:331 (createProject), :364 (copyProject), :377 (renameProject) — erst danach laufen `projectNameSchema.safeParse` in apps/server/src/services/projectsService.ts:130 bzw. :212. `requireUser` (apps/server/src/schema/index.ts:121-126) wirft ohne `ctx.currentUser`; `resolveSession` (apps/server/src/services/authService.ts:258-262) kappt zusätzlich Sessions nicht (mehr) freigeschalteter Nutzer, und Neuregistrierte sind `pending` ohne Session (authService.ts:99, :109, :131). Auch der per Design nicht vertrauenswürdige VM-Gast kommt nicht hin: er hat kein Session-Cookie, der Agent-Gateway-Token authentifiziert nur den WS-Pfad (apps/server/src/index.ts:324-327), nicht /graphql.\n2. Damit ist es genau der im Bedrohungsmodell ausgenommene Fall: DoS durch \"fehlendes Längenlimit\", auslösbar nur MIT gültiger Session.\n3. Die zod-Reihenfolge ist nicht die ermöglichende Bedingung. Die authentifizierte Fläche akzeptiert generell unbegrenzte Strings ohne Body-Limit, z. B. apps/server/src/schema/index.ts:447 (`const text = args.text.trim();` in sendMessage — kein Längendeckel, der Text geht nach SQLite und an den Agenten). Der Zustand ist das fehlende globale `maxRequestBodySize` (apps/server/src/index.ts:313); branchName.ts:15 ist nur ein Verstärkungsfaktor auf einem ohnehin unbegrenzten Pfad. Fehlende Rate-Limits auf authentifizierten Mutationen sind im Projekt bewusst so gehalten (security-haertung-plan.md:559-561).\n\nNicht abschließend klärbar: die reale OOM-Schwelle (JSC-Heapverhalten bei ~64M Array-Slots) wäre nur durch Ausführen belegbar, was hier ausgeschlossen ist. Für das Ergebnis unerheblich, weil die Kette schon am Geltungsbereich scheitert."
        }
      ]
    },
    {
      "id": "C29",
      "voters": 3,
      "refuted": 2,
      "kept": 1,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "Alle Zitate des Kandidaten sind wörtlich korrekt und es findet sich kein übersehener Guard.\n\nCode-Wahrheit (Repo /Users/marco/projects/macvibes):\n- packages/shared/src/domain/branchName.ts:15 lautet exakt `const transliterated = [...lowered].map((ch) => TRANSLITERATIONS[ch] ?? ch).join('');`, davor :14 `toLowerCase()`, danach :17 `.normalize('NFKD')` — kein Längen-Guard am Funktionsanfang (:13-21).\n- packages/shared/src/validation/auth.ts:17-25: `.trim().min(1).max(60, '…').refine((name) => deriveBranchSlug(name).length > 0, …)` — die `.max`-Prüfung ist ohne `abort`, die Refine steht danach.\n- apps/server/src/services/projectsService.ts:130 und :212 rufen `projectNameSchema.safeParse(...)` genau so auf; bei renameProject läuft safeParse (:212) sogar VOR der Ownership-Prüfung (:222).\n- Quelle attacker-kontrolliert: apps/server/src/schema/index.ts:327 / :361 / :374 `name: t.arg.string({ required: true })`, ungefiltert an die Services (:331, :364, :377).\n\nZod-Semantik selbst nachgelesen (zod 4.4.3):\n- node_modules/zod/v4/core/checks.js:342 `continue: !def.abort` im `$ZodCheckMaxLength`-Issue → `continue: true`.\n- node_modules/zod/v4/core/util.js:524-531 `aborted()` liefert nur `true`, wenn ein Issue `continue !== true` hat → hier `false`.\n- node_modules/zod/v4/core/schemas.js:32-46 `runChecks`: `else if (isAborted) continue;` greift nicht, weil `isAborted` false bleibt; `$ZodCheck.init` (checks.js:5-10) setzt kein `when` und `_refine` (api.js:952-960) setzt kein `abort`, also läuft die Refine.\n- node_modules/zod/v4/core/schemas.js:2217 `const r = def.fn(input);` → `deriveBranchSlug` auf dem VOLLEN, getrimmten String.\n\nFehlende Minderungen aktiv gesucht:\n- apps/server/src/index.ts:313-322 `Bun.serve({ port, hostname, idleTimeout, websocket, fetch })` — kein `maxRequestBodySize`; node_modules/bun-types/serve.d.ts:675-678 dokumentiert Default 128 MB.\n- apps/server/src/http/createAppYoga.ts:94 `plugins: [useCookies(), useCsrfGuard(originsFor)]` — kein Body-/Query-Size-Plugin.\n- Rate-Limit existiert nur für register/login (apps/server/src/schema/index.ts:109-110, 267, 305); die Projekt-Mutationen sind ungelimitet.\n- `requireUser` (schema/index.ts:121-126) plus Approval-Check (services/authService.ts:259) — es genügt ein freigeschalteter Account, was der Kandidat korrekt als Vorbedingung nennt.\n\nEinschränkung meiner Sicherheit: Die konkrete OOM-Größenordnung (\"Gigabytes\", Prozess-Tod) konnte ich nur rechnerisch, nicht durch Ausführung bestätigen — deshalb MEDIUM. Der Carve-out \"Nutzer macht seinen eigenen Server langsam\" greift hier m. E. nicht, weil ein einziger Prozess API, Subscriptions, Agent- und Preview-Gateway aller Nutzer trägt und der Aufwand (wenige 100-MB-POSTs im LAN) realistisch ist."
        },
        {
          "lens": "exploitability",
          "confidence": "MEDIUM",
          "refuted": true,
          "reasoning": "Der technische Kern des Kandidaten stimmt, die Ausnutzungskette trägt die behauptete Wirkung aber nicht.\n\nVerifiziert (Code deckt sich mit dem Kandidaten):\n- /Users/marco/projects/macvibes/packages/shared/src/validation/auth.ts:17-25 — `.trim().min(1).max(60).refine(name => deriveBranchSlug(name).length > 0)`.\n- zod 4.4.3: node_modules/zod/v4/core/checks.js:342 setzt `continue: !def.abort` (= true), node_modules/zod/v4/core/util.js:524-527 liefert damit `aborted() === false`, node_modules/zod/v4/core/schemas.js:44-45 überspringt den Custom-Check nur bei `isAborted` — der Refine (node_modules/zod/v4/core/schemas.js:2216 `def.fn(input)`) läuft also wirklich auf dem vollen String. /Users/marco/projects/macvibes/packages/shared/src/domain/branchName.ts:14-19 hat keinen Längen-Guard.\n- Keine Body-Grenze: Bun.serve in /Users/marco/projects/macvibes/apps/server/src/index.ts:312-330 setzt nur port/hostname/idleTimeout/websocket/fetch; /Users/marco/projects/macvibes/apps/server/src/http/createAppYoga.ts:93 registriert nur `useCookies()` und `useCsrfGuard()`. Rate-Limits nur für login/register (/Users/marco/projects/macvibes/apps/server/src/schema/index.ts:109-110).\n\nWarum die Kette reißt:\n1. Rolle: Einziger Weg zu `deriveBranchSlug` ist /Users/marco/projects/macvibes/apps/server/src/services/projectsService.ts:130 und :212, beide hinter `requireUser` (/Users/marco/projects/macvibes/apps/server/src/schema/index.ts:121-126, Aufrufe :331/:364/:377). Eine Session gibt es nur für admin-freigeschaltete Konten (/Users/marco/projects/macvibes/apps/server/src/services/authService.ts:107-110: `if (!user.approved) return { user, session: null }`). Kein anonymer Pfad, kein Pfad aus der VM — grep über alle Aufrufer zeigt nur diese drei Mutationen.\n2. Die zentrale Behauptung des Exploit-Szenarios (\"a handful of concurrent requests exhausts the heap\") ist mechanisch falsch: `toLowerCase → [...] → map → join → normalize` in branchName.ts:14-19 ist vollständig synchron auf Buns einzelnem JS-Thread. Parallele Requests serialisieren sich, es existiert immer nur EINE verstärkte Allokation gleichzeitig, und die Arrays sind nach Rückkehr aus `safeParse` sofort Garbage. Der behauptete Multiplikator \"mehrere Requests × Gigabytes\" entsteht so nicht; übrig bleibt ein transienter Spike plus ein Event-Loop-Stall pro Request.\n3. Damit landet der reale Effekt genau im ausdrücklich akzeptierten Restrisiko: ein angemeldeter, admin-freigeschalteter Nutzer verlangsamt einen Server, den er ohnehin benutzen darf — Kategorie \"fehlende Längenlimits\", auslösbar nur MIT gültiger Session.\n4. Der einzige anonym erreichbare Anteil ist das generische Fehlen von `maxRequestBodySize` (Bun-Default 128 MB, Body-Buffering + JSON.parse in Yoga vor jeder Auth) — das ist ein anderes Finding als das hier geschriebene und ohne den branchName-Verstärker.\n\nNicht verifizierbar: der behauptete Speicher-Peak/Prozess-OOM. Das ist eine Projektion des Forschers; ausführen darf ich hier nichts, und im Repo gibt es dafür keinen Beleg."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "confidence": "MEDIUM",
          "reasoning": "Die technische Kette stimmt, fällt aber in die bewusst deprioritisierte DoS-Klasse.\n\nVerifiziert (der Forscher irrt in keinem Codezitat):\n- packages/shared/src/validation/auth.ts:17-26 — `.trim().min(1).max(60,…).refine((name) => deriveBranchSlug(name).length > 0, …)`, kein `abort: true`.\n- node_modules/zod/v4/core/checks.js:342 — der MaxLength-Check pusht das Issue mit `continue: !def.abort`, also `continue: true`; node_modules/zod/v4/core/util.js:524-533 (`aborted()`) liefert damit `false`, und die Schleife in node_modules/zod/v4/core/schemas.js:33-47 überspringt nur bei `isAborted`. Der `$ZodCustom`-Refine (schemas.js:2208-2223) hat kein `when`, läuft also tatsächlich auf dem vollen String — der Spread in packages/shared/src/domain/branchName.ts:15 wird ausgeführt.\n- Keine Minderung gefunden: kein `maxRequestBodySize` in apps/server/src/index.ts:312-320 (bun-types/serve.d.ts:676-678 dokumentiert 128 MB Default), kein Body-/Query-Size-Plugin in apps/server/src/http/createAppYoga.ts:93 (`plugins: [useCookies(), useCsrfGuard(originsFor)]`), Rate-Limits nur für login/register (apps/server/src/schema/index.ts:109-110).\n\nWarum trotzdem kein zu meldendes Finding (Linse Geltungsbereich/Minderung):\n1. Auslösung erfordert zwingend eine gültige Session: createProject/copyProject/renameProject rufen `requireUser(ctx)` (apps/server/src/schema/index.ts:331, :364, :377), und Konten sind erst nach Admin-Freigabe nutzbar (apps/server/src/services/authService.ts:99, :109). Damit ist es exakt der Fall, den das Bedrohungsmodell ausschließt: „fehlende Längenlimits\", ausgelöst von einem angemeldeten Nutzer.\n2. Derselbe angemeldete Nutzer besitzt bereits eine explizit akzeptierte, deutlich billigere Ressourcen-Erschöpfung: `enterProject` startet für JEDES (auch fremde) Projekt eine MicroVM, nur mit `requireUser` (apps/server/src/schema/index.ts:379-393), und security-haertung-plan.md:562-564 hält als bewusstes Restrisiko fest: „Fremde Sandboxes dürfen weiter gestartet werden … kein Rate-Limit darauf\". Ein Angreifer mit Konto kann den Host also per GB-weiser VM-Allokation kippen, ohne 100 MB Body zu senden — die Speicheramplifikation über den Projektnamen fügt keine neue Angriffsklasse hinzu.\n3. Die eigentliche fehlende Kontrolle ist das nicht gesetzte `maxRequestBodySize` (auch der Forscher führt es nur als „independently\"-Empfehlung); dieses Primitiv steht unauthentifiziert jedem LAN-Client offen und ist nicht der hier gemeldete Sink. Der marginale Beitrag von branchName.ts:15 ist ein konstanter Amplifikationsfaktor auf einer ohnehin vorhandenen Basis.\n\nReine Verfügbarkeitsfolge, durch Neustart behebbar, keine VM-Grenzverletzung, keine Host-Credentials. Nach der Vorgabe „ein angemeldeter Nutzer, der seinen Server langsam macht, ist kein Finding\" → refuted. (Die Härtung `abort: true` plus Längenguard in deriveBranchSlug bleibt sinnvolle Code-Qualität.)"
        }
      ]
    },
    {
      "id": "C30",
      "voters": 3,
      "refuted": 3,
      "kept": 0,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": true,
          "confidence": "HIGH",
          "correctedSeverity": "NONE",
          "reasoning": "CODE-WAHRHEIT: Alle Zitate sind wörtlich korrekt, keine Halluzination. `/Users/marco/projects/macvibes/packages/shared/src/validation/auth.ts:15` lautet exakt `export const passwordSchema = z.string().min(8, 'Passwort: mindestens 8 Zeichen');`; `/Users/marco/projects/macvibes/apps/server/src/services/authService.ts:61` und `:84` stimmen wörtlich; `/Users/marco/projects/macvibes/packages/shared/src/validation/__tests__/validation.test.ts:22` enthält tatsächlich `expect(passwordSchema.safeParse('12345678').success).toBe(true);`; `/Users/marco/projects/macvibes/apps/server/src/schema/index.ts:109` stimmt ebenfalls. Ein globaler Auth-Zwang vor /graphql existiert nicht (`/Users/marco/projects/macvibes/apps/server/src/http/createAppYoga.ts:94-106` setzt nur `currentUser`), die login-Mutation ist also wirklich unauthentifiziert erreichbar.\n\nTrotzdem bricht die Kausalkette an mehreren vom Forscher unterschätzten bzw. übersehenen Guards, und sie hängt an einer Annahme, die NICHT im Code steht:\n\n1) Der Limiter wird VOR dem argon2-Verify mit ZWEI Schlüsseln geprüft — `/Users/marco/projects/macvibes/apps/server/src/schema/index.ts:305-308` (`ip:${ctx.clientIp}` UND `user:${args.username.toLowerCase()}`), Implementierung als echtes Sliding-Window in `/Users/marco/projects/macvibes/apps/server/src/services/rateLimiter.ts:28-47`. Standardmäßig aktiv (`rateLimitDisabled()` nur bei `MACVIBES_RATE_LIMIT_DISABLED=1`, rateLimiter.ts:55-57).\n\n2) Der im Exploit-Szenario zentrale Enumerations-Schritt ist deutlich stärker gebremst als behauptet: `assertWithinLimit(registerLimiter, ...)` läuft VOR `register()` (`/Users/marco/projects/macvibes/apps/server/src/schema/index.ts:267`) mit `max: 10 / 60 min` (Zeile 110) und zählt auch fehlgeschlagene Versuche mit — also ~240 Namensproben/Tag/IP, nicht „registriert, um Usernamen zu lernen\" nach Belieben.\n\n3) Auf dem Login-Pfad gibt es keinen Enumerations-Orakel: unbekannter Nutzer und falsches Passwort liefern dieselbe Meldung (`authService.ts:8` LOGIN_FAILED_MESSAGE, verwendet in `:125` und `:129`).\n\n4) Jeder Neu-Account ist `pending` (`authService.ts:99`, `:109-111`), und `login` weist nicht freigeschaltete Nutzer ab (`authService.ts:131-133`) — Selbstregistrierung verschafft dem Angreifer selbst keinen nutzbaren Account.\n\nEntscheidend: Der „gefährliche Sink\" ist hier eine reguläre, argon2id-geschützte und ratenbegrenzte Authentifizierung (`authService.ts:127`). Es gibt keine angreifergesteuerte Eingabe, die einen gefährlichen Vorgang auslöst — der Angriff funktioniert nur unter der zusätzlichen, im Repo nirgends belegten Voraussetzung, dass ein realer Nutzer ein Top-Listen-Passwort gewählt hat. Das ist eine Härtungsempfehlung (CWE-521, „min 8 statt min 12 + Denylist\"), kein verifizierter vollständiger Angriffspfad. Damit fällt der Kandidat unter „violates best practice\" = FALSE_POSITIVE."
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Die zitierten Zeilen stimmen (packages/shared/src/validation/auth.ts:15 `z.string().min(8, ...)`, apps/server/src/services/authService.ts:61/84, Bun.password.verify in authService.ts:127), aber die Angriffskette reißt an zwei Stellen.\n\n1) Fehlende attacker-kontrollierte Vorbedingung: Der Exploit setzt voraus, dass ein FREMDER Kontoinhaber freiwillig ein Top-N-Passwort gewählt hat. Der Angreifer kontrolliert das nicht und kann es nicht erzwingen — es gibt keine Codepfad-Quelle, die vom Angreifer zum Schaden führt. Das eigene, schwache Passwort zu setzen (authService.ts:61) schadet nur ihm selbst. Damit ist das ein Härtungs-/Best-Practice-Punkt (CWE-521), kein vollständiger Angriffspfad.\n\n2) Reale, gelesene Bremse zwischen Eingabe und Sink: apps/server/src/schema/index.ts:305-308 ruft `assertWithinLimit(loginLimiter, ['ip:...', 'user:...'])` VOR dem argon2-Verify auf; loginLimiter = 20 Versuche / 5 min (schema/index.ts:109) und zwar auf BEIDEN Schlüsseln. Der IP-Schlüssel stammt aus dem Socket (apps/server/src/index.ts:330 `server.requestIP(request)?.address`) — im ganzen Server gibt es kein X-Forwarded-For-Parsing (grep über apps/server/src ohne Treffer), die IP ist über das Netz also nicht fälschbar. Der Username-Schlüssel wird per `toLowerCase()` normalisiert, und usernameSchema (auth.ts:8-13) erlaubt ohnehin nur Kleinbuchstaben, d.h. Case-Varianten können den Zähler nicht auffächern; der DB-Lookup ist exakt (`eq(users.username, username)`, authService.ts:122), Padding-Varianten treffen kein Konto. Abschaltbar ist der Limiter nur per Env MACVIBES_RATE_LIMIT_DISABLED=1 (rateLimiter.ts:55-57), gesetzt ausschließlich in apps/web/playwright.config.ts:58 — nicht im Default-Deployment.\n\n3) Auch die im Szenario nötige Username-Enumeration ist gedrosselt: registerLimiter 10/Stunde pro IP (schema/index.ts:110, angewandt in :267), also ca. 240 Namensproben/Tag.\n\n4) Der behauptete Impact ist teilweise bewusst akzeptiert: Lesen fremder Projekte und Starten fremder Sandboxes ist per R10 gewollt (schema/index.ts:152 `getProjectAny`, :225, :386), so dass der Zugewinn einer Kontoübernahme kleiner ist als beschrieben.\n\nDer Forscher gibt selbst zu, dass der Angriff \"Geduld\" braucht. Ein Rate-Limit von 20/5min auf IP UND Username vor dem teuren Verify ist genau die Gegenmaßnahme gegen Online-Guessing; ohne eine vom Angreifer auslösbare Vorbedingung bleibt nur eine Policy-Empfehlung. Kein echtes Finding."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Die zitierten Codestellen stimmen wörtlich (`/Users/marco/projects/macvibes/packages/shared/src/validation/auth.ts:15` = `z.string().min(8, ...)`; `authService.ts:61` safeParse; `authService.ts:84` `Bun.password.hash`). Der Kandidat scheitert aber an Linse 3 — die Minderung existiert bereits und ist genau gegen dieses Szenario gebaut:\n\n1. Kompensierende Kontrolle vorhanden und gelesen: `/Users/marco/projects/macvibes/apps/server/src/schema/index.ts:109` `createRateLimiter({ windowMs: 5*60_000, max: 20 })`, angewendet in `index.ts:305-308` mit ZWEI Schlüsseln (`ip:` UND `user:<username>`), geprüft VOR dem argon2-Verify. `assertWithinLimit` (`index.ts:112-119`) wirft bei jedem überschrittenen Schlüssel. Der Kommentar `index.ts:105-107` und `security-haertung-plan.md:291-301` (F14) belegen, dass genau „verteiltes Raten auf ein einzelnes Konto\" die Design-Absicht war. Der Kandidat räumt diese Bremse selbst ein und braucht „Geduld\" — d.h. die Kette ist nicht ungemindert, sondern gedrosselt. Die Enumerations-Vorstufe ist zusätzlich per `index.ts:267` (`registerLimiter`, 10/h/IP) auf ~240 Versuche/Tag gedeckelt.\n\n2. Kein bestätigter Angriffspfad, sondern eine Verhaltens-Hypothese: Die Vorbedingung „ein Opferkonto hat ein Top-Listen-Passwort\" ist kein Code-Fakt und aus dem Repo nicht verifizierbar. Es gibt keine Stelle, an der ein Angreifer das Opfer-Passwort beeinflusst; die „gefährliche Operation\" (`authService.ts:127` `Bun.password.verify` mit argon2id) ist die korrekte Authentifizierung selbst.\n\n3. `min(8)` verletzt keinen aktuellen Standard — NIST SP 800-63B setzt 8 Zeichen als Minimum an und rät von Komplexitätsregeln ausdrücklich ab; die Empfehlung des Forschers (Komplexität) läuft dem entgegen. Real bleibt nur die fehlende Breach-/Top-N-Denylist, also reine Härtung ohne belegte Sicherheitsfolge.\n\n4. Impact ist unter dem Bedrohungsmodell weitgehend R10-akzeptiert: Fremde Projekte lesen und fremde Sandboxes starten darf laut R10 jeder angemeldete Nutzer ohnehin; angemeldete Nutzer sind laut Modell „NICHT privilegiert\". Zudem greift der Approval-Gate (`authService.ts:131-133` `if (!user.approved) throw NOT_APPROVED_MESSAGE`, gesetzt in `authService.ts:100` `approved: istErsterAdmin`), sodass selbstregistrierte Konten ohne Admin-Freigabe gar keine Session bekommen.\n\nEs gibt keinen zweiten Passwort-Setz-Pfad (grep über `passwordSchema|password.hash|changePassword` findet nur `register`), der die Policy umgehen könnte — auch das nimmt der Empfehlung die Dringlichkeit. Ergebnis: Härtungshinweis, kein meldepflichtiges Finding."
        }
      ]
    },
    {
      "id": "C31",
      "voters": 3,
      "refuted": 0,
      "kept": 3,
      "survives": true,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": false,
          "confidence": "HIGH",
          "reasoning": "Code-Wahrheit geprüft — alle Zitate stimmen, kein übersehener Guard gefunden.\n\n1) Sink existiert wörtlich: /Users/marco/projects/macvibes/apps/server/src/sandbox/microsandboxProvider.ts:181 `` `0.0.0.0:${hostPort}:${context.previewPort}` `` mit Kommentar :177 „0.0.0.0: Preview ist im LAN erreichbar (R7/NFR)\". Der Port stammt aus :120 `const hostPort = await this.ports.allocate(context.previewPort)`; portService.ts:41-49 bevorzugt exakt den Template-Port (5173/3000), ist also vorhersagbar — und zusätzlich per GraphQL lesbar (schema/index.ts:74-77 `previewHostPort`).\n\n2) Die vom Forscher zitierte Gegenmaßnahme greift nur im Gateway-Pfad: previewGateway.ts:93 `const HOST_COOKIES = new Set([SESSION_COOKIE, COOKIE_NAME])` und :105-122 `sanitizeUpstreamHeaders` filtern `macvibes_session` nur auf dem Weg über 127.0.0.1:vmPort (previewGateway.ts:270). Für Direktzugriffe auf `0.0.0.0:<hostPort>` liegt dazwischen kein Code — msb reicht TCP durch.\n\n3) Cookie-Eigenschaften wie behauptet: cookies.ts:64-68 `path:'/'`, `sameSite:'lax'`, `secure: isSecureRequest(request)`; isSecureRequest (cookies.ts:45-53) liefert im Default-LAN-http `false`. Da der Port nicht Teil der „site\" ist, ist http://host:4173 → http://host:<hostPort> same-site, Lax blockt nicht. Der Beweis steht im Repo selbst: das Gateway auf Port 4173 authentifiziert genau über dieses Cookie (previewGateway.ts:249 `cookieValue(req.headers.get('cookie'), SESSION_COOKIE)`), während die App auf 4000 läuft — die Portunabhängigkeit ist also Designannahme dieses Codes.\n\n4) Aktiv nach Guards gesucht, keiner vorhanden: kein CSP irgendwo (sanitizeDownstreamHeaders, previewGateway.ts:125-144, entfernt nur HSTS/Clear-Site-Data); das iframe erlaubt Skripte (ChatPage.tsx:539 `sandbox=\"allow-scripts allow-same-origin ...\"`), also kann agentgeschriebenes JS `fetch(...,{credentials:'include'})` auf den Direktport absetzen; kein 127.0.0.1-Binding, keine Firewall-Regel im Repo.\n\n5) Impact bestätigt: der Token ist reines Bearer-Credential ohne IP/UA-Bindung (services/authService.ts:236-262 resolveSession), Previews sind bewusst für jeden angemeldeten Nutzer sichtbar (index.ts:220-225, „Ownership wird bewusst NICHT geprüft\").\n\nNicht durch die akzeptierten Restrisiken gedeckt: Restrisiko 1 betrifft die geteilte Preview-Origin 4173; hier verlässt der Host-Session-Token die Host-Grenze in die per Designdefinition untrusted VM — genau das, was F2 (security-haertung-plan.md:158-163) verhindern sollte. Einzige nicht aus dem Code belegbare Annahme: das tatsächliche Bind-Verhalten der externen msb-CLI (nicht ausführbar), die Absicht ist aber im Kommentar :177 explizit."
        },
        {
          "lens": "exploitability",
          "refuted": false,
          "confidence": "HIGH",
          "reasoning": "Kette vollständig nachvollzogen, jede Behauptung des Kandidaten gegen den echten Code geprüft — sie hält stand.\n\n1) Sink existiert wie zitiert: `apps/server/src/sandbox/microsandboxProvider.ts:181` mappt `0.0.0.0:${hostPort}:${context.previewPort}`; der Kommentar 176-179 sagt ausdrücklich \"Preview ist im LAN erreichbar (R7/NFR)\". Der Port stammt aus `portService.ts:41-49` und bevorzugt den Template-Port (z. B. 5173) — also oft deterministisch.\n\n2) Die Schutzmaßnahme existiert, greift aber nur auf dem Gateway-Pfad: `previewGateway.ts:105-122` (`sanitizeUpstreamHeaders`, HOST_COOKIES Zeile 93) entfernt `macvibes_session` und der Upstream geht auf `http://127.0.0.1:${vmPort}` (Zeile 271). Der direkt veröffentlichte 0.0.0.0-Port umgeht diese Sanitisierung komplett — es ist also kein \"bekanntes Restrisiko\", sondern der Bypass eines bewusst gebauten Controls (F2).\n\n3) Angreifer-kontrollierte Quelle: Der Preview-Inhalt kommt aus der per Design nicht vertrauenswürdigen VM und läuft im Browser des Opfers mit Skripten — `apps/web/src/pages/ChatPage.tsx:530-539` setzt `sandbox=\"allow-scripts allow-same-origin ...\"`. Kein CSP-Header irgendwo (grep über apps/server/src und apps/web/src: kein Treffer für content-security-policy); `sanitizeDownstreamHeaders` (previewGateway.ts:130-144) entfernt nur HSTS/Clear-Site-Data. Selbst ohne Skripte genügt ein `<img src>`.\n\n4) Opfer erreichbar ohne Ownership-Hürde: `apps/server/src/index.ts:216-224` authentifiziert am Gateway JEDE gültige Session ohne Ownership-Prüfung (\"fremde Projekte dürfen live betrachtet werden (R10)\"); `schema/index.ts:212-216` liefert via `listProjects` (projectsService.ts:32-39, kein Owner-Filter) ALLE Projekte inklusive `previewHostPort` (`schema/index.ts:74-77`) an jeden angemeldeten Nutzer. Der Angreifer kennt den Ziel-Port also exakt, ohne Raten.\n\n5) Cookie wird tatsächlich angehängt: `cookies.ts:60-69` — `sameSite: 'lax'`, `domain: null`, `path: '/'`, `secure: isSecureRequest(request)`. `isSecureRequest` (cookies.ts:45-53) liefert im reinen LAN-http false; der Kommentar 41-43 belegt, dass genau dieser Klartext-Betrieb ein unterstützter Modus ist, und `PreviewModel.ts:57-62` hat den http-Zweig. SameSite ist site-, nicht port-gebunden: `http://<host>:4173` → `http://<host>:<hostPort>` ist same-site, Lax blockt nicht, `Secure` fehlt. `httpOnly` hilft nicht, weil der Token beim Angreifer-Devserver als normaler Request-Header ankommt — genau das, was previewGateway.ts:98-103 im Kommentar selbst beschreibt.\n\n6) Schaden real: `authService.ts:236-263` (`resolveSession`) ist ein reiner Bearer-Lookup über `sessionKey(token)`, keine IP-/UA-Bindung. Ein Replay per curl umgeht auch den CSRF-/Origin-Guard (`createAppYoga.ts:40-94`), weil der Angreifer alle Header selbst setzt. Opfer kann ein Admin sein (`authService.ts:194,213`) → Privilegieneskalation; unter dem Bedrohungsmodell erreicht der Gast damit Host-Credentials.\n\nRolle des Angreifers: entweder ein freigeschalteter, unprivilegierter Nutzer mit eigenem Projekt (er liest `previewHostPort` seines eigenen Projekts trivial aus der eigenen UI), oder direkt der prompt-injizierte Agent in der VM — dieser kann `location.hostname + ':' + MACVIBES_PREVIEW_PORT` raten, weil `portService.ts:41-49` genau diesen Port bevorzugt, und damit schon das Opfer treffen, das nur seine EIGENE Preview ansieht (kein Social Engineering nötig).\n\nEinzige echte Einschränkung: In der in CLAUDE.md dokumentierten Caddy-/TLS-Variante (App https, iframe https://host:8443) wäre das Cookie `Secure` und der http-Abgriff scheitert an Mixed Content + Schemeful-Same-Site. Der Klartext-LAN-Betrieb ist aber ein ausdrücklich unterstützter Modus (cookies.ts:41-43, PreviewModel.ts:57-62) und entspricht dem vorgegebenen Bedrohungsmodell (\"Server bindet 0.0.0.0, im LAN erreichbar\"). Damit ist die Vorbedingung erfüllbar, nicht konstruiert. Auch das akzeptierte Restrisiko 1 (geteilte Preview-Origin auf 4173) deckt das nicht ab: hier leckt der Host-Session-Token in die VM, was der Code explizit verhindern will.\n\nNicht ausführbar geprüft (kein Code-Ausführen erlaubt): dass `msb run -p 0.0.0.0:...` tatsächlich auf allen Interfaces bindet — der Code-Kommentar microsandboxProvider.ts:176-178 und der Gateway-Upstream auf 127.0.0.1 machen das aber eindeutig zur Absicht."
        },
        {
          "lens": "scope-and-mitigation",
          "refuted": false,
          "confidence": "MEDIUM",
          "reasoning": "Kette im echten Code nachvollzogen, keine greifende Minderung gefunden, und der Fall fällt unter keines der vier akzeptierten Restrisiken.\n\n1) Sink bestätigt: /Users/marco/projects/macvibes/apps/server/src/sandbox/microsandboxProvider.ts:177-181 startet jede Projekt-VM mit `-p 0.0.0.0:${hostPort}:${context.previewPort}` (Kommentar: „Preview ist im LAN erreichbar (R7/NFR)\"). hostPort stammt aus PortAllocator.allocate (microsandboxProvider.ts:120), der den Template-Port bevorzugt (portService.ts:41-49, also i. d. R. 5173).\n\n2) Die Minderung, die dadurch umgangen wird, existiert und ist explizit: previewGateway.ts:93 (HOST_COOKIES) und :105-122 (sanitizeUpstreamHeaders) entfernen `macvibes_session` auf dem Weg in die VM, mit genau der Begründung „Cookies sind nicht portgebunden … der Dev-Server in der VM bekäme sonst den Session-Token jedes Betrachters\" (previewGateway.ts:99-103). Das Gateway selbst braucht nur Loopback (previewGateway.ts:271 `http://127.0.0.1:${vmPort}`, :294 ws://127.0.0.1) — der 0.0.0.0-Publish hat für den Browser-Pfad keine Funktion mehr: die iframe-URL zeigt ausschließlich aufs Gateway (PreviewModel.ts:52 und :59). Die Fix-Empfehlung (127.0.0.1-Mapping) ist damit funktional folgenlos und NICHT das „eigene Vorhaben\" aus Restrisiko 1 (Wildcard-Hostnamen/TLS).\n\n3) Cookie-Semantik: writeSessionCookie setzt `sameSite: 'lax'`, `domain: null`, `secure: isSecureRequest(request)` (cookies.ts:60-69); im dokumentierten Klartext-LAN-Betrieb (`bun run start`, Server auf 0.0.0.0:4000, config.ts:151) ist secure=false. Dass eine Preview auf einem anderen Port same-site zur App ist und Lax deshalb nicht greift, sagt das Repo selbst (originPolicy.ts:5-8: „Weil die Preview auf Port 4173 same-site zur App auf 4000 ist, greift SameSite=Lax nicht\"). Das iframe-`sandbox` hilft hier nicht: ChatPage.tsx:539 enthält `allow-same-origin`, die Preview behält also eine echte, same-site Origin.\n\n4) Kein Auffangnetz beim Replay: Der gestohlene Token ist ein reiner Bearer ohne IP-/UA-Bindung (authService.ts:236-264), und die CSRF-Prüfung lässt Requests ohne Origin und ohne Sec-Fetch-Site bewusst durch (originPolicy.ts:73-79) — curl mit dem Cookie ist also gültig. Genau die Kontrolle, die Preview-JS sonst blockiert (4173 steht nicht in allowedOrigins, originPolicy.ts:35-49), wird durch die Token-Exfiltration umgangen. Den exakten Zielport kann ein angemeldeter Angreifer über das GraphQL-Feld `previewHostPort` (schema/index.ts:74-76) auslesen; die Exfiltration läuft über den bewusst offenen Public-Egress.\n\nNicht abgedeckt von R10 (fremde Sandbox starten ≠ fremde Session stehlen) und nicht von Restrisiko 1 (geteilte Origin auf 4173) — betroffen ist der zusätzliche, direkt publizierte VM-Port. Einschränkung, die ich nicht wegdiskutieren kann und die die Confidence auf MEDIUM hält: im Caddy-HTTPS-Deployment (CLAUDE.md) ist das Cookie `Secure` und ein Abfluss nach `http://host:5173` scheitert an Mixed-Content/Secure — die Kette gilt für die Klartext-Variante, die laut NAECHSTE-SESSION.md:550 („Login und Preview … über http://<mac-ip>:4000 … muss gehen\") weiter unterstützt ist. Severity MEDIUM ist damit angemessen."
        }
      ]
    },
    {
      "id": "C32",
      "voters": 3,
      "refuted": 3,
      "kept": 0,
      "survives": false,
      "unverified": false,
      "votes": [
        {
          "lens": "code-truth",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Code-Wahrheit: Die Zitate stimmen wörtlich. `/Users/marco/projects/macvibes/apps/server/src/config.ts:162` lautet exakt `maxSandboxes: Number(Bun.env.MACVIBES_MAX_SANDBOXES ?? 8),`; `index.ts:187` reicht den Wert unverändert weiter; `sandbox/sandboxManager.ts:241` ist `while (active().length >= this.options.maxSandboxes) {`, aufgerufen aus `enter` über `sandboxManager.ts:101`. Eine Validierung existiert weder in config.ts noch in index.ts (grep nach `Number.isInteger|isFinite|isNaN|validateConfig` in beiden Dateien: kein Treffer), im Gegensatz zu `http/egressPolicy.ts:140` (`if (!Number.isInteger(port) || port <= 0 || port > 65535)`). Insofern ist die technische Kausalkette NaN → `x >= NaN === false` → keine Eviction korrekt beschrieben.\n\nTrotzdem kein Finding: Die Quelle ist nicht angreiferkontrolliert. `MACVIBES_MAX_SANDBOXES` ist eine Host-Env-Variable, die ausschließlich der Betreiber setzt (loadConfig liest `Bun.env` einmalig beim Prozessstart, config.ts:147-162); weder der als untrusted modellierte VM-Gast noch ein LAN-/angemeldeter Nutzer kann sie beeinflussen. Die vom Kandidaten selbst genannte Precondition ist ein Betreiber-Tippfehler (\"MACVIBES_MAX_SANDBOXES=16 max\") — eine hypothetische Fehlkonfiguration, kein Angriffspfad. Im Auslieferungszustand (Variable ungesetzt, in `.env.example` gar nicht erwähnt) greift der Fallback `?? 8` und das Limit funktioniert. Der einzige realistische Fehlwert, die leere Zuweisung `MACVIBES_MAX_SANDBOXES=`, ergibt `Number(\"\") === 0` und lässt den Pfad FAIL-CLOSED laufen: `0 >= 0` ist wahr, `candidates.length === 0` → `SandboxCapacityError` (sandboxManager.ts:250-253), also gar keine Sandbox statt unbegrenzt vielen.\n\nZusätzlich fällt die behauptete Auswirkung genau in die im Bedrohungsmodell ausgeschlossene Klasse: Ressourcenerschöpfung durch einen bereits angemeldeten Nutzer auf dem eigenen Server, und das erst nach vorheriger Betreiber-Fehlkonfiguration. Härtungsempfehlung ja, Schwachstelle nein."
        },
        {
          "lens": "exploitability",
          "refuted": true,
          "confidence": "HIGH",
          "reasoning": "Code-Behauptungen stimmen, die Angriffskette nicht.\n\nVerifiziert: /Users/marco/projects/macvibes/apps/server/src/config.ts:162 enthält wörtlich `maxSandboxes: Number(Bun.env.MACVIBES_MAX_SANDBOXES ?? 8)` ohne NaN-/Range-Guard; /Users/marco/projects/macvibes/apps/server/src/index.ts:187 reicht den Wert durch; /Users/marco/projects/macvibes/apps/server/src/sandbox/sandboxManager.ts:241 vergleicht `while (active().length >= this.options.maxSandboxes)`. Mit NaN wäre die Schleife tatsächlich tot (JS-Semantik) und der Cap still weg — die Mechanik als solche ist korrekt beschrieben.\n\nDie Kette reißt an der Quelle: MACVIBES_MAX_SANDBOXES ist eine Prozess-Umgebungsvariable des Host-Servers. Keine Angreifer-Rolle des Bedrohungsmodells (anonym im LAN, angemeldeter Nutzer, Code in der MicroVM) kann sie setzen. Ein repo-weiter Grep zeigt genau eine Verwendung — config.ts:162 — und keinen Pfad, über den ein Request, ein Guest-Mount oder ein API-Handler Server-Env schreibt. Die einzige Vorbedingung des Kandidaten lautet wörtlich \"Operator sets MACVIBES_MAX_SANDBOXES to a value that is not a clean number\": Betreiber-Fehlkonfiguration, keine angreiferkontrollierte Eingabe.\n\nDie Vorbedingung ist zusätzlich unrealistisch: die Variable kommt in /Users/marco/projects/macvibes/apps/server/.env.example überhaupt nicht vor (Grep findet sie nur in config.ts und in CANDIDATES-RAW.json). Ein Operator, der sie setzt, hat sie aus dem Quelltext `Number(... ?? 8)` gelesen und weiß, dass eine Zahl erwartet wird. Der Kandidat dreht \"undokumentiert\" als Plausibilitätsargument um — tatsächlich senkt es die Wahrscheinlichkeit weiter.\n\nSelbst mit geschenkter Fehlkonfiguration ist der Schaden reine Ressourcen-Erschöpfung durch einen angemeldeten Nutzer, der wiederholt Sandboxes startet. Das fällt unter die DoS-Ausnahme des Auftrags (nur real ohne gültige Session bzw. bei realistischem Aufwand) und berührt R10, das das Betreten fremder Sandboxes bewusst erlaubt (Kommentar sandboxManager.ts:242-246). Zwei unabhängige Bruchstellen: nicht angreiferkontrollierte Quelle plus akzeptierte DoS-Klasse.\n\nNebenbefund, der nichts rettet: bei leerem Wert liefert `Number('')` 0, und `active().length >= 0` ist immer wahr — dieser Fall schlägt fail-closed mit sofortiger SandboxCapacityError um und wäre für den Betreiber unmittelbar sichtbar. Ein validierender posInt-Helper ist sinnvolle Robustheit, aber kein Sicherheitsfinding in diesem Modell."
        },
        {
          "lens": "scope-and-mitigation",
          "reasoning": "Die technische Mechanik stimmt, die Angriffskette fehlt jedoch komplett an der Quelle.\n\nVerifiziert im echten Code:\n- /Users/marco/projects/macvibes/apps/server/src/config.ts:162 lautet exakt `maxSandboxes: Number(Bun.env.MACVIBES_MAX_SANDBOXES ?? 8),` — kein NaN-/Range-Guard.\n- Durchreichung bestaetigt: /Users/marco/projects/macvibes/apps/server/src/index.ts:187 `maxSandboxes: config.sandbox.maxSandboxes,`.\n- Senke bestaetigt: /Users/marco/projects/macvibes/apps/server/src/sandbox/sandboxManager.ts:241 `while (active().length >= this.options.maxSandboxes) {` — bei NaN ist der Vergleich stets false, die Schleife laeuft nie, SandboxCapacityError (sandboxManager.ts:251) wird nie geworfen.\n- Es existiert tatsaechlich KEINE Minderung: `grep -rn \"Number.isInteger|Number.isFinite|isNaN\"` in apps/server/src trifft nur egressPolicy.ts:24,32,140 und agent/daemon/main.ts:93 — die Config-Numerik ist ungeprueft. Ich erfinde hier also keine Abwehr.\n\nEntscheidend fuer die Ablehnung ist der Geltungsbereich, nicht eine Minderung:\n1. Die einzige Trigger-Quelle ist `Bun.env.MACVIBES_MAX_SANDBOXES`, also die Host-Umgebung bzw. /Users/marco/projects/macvibes/apps/server/.env. Der Kandidat gibt das in seinem eigenen Feld \"preconditions\" zu: \"Operator sets MACVIBES_MAX_SANDBOXES to a value that is not a clean number\". Kein Bedrohungsakteur des Modells kann diesen Wert setzen: weder der untrusted Gast in der MicroVM, noch ein LAN-Client, noch ein angemeldeter Nutzer. Ich habe geprueft, dass nichts im Server `Bun.env[...]` schreibt oder eine .env erzeugt (grep `Bun.env\\[` in apps/server/src und packages: kein Treffer); die .env wird nur gelesen bzw. auf Dateirechte geprueft (index.ts:350 `warnIfEnvFileReadable`). Wer die Server-Env setzen kann, hat bereits Host-Kontrolle — dann ist die Kapazitaetsgrenze irrelevant.\n2. Der cwd des Servers ist die Repo-Checkout-Verzeichnis apps/server (package.json Root-Skript `\"start\": \"... cd apps/server && bun run start\"`), waehrend Gast-Workspaces unter macvibesHome (config.ts:148, Default ~/macvibes) liegen. Ein Gast kann also auch nicht per untergeschobener .env die Bun-Autoload-Env kapern.\n3. Die Variable ist nirgends dokumentiert — `grep -rn MACVIBES_MAX_SANDBOXES` liefert ausserhalb von config.ts:162 null Treffer, insbesondere keinen Eintrag in apps/server/.env.example. Der Kandidat dreht das zum Argument fuer Plausibilitaet um; realistisch ist das Gegenteil: Wer den Namen nur aus dem Quelltext kennt, sieht dort die numerische Semantik.\n4. Der zweite Baustein des Impact-Arguments — \"enter ist bewusst ohne Ownership erreichbar\" — ist explizit Requirement R10 (\"fremde Sandboxes duerfen BEWUSST gestartet werden\") und im Code als bewusste Entscheidung kommentiert (sandboxManager.ts:242-246). Er traegt hier also nichts Neues bei.\n5. Im Default-Deployment (Variable nicht gesetzt) greift `?? 8` und der Cap funktioniert; die Tests in apps/server/src/sandbox/__tests__/sandboxManager.test.ts:216,241,255 belegen den funktionierenden Eviction-Pfad mit gueltigen Zahlen.\n\nDamit bleibt: eine Fail-Open-Robustheitsluecke bei Fehlkonfiguration durch den Betreiber selbst. Das ist genau die Kategorie \"koennte in irgendeiner Konfiguration ausnutzbar sein\" ohne angreifergesteuerte Quelle — sinnvolle Haertung (validierender posInt-Helper), aber kein zu meldendes Sicherheitsfinding unter dem macvibes-Bedrohungsmodell.",
          "refuted": true,
          "confidence": "HIGH"
        }
      ]
    }
  ]
}
