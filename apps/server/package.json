{
  "name": "@macvibes/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --hot run src/index.ts",
    "start": "bun run src/index.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "bun test",
    "test:ci": "bun test",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "baselines": "bun scripts/buildBaselines.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.200",
    "@macvibes/shared": "workspace:*",
    "@pothos/core": "^4.3.0",
    "@whatwg-node/server-plugin-cookies": "^1.0.2",
    "drizzle-orm": "^0.36.4",
    "graphql": "^16.9.0",
    "graphql-yoga": "^5.10.4"
  },
  "devDependencies": {
    "drizzle-kit": "^0.28.1"
  }
}
